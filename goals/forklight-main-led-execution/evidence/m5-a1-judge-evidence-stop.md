# M5-A1 Judge evidence stop and exact-Candidate delivery replacement

Date: 2026-08-18 (Asia/Shanghai)

## What is already accepted

Task `088d5506-0b79-4de9-bd6d-e230b115b245` completed one Grok 4.6 Xhigh native Goal and captured
Revision `56181554-d009-436b-96a9-1208ea236e87`, digest
`dd4f30324cb460ff4d82657008e23ac2bfbf00c3ee7a6247438ff8fd93ca7299`. The exact Candidate owns
the approved 16 paths. ForkLight independently passed the Workspace-local seed policy, build, all
121 focused tests and `git diff --check`. Main inspected the real diff and accepted it.

Review Graph `4748202f-33a0-4857-a71d-0a5c3cc914d5` retained one usable Codex Luna Max `accept`.
The DeepSeek Pro Task succeeded and its visible result also proposed `accept`, but ForkLight
correctly rejected the structured opinion as `unsafe-content`; it is not counted as usable.

## Why Integration stopped

Integration preflight `457e2b9d-1a9f-4636-b023-4031cb395e3d` rejected only because the Task's
required two-Judge policy has fewer than two usable terminal opinions. There is no source drift,
acceptance failure or reviewer disagreement. The Review Graph's Judge set is frozen. The existing
same-Judge repair admits only an otherwise-valid overlong summary and correctly rejects
`unsafe-content`, so it cannot be used here.

## Accepted next path

Under 一骏's standing authorization to continue later Tasks, Main admits one bounded delivery
replacement. It carries the exact accepted Candidate as a Workspace-local source patch, verifies
the exact 16-path set and source applicability before Grok starts, and runs one fresh Grok 4.6
Xhigh native Goal with unchanged A1 acceptance. Passing behavior must not be rewritten. The fresh
Revision receives exactly two new different-view read-only Judges and Main serial Integration.

The replacement has one Attempt and zero validation repair, correction, reverify, adaptation,
fallback, model switch, Competition, third Judge or Judge replacement. If acceptance changes or
either new Judge is unusable/blocking, A1 stops with the reusable Candidate and a decision packet;
no further replacement is automatic. Earlier Tasks, Graphs and Workspaces remain immutable and
protected. No commit, push, reset or Hub UI work is authorized.
