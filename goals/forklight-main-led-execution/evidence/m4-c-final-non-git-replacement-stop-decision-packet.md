# M4-C final non-Git replacement stop decision packet

Date: 2026-08-18 (Asia/Shanghai)

## Result now true

The final authorized non-Git post-state-proof replacement produced and independently verified the
exact accepted M4-C Candidate, passed two independent Judges and received Main accept, but did not
graduate M4-C because safe Integration failed its source-verification phase and rolled back.

Task `e476516f-d653-4ec4-b919-8cb425a6f18d`, Session
`3cb9a9e4-5e30-4fa6-9d8e-64842107bdbc`, used its only Attempt
`9ccc2fed-832f-490e-9e1b-a49546de891c` and completed a real Grok 4.6 Xhigh current-model-only
native Goal. Revision `12499c76-33bc-4578-9e9b-1878a81ba4b4` has digest
`d7327f8c6a0ccd5232b950af4a2ed59143f2c0ef81c846d5287c6305352d92ab`, exactly ten paths and
1,925 changed lines. Its `result.diff` is byte-identical to the two prior final M4-C Candidates.

## Candidate, verification and review evidence

ForkLight independent verification event `2615` passed all four approved commands:

- the marker plus two reverse `git apply --check` operations passed in the real plain Candidate
  Workspace;
- `npm run build` passed;
- all 343/343 focused tests passed;
- `git diff --check` passed.

Review Graph `3a7027de-ccf2-4927-8783-1293aceb9fa9` bound both opinions to that exact Revision.
Codex Luna Max and DeepSeek Pro returned two usable accepts. Main inspected the actual diff and
recorded an exact-Revision accept with the full digest. No Worker edit, retry, validation repair,
Main correction, reverify, adaptation or fallback ran.

## Integration failure and rollback

Safe preflight receipt `005005c3-0fbe-4510-81d8-daa565cf5900` found no rejection reason and bound
the exact ten paths plus source evidence. Integration operation
`4a8515d4-b9d9-407b-9024-f7f2159a8450` then:

1. applied the exact Candidate successfully;
2. ran all four source-verification commands;
3. passed build, 343/343 tests and diff validation;
4. failed only the post-state script because the fresh Integration verification project did not
   contain Task-local marker
   `.forklight/m4-c-non-git-post-state-proof-replacement.json`;
5. automatically rolled back the entire Candidate.

The non-Git fix removed the incorrect repository-metadata assumption, but the accepted command
still assumed Task-local marker state would be copied into a later Integration verification
project. `.forklight` is intentionally excluded from the Candidate, so that marker is not part of
the Revision. This is an acceptance-contract phase-boundary error, not a Grok, Candidate, build or
product-test failure.

Rollback is complete. The eight pre-existing affected files exactly match the preflight SHA-256
source evidence, both Candidate-added files are absent, project `git diff --check` passes, and
client/Daemon identity remains matched. No activated Integration exists.

## Stop boundary and reusable output

The accepted Work Item explicitly says any Integration failure stops it and grants zero retry,
reverify, Worker repair, Main correction, adaptation, fallback or further replacement. Main does
not amend the command, manually apply the Candidate, rerun Integration or create another Task.

The exact Candidate, independent verification, two usable Judge accepts, Main accept, failed
Integration and rollback evidence remain durable and reusable only if 一骏 later creates a new
explicit authority boundary. Storage preview classifies the Task
`protected / unresolved-partial`, with 140,609,160 regenerable bytes, 6,106,638 durable bytes,
zero unknown bytes, no process and Store integrity `ok/0`. Do not reclaim it while unresolved.

M4-C is stopped, M4-D remains dependency-held, and no M4 Main-Token-saving claim exists.

## Subsequent explicit authorization

On 2026-08-18 一骏 explicitly revoked this packet's final stop only for one
Integration-contract-only recovery. Main may amend the existing post-state proof so it derives the
accepted seed digests and exact ten paths without Task-local marker state, prove it in the protected
Candidate Workspace and a fresh Integration-like plain project, then create one fresh preflight
receipt and run exactly one safe Integration for the same accepted Revision. No new Task, Grok,
Candidate, reverify, correction, Judge, Main re-review, fallback or later Integration is
authorized. This authorization does not alter the failed operation or rollback evidence above.

## Subsequent delivery

The one authorized Integration-contract recovery succeeded. Receipt
`08d66a6e-ae59-413a-ae90-dd2537ac9afc` passed and operation
`1f9eac08-1b15-496b-a55d-78ddaaaf66e0` completed all four safe stages. The full check passes
3,061/3,061 and activated client/Daemon identity is matched. M4-C is graduated; delivery evidence
is `m4-c-family-value-report-delivery.md`.
