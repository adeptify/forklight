# M4-C family value report delivery

Date: 2026-08-18 (Asia/Shanghai)

## Graduated result

M4-C is integrated and activated. ForkLight now exposes one canonical read-only family Main Token
value report across Daemon, CLI and MCP. It lists every in-scope pair, proves `proven-lower` only
from a current accepted strictly-positive M4-B pair, and keeps Worker Token, runtime estimate,
native-currency cost, time, correction/handoff counts and typed missing evidence separate.

Activated CLI smoke for family `forklight-main-token-leverage` returned deterministic
`cannot-determine / uncovered-family`, zero comparisons and `createdWork: false`. No M4 saving claim
is made before M4-D supplies valid representative pairs.

## Exact Candidate and quality chain

Final Task `e476516f-d653-4ec4-b919-8cb425a6f18d`, Session
`3cb9a9e4-5e30-4fa6-9d8e-64842107bdbc`, used one Grok 4.6 Xhigh current-model-only native Goal
Attempt. Revision `12499c76-33bc-4578-9e9b-1878a81ba4b4`, digest
`d7327f8c6a0ccd5232b950af4a2ed59143f2c0ef81c846d5287c6305352d92ab`, contains exactly ten paths
and 1,925 changed lines. It is byte-identical to both prior final M4-C Candidates.

ForkLight independent verification event `2615` passed the non-Git post-state proof, build, all
343 focused tests and diff validation. Review Graph `3a7027de-ccf2-4927-8783-1293aceb9fa9`
returned two usable exact-Revision accepts from Codex Luna Max and DeepSeek Pro. Main inspected and
accepted that exact Revision and full digest.

## Integration-contract recovery and activation

The first safe Integration applied the Candidate but rolled back because its fresh verification
project lacked a Task-local marker. Build, 343 tests and diff validation passed in that failed
operation. 一骏 explicitly authorized one Integration-contract-only recovery with no new Worker,
Candidate, verification, Judge or Main review.

Main amended only the project-owned post-state proof. It now validates both accepted patch digests,
derives the exact ten-path set from the retained patch and runs the two reverse checks without
`.git`, `.forklight` or sibling Workspace state. The proof passed against the protected Candidate
and fresh Integration-like project `/tmp/forklight-m4c-integration.RJr1nO` with neither metadata
directory present.

Fresh preflight receipt `08d66a6e-ae59-413a-ae90-dd2537ac9afc` bound the unchanged full digest,
exact ten paths and current source evidence with no rejection. The sole authorized Integration
operation `1f9eac08-1b15-496b-a55d-78ddaaaf66e0` passed:

- source apply: 1/1 command;
- source verification: 4/4 commands;
- artifact build: 1/1 command;
- runtime activation: 2/2 commands.

The post-Integration full `npm run check` passes 3,061/3,061. After the expected post-build Daemon
restart, client and Daemon match build
`fb41811aa635ed7f272a6c1b42d825b0cbaa005a5a1d21150b38cd402cd6cb09` and source digest
`00c675fefa1e6132acd63cf71cb2805cacdb8844acb4bcf5e57e925b133479a5`.

## Workspace disposition

Storage preview classifies the delivered Task `reclaimable / integration-delivered`, with
140,609,448 regenerable bytes, 7,378,311 durable bytes, zero unknown bytes, no process and Store
integrity `ok/0`. No reclaim ran at this checkpoint. Candidate, review, Main and Integration
evidence remain durable. M4-D is now dependency-ready; M4 has not yet graduated.
