# M2 storage lifecycle graduation evidence

Date: 2026-08-14 (Asia/Shanghai)

## Graduated user result

ForkLight now audits one local developer's Task roots against current Store truth, previews exact
known regenerable targets, preserves durable and unknown content, rechecks eligibility before
confirmed reclaim, records terminal dispositions, reports process outcomes, and verifies SQLite
`quick_check` plus foreign keys. CLI and MCP share the same authority. A normal confirmed batch can
return after the former generic 15-second client window without cancellation, retry or replay.

Unknown top-level roots or processes are never guessed away. Running, reviewable, unresolved,
outcome-unknown, reusable-partial and explicitly retained work stays protected with a closed reason.

## Product delivery lineage

The original M2-C Goal reached its correction cap after producing reusable Candidate
`99435c50-1815-414b-9430-df6eef1b619b`. Main preserved it and used the authorized exact-Candidate
recovery Work Item rather than bypassing policy. Recovery Candidate
`5e90879d-0609-48e8-8845-f30d0e469bd2` passed ForkLight verification, two Judges, Main accept and
safe Integration `5febafdd-88ee-44d0-86ab-de5b9dbff75f`; all three recovery audits passed and the
source full check passed 2,916/2,916 tests.

The first real audit then exposed one authentic pre-`origin` Competition handoff. The narrow
compatibility Work Item normalized only that proven old read shape in memory. Corrected Candidate
`f587fd8d-deab-4b60-bccc-012011e18355`, digest
`c93a1035b9c5c7c5dcdec0a15c604428e6e65bebf95d918115488c5e0463345b`, passed build, 189 focused
tests, two Judges, Main accept and safe Integration
`aff3a159-fccc-4f1e-a161-19e9b17a88d3`. Because xAI local authentication disappeared after
activation, the already integrated source was audited through the honest DeepSeek Pro
`single-run` Goal `execution/m2-c-legacy-audits/goal.json`; its four serial zero-diff gates passed.
No Store row was migrated or rewritten.

The real batch then exposed a separate observation gap: daemon work completed in about 32 seconds,
but the generic client socket window expired at 15 seconds. The accepted transport Work Item used
DeepSeek Pro because Grok 4.6 Xhigh was still authentication-blocked. Candidate
`6d28c588-3fa8-4610-a20e-be8df0497d1d`, digest
`754aa1c278c820671d2357d1228a99fdb4c646cf7bc6826dcb488847ad70f1e9`, changed only
`src/daemon/client.ts` and `tests/daemon.test.ts` (2 files, 40 lines). ForkLight passed build,
266 focused tests and diff validation; Codex Luna Max and MiniMax M3 independently proposed
accept; Main accepted and Integration `ca098a68-2f5f-44b7-93a7-a4fe14846816` passed source apply,
source verify, build and activation. Operational Goal `execution/m2-c-reclaim-transport/goal.json`
completed 4/4, including timeout mapping, CLI/MCP parity and no-replay audits. The final source
`npm run check` passed 2,924/2,924 tests.

## First real-home preview and confirmed reclaim

Before mutation, `forklight storage audit --json` and `forklight storage preview --json` reported:

- 338 known entries: 86 protected, 248 reclaimable, 4 already reclaimed;
- zero unknown-orphan roots and zero observed Task processes;
- 1,079,179,854 reclaimable bytes across 363 known regenerable targets;
- eligible-root content preserved by design: 3,651,519,529 durable bytes and 5,192,460 unknown
  bytes;
- Store integrity `quickCheck: ok`, foreign-key violations `0`;
- exact reasons: 61 terminal reviewers, 153 integrated deliveries, 1 materialized handoff,
  24 verified remediation deliveries and 9 Main-resolved terminal Tasks.

一骏 explicitly authorized ordinary all-eligible reclaim. Durable events prove 248/248 reclaimed
dispositions from `2026-08-14T09:31:38.935Z` through `2026-08-14T09:32:10.738Z`:

- 363 targets removed, 1,079,179,854 bytes;
- target categories: baseline 23, claude-config 23, codex-home 9, codex-tmp 9, grok-home 5,
  verifier-git 23, verifier-git-index 248, workspace 23;
- zero stopped, refused or escalated processes;
- zero partial results;
- every disposition recorded retained durable categories and integrity `ok/0`.

The original CLI observer timed out before this result returned, but the daemon did not cancel or
replay the request. Main recovered truth from the durable dispositions and read-only audit instead
of resubmitting deletion.

## Post-fix real-home proof

The transport supplement naturally created one integrated implementation Task and two terminal
Judge Tasks. Exact preview named 17 known regenerable targets, 153,970,084 bytes, 2,626,162 durable
bytes to preserve, zero unknown bytes, zero processes and integrity `ok/0`. The same ordinary
authorized product path reclaimed all three. This invocation took more than the former 15-second
window and returned successfully through the corrected CLI:

- 3/3 applied; 17/17 targets removed; 153,970,084 bytes;
- one `integration-delivered` and two `reviewer-graph-terminal` reasons;
- durable logs, patches, source manifests, revisions, reviews and Integration evidence retained;
- zero process action; integrity `ok/0`.

Final real-home audit/preview after that reclaim:

- 344 entries: 254 reclaimed and 90 protected with explicit current reasons;
- zero reclaimable entries and zero eligible targets;
- zero unknown-orphan roots and zero processes;
- Store integrity `quickCheck: ok`, foreign-key violations `0`;
- next action `protect-and-wait`, not a false all-clear or deletion prompt.

The failed zero-Attempt Grok audit was explicitly resolved as `handled-elsewhere` with replacement
audit Task `e31357ee-3ada-4251-81fd-678b4d73a6e7` as evidence. Waiting Tasks in the stopped
historical Goal and unresolved/review/open-operation Tasks remain protected; no protected target
was forced through reclaim.

## Resolved-terminal supplement final preview

On 2026-08-16 the narrow resolved-terminal precedence Candidate and the separate read-only native
Goal plan-persistence correction had both completed their required review, normal Integration,
activation and full checks. The new wrapper-free live audit and the two remaining public/safety
audits all passed serially with exact zero-byte Candidates.

Main then previewed only the six historical Task ids frozen in the accepted supplement. Every one
is now `reclaimable/main-resolved-terminal`:

- 6 Tasks, 31 known regenerable targets and 685,377,515 regenerable bytes;
- 9,102,847 durable bytes preserved;
- zero unknown bytes and zero observed processes across the six;
- SQLite `quickCheck: ok` and zero foreign-key violations on every preview.

The global read-only audit reports 427 known Task entries: 97 protected, 70 reclaimable and 260
already reclaimed. It has no `unknown-orphan` classification, no observed process and integrity
`ok/0`. A separate 5,196,678 bytes of unknown content lives inside known historical roots and stays
protected; it is not treated as orphaned or eligible content.

At that checkpoint no deletion had run. 一骏 then explicitly authorized the six exact
`storage reclaim --task <id>` calls, never `--all-eligible`. A repeated fresh-preview transport
failure promoted the bounded storage-read observation Work Item; Main did not use stale evidence or
start deletion until that fix passed normal delivery gates and activated CLI preview returned.

## Storage-read observation and final exact reclaim

Observation Task `14992906-f3ac-4ff6-8555-e3859792ae88` delivered exact two-path Revision
`20662dd6-0206-434c-9e90-4797996482e1`, digest
`6354907cf0a3003bcb11cc5bb4b2a5a06158ca1519eb929661fb76db432d0134`. ForkLight verification,
Codex/DeepSeek accepts, Main accept, Integration
`8e97d3f9-0c83-43f5-b18d-5ae083ed23e0`, activation and the 2,971-test full check passed. The fix
only maps `storage_audit/storage_preview` to the existing long observation window; it adds no
retry, replay, cancellation or storage-state behavior.

Fresh activated CLI audit and six serial exact previews then reproduced the authorized boundary:
31 known regenerable targets, 685,377,515 bytes, 9,102,847 durable bytes, zero unknown bytes, zero
processes and integrity `ok/0`. Main issued exactly six confirmed per-Task reclaims:

- `f5d1142a-6eca-4d4c-8c43-801d0b284056` — 5 targets, 111,173,349 bytes removed;
- `ed0f3982-1c23-408a-a82f-4bc7ee9c43a5` — 5 targets, 110,629,983 bytes removed;
- `03090843-548b-4aec-8271-f5307344b17a` — 5 targets, 110,833,805 bytes removed;
- `710135bc-3af5-41da-bc8e-8365c4580f3d` — 5 targets, 111,180,833 bytes removed;
- `83ac853d-28bc-401c-a234-aed2f34a494e` — 5 targets, 110,755,275 bytes removed;
- `827ab13b-40ff-469d-be02-2e0c3e3b9e9b` — 6 targets, 130,804,270 bytes removed.

Every result was fully applied, recorded its terminal disposition, removed only the named known
targets, took no process action and returned integrity `ok/0`. Post-reclaim exact previews report
all six as `reclaimed/known-regenerable-removed`, with zero targets, regenerable bytes, unknown
bytes or processes. The same 9,102,847 durable bytes remain in logs, result diff, revisions, source
manifest and generated/raw patch categories.

Final global audit reports 430 entries: 97 protected, 67 unrelated reclaimable and 266 reclaimed.
There is no unknown-orphan classification or process and SQLite remains `ok/0`. The 5,196,678 bytes
of unknown content inside known historical roots remain protected. No other eligible Task was
reclaimed.

## Acceptance reconciliation

- Passed: audit/preview are read-only and share Core truth through CLI/MCP.
- Passed: confirmed reclaim re-evaluates current Store state and deletes only canonical known
  regenerable targets.
- Passed: durable/unknown content, Candidate/review/handoff/Integration/Token evidence and Store
  rows remain readable.
- Passed: authentic legacy handoff is readable without Store migration; unproven origin stays
  fail-closed at semantic consumers.
- Passed: process ambiguity, physical symlink escape, partial failure and retain integrity/bytes
  paths have focused temporary-Home tests.
- Passed: real home has no unknown orphan or observed process; integrity is `ok/0` after both
  mutation waves.
- Passed: the client observation window no longer expires at 15 seconds for `storage_reclaim`;
  ordinary short methods retain their prior bound and no retry/cancel/replay path exists.
- Passed: long real-home `storage_audit/storage_preview` scans return through the activated CLI;
  ordinary short methods retain their prior bound.
- Passed: the six exact resolved-terminal Tasks completed confirmed reclaim and their durable
  evidence remained readable through post-reclaim preview.
- Not run by design: historical backup deletion, scheduled janitor, Store compaction and Hub/UI;
  all are explicit non-goals.

M2-C is graduated. The Coding and non-Coding reviewed-delivery journeys are also complete, so this
final storage evidence closes M2 as a whole. M3 remains unopened pending 一骏's confirmation.
