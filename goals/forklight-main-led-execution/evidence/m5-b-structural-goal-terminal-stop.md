# M5-B structural-mobile native Goal terminal stop

## Outcome

ForkLight Task `2fea24e4-b053-4b18-b1b0-fa1f1a3e2d75` is terminal `failed`. Its one Grok 4.6
Xhigh current-model-only native Goal Attempt exited code `0` after 45 turns, but ForkLight recorded
`Grok native Goal status is unknown`. No Candidate Revision, independent verification, Review
Graph, Main decision or Integration exists. The Workspace disposition is `protect-review`.

## Reusable product output

The protected Workspace contains the complete seven-path Goal-first Hub Candidate. Compared with
retained Revision `8454e3cf...`, `DESIGN.md`, `src/hub/public/i18n.js` and
`tests/hub-ui-assets.test.ts` are byte-identical. The structural-mobile delta is limited to:

- `src/hub/public/index.html`
- `src/hub/public/app.css`
- `src/hub/public/app.js`
- `tests/hub-responsive-layout.test.ts`

Main ran the accepted static diagnostics directly on this exact Workspace. `npm run build` passed.
The six-file focused suite passed all 424 tests. Bootstrap syntax, JavaScript parsing and the
page-visible em/en-dash guard also passed. These diagnostics prove reusable output but do not
replace ForkLight independent acceptance or real rendered UI review.

## Exact terminal cause

Grok's host-owned Goal completion classifier launched two current-model reviewer sessions. Both
continued reading the Candidate and producing useful evidence through approximately 9 minutes 58
seconds. At 599,996ms both were canceled while entering the final verdict turn. The Runtime log
records `foreground subagent exceeded await budget` with `budget_ms=600000`, followed by both
sessions being canceled during shutdown. The parent had already written that the gating criteria
held and the remaining fit proof belonged to Main's real-browser gate, but it never produced
durable `complete + achieved` Goal truth.

This is a terminal-handoff failure, not UI acceptance. ForkLight failed closed, produced no
Revision and skipped its declared validation repair because the Worker did not return normally.

## Accepted one-time recovery

`execution/m5-wave-4/05-m5-b-exact-structural-candidate-delivery.yaml` materializes the original
seven-path seed plus structural delta
`62a29012dc7bfa1c7cdf8745d100089deb96bc28f2536a54060e6d51f4a5a397`. A clean temporary
materialization is byte-equal to the protected Workspace on all seven paths.

ForkLight still launches a real Grok native `/goal` with Grok 4.6 Xhigh current-model-only. The
wrapper changes only the Grok child to documented `GROK_WORKFLOWS=0`, selecting the legacy
model-facing `update_goal` driver that still performs completion verification, and explicitly
freezes the already accepted 24-hour foreground process breaker. The Worker is read-only. One
Attempt is allowed; validation repair, Main correction/reverify, extra Attempt, fallback,
adaptation, Competition and further automatic replacement are zero.

Success still requires ForkLight independent verification, Main's real
320/360/390/667x375/1440 interaction and motion pass, two different-view Judges and Main serial
Integration. Repetition of the same Goal-terminal loss stops M5-B with both Workspaces protected.

## Legacy-driver result and Main-direct supersession

Task `78c6fec4-b030-4b30-988b-41d01973cbdd` did not reach delivery. Its one Attempt exited code
zero after 11 turns, but ForkLight found no native Goal state under the Task-local Runtime Home and
recorded terminal `failed`. It produced zero changed files and no Candidate Revision, verification,
Review Graph or Integration. This differs from the preceding ten-minute classifier cancellation,
but still leaves no product acceptance truth.

一骏 has superseded further UI recovery Tasks: Main now owns the seven-path implementation and full
browser acceptance directly. The original structural Workspace remains protected as the exact
input. ForkLight's `main-direct` surface records the decision without pretending a Worker delivery.
Future Grok work has no model-work waiting ceiling; any forced client observation window is
non-terminal and cannot cancel or reclassify a still-running Task.
