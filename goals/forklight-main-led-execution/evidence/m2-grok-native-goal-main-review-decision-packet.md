# M2 Grok CLI native `/goal` — Main review decision packet

Recorded: 2026-08-15 Asia/Shanghai

## Stop result

The product Candidate is complete, independently verified and accepted by two usable Judges, but
Main recorded `revise`. It does not yet prove that every Grok native Goal role uses the Task's
frozen `grok-4.6/xhigh` identity. No Integration, activation or unwrapped product live Task ran.

## Reusable completed output

- ForkLight Task: `010812a2-0939-4315-9e19-ae7b892e677b`
- Honest bootstrap execution label: `auto -> persistent-session`
- Grok Session: `085b9926-b171-4ffa-ad96-dc76a3548ad3`
- Native implementation Goal: `6b724715-a358-480f-9844-98c8560edb4b`
- Native validation-repair Goal: `28e1ef0e-734e-480c-bfab-1290ebdbd398`
- Native typed-correction Goal: `2df56a31-2a0d-402e-9954-df5f4ae690d8`
- Current Candidate Revision: `00d99db6-429f-4786-b982-740f19581b31`
- Full digest: `e12f45e8d2b9daceebc1b5d53929a455e7ae0965110853b3e677960a0fd42f62`
- Scope: 14 accepted paths, 1,648 changed lines; storage and Hub paths are untouched.

The Candidate implements `/goal <contract>`, same-Session `/goal resume`, typed post-completion
successors, exact task-local state parsing, privacy-safe observations, registered planner tools with
actual Bash denied, protected auth/config/state paths, and a fail-closed terminal gate requiring
`complete + classifier attempt + achieved + readable owned details`.

ForkLight independently passed on the current Revision:

- `npm run build`
- the accepted eight-file suite: 471/471
- `git diff --check`

Review Graph `749b5210-fae8-4a6f-bde2-74eb4f41c178` bound both usable opinions to the exact current
Revision and digest:

- Codex Luna Max Task `fdcc7894-6d38-4392-b989-7ddc26deafbe`: `accept`
- DeepSeek Flash Task `a33c4c1a-d660-426d-9d0f-27cfdff49ca5`: `accept`

## Exact remaining gap

Official xAI Grok Build source at audited commit `eb267fe` resolves
`GROK_GOAL_USE_CURRENT_MODEL_ONLY` with default `false`. Its role-model resolver allows configured
or remote planner, strategist and skeptic pairs unless that kill-switch is true. The bootstrap
smoke explicitly set the flag; the product Candidate does not.

The Candidate currently sets only `GROK_GOAL=1` and `GROK_WORKFLOWS=1` for native mode. Therefore
the top-level `-m grok-4.6 --effort xhigh` arguments do not by themselves prove that all native Goal
roles preserve the frozen Worker identity. This directly blocks the accepted “exact model/effort”
and truthful Runtime-strategy requirement.

## Attempted remedies and stop boundary

The original native Goal delivered the behavior. One automatic validation-repair reduced five
compile errors to one, and one typed Main correction fixed the final exact-optional-property error.
The final verification passed. The new role-model gap was found only during Main's source-backed
review after the two Judges completed.

The Task has consumed its one Worker validation repair and one Main correction. Continuing by
editing the Workspace from Main, silently integrating and repairing afterward, lowering review, or
recreating the Task automatically would violate the accepted execution boundary.

## Workspace disposition and required decision

- Protect the complete Task Workspace, Candidate Revision, three native Goal states, verification,
  Review Graph and logs as reusable partial output.
- Do not apply Revision `00d99db6-429f-4786-b982-740f19581b31`.
- The smallest recovery reuses the exact Candidate, adds
  `GROK_GOAL_USE_CURRENT_MODEL_ONLY=1` only for `native-goal`, pins native and explicit-persistent
  env behavior in focused tests, and reruns original verification, two-Judge review, Main review,
  safe Integration and the unwrapped live Task.
- Any broader Runtime configuration system, model handshake, Store copy, hash, lock, lease or
  distributed mechanism is rejected.

The native Work Item now waits for authority to admit that one exact-Candidate recovery. The
independent, already authorized storage recovery may continue because its two paths are disjoint;
Main Integration remains serial and M3 remains closed.
