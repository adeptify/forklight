# M2 Grok native Goal Git-isolated replacement — delivery evidence

Recorded: 2026-08-16 Asia/Shanghai

## Result

The authorized wrapper-only replacement delivered the native Goal product Candidate through the
complete ForkLight quality chain. Source apply, independent verification, two fresh usable Judges,
Main accept, artifact build, Daemon activation and the high-risk full check all passed. New Grok
4.6 Xhigh Tasks now resolve `auto -> native-goal` in the activated product.

The post-Integration no-wrapper live-Task gate is still open. The replacement Task itself remains
truthfully recorded as `persistent-session` because its non-product bootstrap invoked Grok `/goal`
before the product adapter was active. Main did not relabel history or create a ceremonial Task.

## Frozen authority and execution

- Replacement Task: `847b24ef-0ac9-4958-9959-9a64b0bc466e`
- Session: `726ff5ff-db5c-4d36-8224-b0822a36e39a`
- Sole Attempt: `35aa881d-2d9e-429f-ac70-ebeeffdcd811`
- Runtime identity: `grok-build/xai/grok-4.6/xhigh`
- Recorded execution truth: `persistent-session`; operational bootstrap used real native `/goal`
- Old retained Candidate: Revision `00d99db6-429f-4786-b982-740f19581b31`, digest
  `e12f45e8d2b9daceebc1b5d53929a455e7ae0965110853b3e677960a0fd42f62`
- Fresh Candidate: Revision `6696e0b5-49f1-4f32-a68a-985068c2906e`, digest
  `251e45550486c39fb7ec9de0aff795d8bebebfe2adb6d22a4b90af2a12ca258d`
- Candidate scope: 14 paths, 1,669 changed lines; the other 12 retained paths are byte-identical and
  only `src/workers/grok.ts` plus `tests/worker-runtime.test.ts` differ from the retained Candidate.

The historical failed wrapper is unchanged. The replacement wrapper adds only
`GIT_CONFIG_GLOBAL=/dev/null` and `GIT_CONFIG_SYSTEM=/dev/null` to its internal Git subprocess.
Focused policy and sandbox proof showed that this prevents Git from reading the denied user/system
configuration without changing Candidate bytes, product policy or source state.

## Native Goal and verification truth

Runtime-owned Goal `c170a1a9-ebb7-4bea-a748-59594f4754f7` reached:

- `status: complete`
- `phase: Idle`
- `total_worker_rounds: 1`
- `classifier_runs_attempted: 1`
- `last_classifier_verdict: achieved`
- a readable classifier-details file owned by the Task-local Grok Home

ForkLight independent verification sequence `1843` passed all five accepted commands: wrapper
policy, retained-seed reverse proof excluding the two delta paths, build, the original eight-file
471-test suite, and diff validation. The focused environment assertions prove native first launch,
resume and successor all set `GROK_GOAL_USE_CURRENT_MODEL_ONLY=1`, while explicit
`persistent-session` removes an ambient copy of that native-only variable.

## Independent review and Main decision

Review Graph `8b0c7fa8-fa4a-497e-99ed-9ea98569320d` froze exactly two fresh assignments:

- Codex Luna Max Task `f4c58806-3ea7-480c-a64c-ee80b49759cd`: usable `accept`.
- DeepSeek Flash 1M Task `a721dc89-4222-4c01-824b-1858cf292bc0`: usable `accept`.

No third or replacement Judge was created. Main accepted the exact fresh Revision and digest after
the two-path comparison, native terminal-state inspection and verification review. The Codex Judge
reported one advisory reviewer-task budget warning; the accepted Candidate contract allows 14
files/1,700 lines and the actual Candidate is 14/1,669, so it was not a delivery blocker.

## Integration and activation

- Preflight receipt: `69abb29c-94db-474f-9974-0439ac6c24d3`
- Integration operation: `daa61736-5012-4ed3-9724-bd4dfeaf2b4d`
- Result: `completed/applied`
- Passed stages: source applied, source verified, artifact built, runtime activated
- Post-Integration focused suite: 471/471 passed
- High-risk full check: build plus 2,949/2,949 tests passed
- Final health: client and Daemon build identities matched; `grok-4-6-xhigh` is launchable and
  resolves to `native-goal`.

No commit, push, reset, historical relabel or storage reclaim occurred.

## Economics and remaining gate

The Grok Attempt took 1,065,395 ms, 11 turns and has a runtime cost estimate of USD 0.32089574.
Official Worker Token usage is unavailable, so ForkLight correctly reports incomplete Worker
volume and cannot claim Main-Token reduction. DeepSeek Judge cost was USD 0.585437; Codex Judge
cost evidence is unavailable.

The no-wrapper live Task remains required before the native supplement graduates. Main did not
manufacture a self-proof Task: the meaningful downstream storage recovery and Judge-result repair
Candidates are both protected behind their own explicit no-replacement/two-usable-Judge stops, and
the current authorization explicitly adds no new authority for either chain. Continuing that gate
therefore requires a new Milestone-level decision identifying the real M2 Work Item to run; M3
remains closed.

## Focused continuation feasibility audit

Main re-read current Store and source truth after activation rather than assuming an old blocker:

- Storage Task `6fbf8e70-a48a-4e7b-8cb8-3dc07e836085` still has a source-compatible, independently
  verified two-path Revision, but its immutable requirement is two usable Judges and its exact
  graph has one usable Codex accept plus one unusable DeepSeek result. Main previously recorded
  `reject`; its Workspace remains protected.
- Judge-result repair Task `b4d1fcab-215d-498f-bf8c-486ff1cc20b7` still has a source-compatible,
  independently verified 16-path Revision, but its exact graph has one usable Codex accept plus
  one unusable Volcengine result. Its correction allowance is exhausted and Integration remains
  blocked.
- The activated source contains no Judge-result repair product path. A protected Candidate cannot
  be exercised before its own Integration.
- Review Graph identity is frozen per exact Revision. The current product cannot append, replace
  or rerun one terminal assignment; a Main accept cannot override `requiredJudges: 2`.
- Re-running either Candidate, changing Judge identity, creating a third opinion, manually editing
  Store truth, lowering the gate or treating Main as a Judge would cross an explicit frozen
  boundary. None is an implicit continuation of the native replacement authority.

There is therefore no hidden ready Task that Main can start safely. The next executable decision
must explicitly identify which prior boundary is revoked and one bounded replacement behavior; it
cannot be inferred from the long-term Goal alone. Until that decision, stopping avoids a fourth
Candidate/review loop while preserving every reusable artifact.
