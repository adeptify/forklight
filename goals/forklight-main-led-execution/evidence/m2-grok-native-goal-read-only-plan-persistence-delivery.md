# M2 read-only native Goal plan-persistence delivery

## Outcome

The authorized protected-partial recovery delivered the normal Grok adapter correction without
rerunning research or widening the two-path Candidate. A real Grok 4.6 Xhigh native Goal persisted
its Task-local plan and reached `complete + achieved`; ForkLight independently verified the exact
Candidate, two Judges accepted it, Main integrated it through the normal safe path, and the new
activated read-only live audit proved plan persistence with zero Workspace change.

## Recovery Candidate

- Task: `89829dc6-be26-44ea-8517-ba901e0f1be3`
- Session: `da1ab17e-34a4-49bb-ab11-13f8f95cb50d`
- Attempt: `a03061b4-129f-47c7-9b23-e900ddda4c91`; one Attempt, zero repair, correction,
  reverify, adaptation, retry or replacement.
- Native Goal: `f1a3cbc9-1b8a-4d37-96a3-b13614383993`; `complete/Idle`, classifier `achieved`,
  one Worker round, 5,358-byte Task-local plan.
- Revision: `f934cc84-36bf-40f6-93e4-a7c849d6901c`
- Digest: `56a6a4aef264a530b7c5d8e1193ca4c458c04f1841d485b6beb52319247e83fe`
- Scope: exactly `src/workers/grok.ts` and `tests/worker-runtime.test.ts`, 143 changed lines.
- Verification sequence `2002`: build, 137 focused runtime tests, exact retained-Candidate reverse
  apply and diff validation all passed.
- Worker official usage is unavailable; ForkLight records only runtime estimate USD `0.35795404`.
  No official cost or Main Token-saving claim is made.

## Review and Integration

- Review Graph: `60ea83d9-7bf1-4cb5-9eea-ca6ba5eb001d`
- Codex Luna Max and DeepSeek Flash each returned one usable `accept` for the exact Revision.
- Main accepted after inspecting the actual diff and resolving the non-blocking sibling-path note
  against the real Task layout (`grok-home` and `workspace` are siblings).
- Preflight receipt: `6b8592af-603a-4996-b92c-8e9e37bff7f8`, no rejection.
- Integration operation: `18f71ac9-10e7-4e04-9225-22c12b3368d5`; source apply, four source
  verification commands, build and runtime activation all passed.
- Final `npm run check`: 2,971/2,971. Restarted Daemon and client build identities matched.

## Activated audits

Live audit Task `b3b78cfc-d3e8-4335-9707-a872b6d9561b` used Session
`924d27fb-e881-4f22-9622-a822fd62eae2` and one Grok 4.6 Xhigh native Goal
`e9bf72b0-957e-4efc-841e-7278768f6c5a`. The Goal is `complete/Idle`, classifier `achieved`, its
Task-local plan is 5,124 bytes, all three independent commands passed, and its Candidate diff is
exactly zero bytes. Main accepted the deterministic zero-change result under the accepted explicit
Judge skip.

Public storage compatibility Task `196931bf-15f2-4212-b90b-ebce0a853de6` passed build, focused
daemon/CLI/MCP tests and diff validation with zero Candidate change. Final scope/safety Task
`d4c8d73f-e64a-47c6-9f28-2b7903fd6bff` passed build, storage/daemon/CLI/MCP regressions and diff
validation with zero Candidate change. Main accepted both deterministic read-only results; no
Integration was needed or performed for an empty audit Candidate.

## Exact storage preview and current gate

Serial CLI preview classifies all six accepted historical Tasks as
`reclaimable/main-resolved-terminal`. Together they contain 685,377,515 regenerable bytes in 31
known targets; 9,102,847 durable bytes remain preserved. Unknown bytes are zero for all six,
processes are empty, and every preview reports SQLite `quickCheck: ok` with zero foreign-key
violations.

The global audit has no `unknown-orphan` classification or process and reports integrity `ok/0`.
Unknown content already preserved inside known historical roots remains protected. At this evidence
checkpoint no reclaim had occurred and separate destructive approval was still required.

一骏 subsequently authorized exactly those six calls. After the bounded storage-read observation
fix passed Grok native Goal execution, independent verification, two Judges, normal Integration,
activation and the 2,971-test full check, fresh activated CLI previews reproduced this exact scope.
All six serial reclaims then applied: 31 targets and 685,377,515 regenerable bytes were removed,
while all 9,102,847 durable bytes remain readable. Post-reclaim previews show zero regenerable/
unknown bytes or processes; the final global audit has no unknown orphan, no process and integrity
`ok/0`. `--all-eligible` was never used. M2 is graduated and M3 remains closed pending Milestone
confirmation. No commit, push or reset occurred.
