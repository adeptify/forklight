# M5-B Main-direct user-view Hub delivery

## Result

ForkLight Work now leads with the user's actual hierarchy instead of a form and a vocabulary
lesson. Goals contain parallel Plans; Plans contain parallel Tasks; indentation, connectors and
disclosure communicate the relationship. The root offers New Goal, each Goal offers Add Plan and
each Plan offers Add Task. Each action opens the existing outcome intake and proposal/confirmation
flow with the requested shape fixed and exact visible parent context. Nothing is presented as
created before canonical confirmation.

Independent Plans and one-off Tasks remain reachable. Search retains matching ancestors. Now,
History, Goal file, Task detail, Decision Center and System keep their existing truth/action
contracts. No Hub server, API, schema, Core, Store or daemon hierarchy changed.

## Main-direct truth

一骏 explicitly superseded further M5-B UI Workers after Task
`78c6fec4-b030-4b30-988b-41d01973cbdd` failed before Candidate capture. ForkLight Main-direct
decision `ceba063f-8ce3-4773-b8cd-fa039bd9ea17` records this bounded source implementation and is
terminal `completed` with verification `passed`. It creates no Worker Attempt, Candidate Revision,
Integration receipt or Token-saving claim. Earlier failed Grok Tasks and protected Workspaces keep
their original truth.

The final acceptance correction remains inside the approved Hub asset/test boundary:

- a contextual Add action forces its folded Goal and Plan ancestors open, so the composer is
  visible and focused at the chosen tree position;
- create submit/error/confirm state participates in visible-work retention, so refresh guards do
  not swallow user feedback;
- Task-detail requests use a monotonic generation, so close then same-id reopen cannot accept an
  older response;
- mobile loading, success and failure sheets expose one visible Back, and closing restores focus to
  the visible page Back or the original trigger as appropriate;
- the phone root New Goal control keeps a practical 44px target.

## Verification

- `npm run build`: passed.
- Six accepted Hub files: 424/424 passed.
- Final `npm run check`: 3162/3162 passed.
- `git diff --check` and standalone parsing of `app.js`/`i18n.js`: passed.
- Real browser QA passed at 320×720, 360×750, 390×844, 667×375 and 1440×900 CSS viewports. It
  covered horizontal fit, hierarchy, History/folded creation, exact parent context, add/cancel focus
  return, empty-submit recovery, search ancestry, System outside/Escape dismissal, Task-detail
  Back/focus, narrow/wide transitions and reduced-motion-safe restrained feedback.
- The original independent UX reviewer and contract/state reviewer both returned `accept` after
  correction, with no remaining Blocker/P1.

## Handoff

M5-B is graduated. M5-C is the sole active serial Work Item. It may reuse the accepted source and
M5-A3 installed-package journey for automated package, desktop/mobile, accessibility and restart
preflight. Its 30-minute journey and uncoached ten-second comprehension answers still require a
genuinely unfamiliar willing operator in a clean disposable environment; an Agent simulation,
coached answer or favorable replay is not valid evidence. No commit, push or reset occurred.

## User-friendly Goal-document revision after reopened acceptance

一骏 later rejected the first graduated surface as still too mechanical. Main preserved its safe
tree, contextual creation and detail continuity, then revised only the accepted Hub asset/test
boundary. The tree is now a document outline rather than an inventory of system objects. Repeated
visible Goal/Plan/Task badges are assistive or secondary; indentation, connectors, disclosure,
type scale and in-branch add actions explain the hierarchy.

The selected Goal now leads with its name, desired result, current truth and next meaningful
action. A real decision stays prominent, while no-decision, no-blocker, empty delivery and empty
active-task states do not each become first-screen cards. Current Plan and Task form the active
chapter. Technical detail remains folded after the Goal document. Work's redundant heading,
instructional copy and worker/count chrome are removed without removing the product navigation or
System truth. The contextual composer uses a 160ms opacity/translation transition and becomes
instant under reduced motion.

Final retained visual evidence:

- `m5-b-user-friendly-desktop-final.png` — 1440×900 Goal document plus 330px outline.
- `m5-b-user-friendly-mobile-file-final-v2.png` — mobile Goal file with Back and document-first
  reading order.
- `m5-b-user-friendly-mobile-add-plan-final.png` — Add Plan composer embedded in the exact Goal
  branch with focused input and usable actions.

Exact CDP layout measurements prove no horizontal overflow at 320, 360 and 390 CSS pixels: the
document, tree and Goal file scroll widths equal the viewport; root add, Back and decision controls
remain in bounds. The 667×375 and 1440×900 layouts also fit. Final machine evidence is:

- `npm run build`: passed.
- `node --disable-warning=ExperimentalWarning --test --import tsx tests/hub-responsive-layout.test.ts tests/hub-ui-assets.test.ts`: 269/269 passed.
- `git diff --check` plus standalone Hub script parsing: passed.
- the one required Impeccable detector run: `[]`.
- one fresh independent read-only final reviewer: `accept`, no Blocker/P1, after independently
  rerunning the scoped tests and parse/diff checks.

No backend, API, schema, Store, Core or daemon behavior changed. This revision is also Main-direct:
it creates no Worker Attempt, Candidate Revision, Integration receipt or Token-saving claim. It
closes the reopened M5-B human-acceptance gap; M5-C may continue its existing orthogonal runbook
quality chain before the genuinely unfamiliar-operator journey.

## Selected-branch disclosure acceptance

A later live operating audit found one remaining first-screen hierarchy problem: the current Store
contains six non-terminal Goals, and the tree opened all six Goal branches plus every short Plan by
default. The structure was true but the result again read as one long Task inventory. Main corrected
only the disclosure logic and focused assertions. The selected Goal and its selected/current Plan
now open by default; the other parallel Goal rows remain visible and folded.

Live acceptance against the current Store proves:

- the initial `Now` tree renders six sibling Goals, five folded, with only the selected Goal, its
  current Plan and four Tasks open;
- an explicit sibling disclosure remains open after Work → Decision Center → Work rebuild;
- a descendant search temporarily reveals the matching Goal, Plan and Task, then clearing search
  restores the user's prior fold state;
- selecting a folded Goal opens that Goal and its current Plan and changes the Goal document to the
  same canonical identity;
- at the narrow breakpoint, selecting the current Goal opens the single Goal-file pane at scroll
  top zero, Back restores the tree, the selected branch remains open and horizontal overflow is
  zero.

`npm run build` passes. The six accepted Hub files pass 425/425, `git diff --check` and standalone
`app.js` parsing pass, and the visible-script dash audit passes. This small three-path correction
adds no copy, style, action or truth adapter, so it does not rerun the already-consumed Impeccable
detector and does not add a Judge: focused pure-state assertions plus the live canonical-Store flow
fully determine its behavior. No backend, API, schema, Core, Store, daemon, CSS or i18n path changed
in this refinement. M5-B is accepted again. The prior clean bundle is now stale only as UI package
evidence and must be regenerated before a future unfamiliar-human observation.

## Decision Center hierarchy acceptance

The next live pass found 131 canonical pending decisions: four Goal-bound items and 127 parentless
review/recovery Tasks. Showing all 127 parentless Tasks as open peer cards made the route hundreds of
screens long. Main kept the four Goal groups visible and put the parentless cards, unchanged, inside
one native disclosure labeled `独立任务的决定 · 127`. Groups of up to three still open by default;
this real backlog starts folded. The explicit open/fold choice uses the existing bounded tab-local
reading state. Generic Runtime count chips no longer compete with the decisions; connection truth
remains available in System.

Live acceptance against the current Store proves:

- the route contains exactly four Goal groups plus 127 parentless cards, with only the four Goal
  cards in the initial accessibility snapshot;
- the parentless summary reports the exact count and starts closed; opening it retains all 127
  original `打开确认操作` paths and the first/last canonical Task identities;
- the open state survives Decision Center → Work → Decision Center, and deliberate close survives
  the same reading-state path;
- at the narrow override, the actual 433px CSS viewport has equal document/body scroll width,
  the summary remains a 44px target inside the viewport, and both the first and last expanded cards
  remain contained; resetting the override restores the normal viewport;
- the Decision Center chrome contains zero generic Runtime count chips, while no decision action,
  Task record or Store state changes during acceptance.

`npm run build` passes. The two focused UI files pass 271/271 and all six accepted Hub files pass
426/426. Standalone `app.js` parsing and `git diff --check` pass. The change stays inside the
accepted Main-direct UI boundary and uses the existing Decision Center surface, native disclosure
semantics and deterministic live Store assertions, so no UI Worker, new Judge or repeated
Impeccable detector is introduced. No backend, API, schema, Core, Store or daemon path changed.
The focused Work/Decision Center/System UI audit is closed. Packet `65178a8a` remains historical
technical evidence and must not be used for a future unfamiliar-human run after this source change.

## Auxiliary route, Model catalog and Limits acceptance

一骏 then asked Main to continue developing the product without waiting for the deferred unfamiliar
operator. The live complete-Hub pass found that auxiliary routes inherited the previous page's
scroll position, the Model catalog exposed every raw ID/Endpoint/Remove action as primary content,
and Limits was still a mixed-language raw settings panel.

Main corrected only the accepted Hub presentation boundary:

- changing to a non-Work route resets that destination to top-left; same-route refresh keeps its
  reading position, while returning Work restores its captured page, tree, Goal file, selection and
  disclosure context;
- the current eight models render as two-column human label/provider rows, each with one closed
  native technical disclosure containing the unchanged configuration ID, Provider model ID,
  optional Endpoint, effort values and Remove confirmation/action;
- Limits now states that it is the last shared safety line, names all four settings by consequence,
  shows the saved 1,800,000ms value as 30 minutes, preserves blank default budget as `null`, and
  says plainly that no-effective-progress is not a total runtime deadline.

Live acceptance proves Model has eight rows, eight closed disclosures and zero initially visible
raw IDs, URLs or Remove buttons; opening one disclosure reveals the exact original values and one
unchanged Remove action. Route checks prove Model and Worker start at scroll zero after navigation,
same-route scroll remains `180 → 180`, and Work page/tree scroll, selection and current Goal survive
a round trip. Limits renders its four canonical current values without saving. Desktop is a
contained two-column form; Chinese and English at the actual 433px narrow viewport are one column,
have equal document/body viewport width, no translated-label overflow, and keep inputs, unit
controls, Save and Back at 44px.

`npm run build` passes. The two focused UI files pass 277/277; all six accepted Hub files pass
432/432. Standalone `app.js`/`i18n.js` parsing, `git diff --check` and the visible-copy dash audit
pass. No backend, API, Store, Core, daemon, saved setting, model record or action changed. This is a
deterministic Main-direct refinement, so it creates no Worker Attempt, Candidate, Integration
receipt, Token claim, new Judge or second Impeccable pass. Packet `65178a8a` remains historical and
must be rebuilt only when the real human run is scheduled.

## Delivery overview-first acceptance

The next complete-Hub pass found that Delivery always mounted its configuration editor even when a
valid profile was already saved. The route exposed internal identity, exact shell commands and the
destructive action as primary content, making the product read like unfinished setup.

Main kept the existing delivery profile and save/remove contracts unchanged and corrected only the
accepted Hub presentation boundary:

- each saved profile now leads with its human label and a compact count of build, update and
  verification bindings;
- internal ID, exact commands, Edit and Remove remain available inside one native requested-detail
  disclosure;
- Create and Edit open one explicit local draft, while Cancel discards it and returns focus to the
  exact originating control;
- a same-tab language render preserves the unsaved draft, and the canonical settings Save bar stays
  absent until a real saved-setting change exists.

Live acceptance against the current saved profile proves that the closed overview contains no
internal ID or Delete action, while opening requested detail reveals the exact unchanged values and
actions. Create focused the ID field; temporary ID/label values survived a Chinese-to-English
render; Cancel removed the editor and restored Create focus. Edit loaded the saved values with a
read-only ID; Cancel restored focus to that profile's Edit action. At the actual 433px viewport the
profile, disclosure, one-column bindings and internal identity all remain contained, with 44px
disclosure and Create targets.

`npm run build` passes. The two focused UI files pass 279/279 and all six accepted Hub files pass
434/434. Standalone `app.js`/`i18n.js` parsing, `git diff --check` and the visible-copy dash audit
pass. No Save, Delete or other mutation was invoked; no backend, API, schema, Store, Core, daemon,
delivery profile or execution behavior changed. This deterministic Main-direct refinement adds no
Worker Attempt, Candidate, Integration receipt, Token claim, new Judge or second Impeccable pass.
Packet `65178a8a` remains historical and must be rebuilt only when the real human run is scheduled.

## Main-only Worker assignment guidance acceptance

The next user requirement adds one human assignment hint to each saved Worker without turning it
into a Worker instruction or an automatic routing system. `assignmentGuidance` is optional,
trimmed, limited to 1,200 characters and persisted by the existing Worker Profile validator. Blank
input clears it and legacy profiles remain valid. `ResolvedWorkerSelection` intentionally does not
contain the field, so the Runtime/model execution chain has no path to inject it into Worker input.

The new read-only `forklight_worker_catalog` projects safe saved identity, Runtime/model/default
truth and optional guidance for Main before it forms the existing explicit shortlist and routing
decision. It performs no Provider call, launch, Task creation or settings mutation. The installed
Main skill requires consideration of the guidance with task fit, user preference and readiness,
requires an honest final selection reason, and expressly forbids forwarding or paraphrasing the
guidance into Worker-facing material. Setup JSON and human output expose the same optional truth.

The Hub presents the field as “什么时候把任务交给它” / “When Main should choose this Worker”,
with plain copy saying that only Main uses it for selection. Saved rows show either a compact fit
summary or an explicit no-preference state. Create/Edit capture, language rebuild and route rebuild
retain an unsaved draft; blank save removes the field. A reproduced pre-existing restoration-order
bug was fixed by restoring only after the grouped controls are attached and by capturing a rendered
Worker form before route exit.

`npm run build` passes. The exact focused Core/Setup/CLI/MCP/Main-skill command passes 202/202 and
the accepted six-file Hub command passes 436/436. JavaScript syntax, `git diff --check` and the
temporary-debug scan pass. Live current-Store acceptance at the actual 433px CSS viewport proves a
contained 96px-tall textarea, approximately 44px Save/Next controls and exact draft retention over
Worker → Main → Worker. No Save or destructive action ran; refreshing cleared the local test draft,
and no current profile, Task, Provider or Store truth changed.
