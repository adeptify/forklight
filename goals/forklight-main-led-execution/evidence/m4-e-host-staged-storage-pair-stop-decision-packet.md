# M4-E host-staged storage pair — stop decision packet

## Outcome

This distinct subject is **incomplete evidence**, not a valid positive or negative Main Token pair.
The bounded artifact, Grok Candidate, ForkLight verification and two different-view Judge results
are usable. The sole measured delegated Main session also reached the correct semantic conclusion:
accept the exact Candidate. It did not record that decision or integrate the artifact because its
only `delivery decide` call used the wrong CLI argument form.

Per the accepted no-favorable-replay boundary, Main ran no retry, resumed session, Host-side decide,
replacement pair, extra Judge or Integration fallback. M4 remains ungraduated, Hub remains held and
M5 remains closed.

## Preserved old evidence and distinct identity

- The old valid 154,171-direct / 553,038-delegated negative remains unchanged.
- The stopped 128,302-direct root and both 46,366 / 46,796 delegated pre-admission events remain
  unchanged and were not reused.
- New Task: `214a49f1-87bf-4f41-b165-5a3ca4c68c12`.
- New comparison: `cmp-m4e-storage-host-staged-20260818`.
- New task class: `m4-host-staged-storage-lifecycle-checkpoint`.
- New subject: protected `2d774265-344f-43ea-8f69-79e2624765d3`; reclaimable
  `6676be3a-24b4-4bbc-a8c0-7f2a3079e848` and
  `b664e69e-ee30-4268-8de3-1f7c07fb808d`.
- No storage reclaim, product source change, Hub/UI work, commit, push or reset occurred.

## Direct Main evidence

- Run: `codex-run:01a012ee-f5b3-7f30-a449-4e1158163e25`, `gpt-5.6-sol / xhigh`.
- Complete terminal counters: 171,412 total input, 142,592 cache-read input, zero cache creation,
  3,124 output, 814 reasoning-output subset.
- Count-only ForkLight sample: `mus-e8d92236-e6ee-424b-ad99-d82b2a3798bc`.
- Canonical disjoint gross: **174,536** Tokens.
- Closed validator and `git diff --check` passed. The artifact has 65 lines and 2,090 bytes.
- SHA-256:
  `755016be82f5cbf8c960b0fb246238e13d245db799f608a3a5b841c881176be4`.

An earlier launcher invocation was a shell no-op: it started no model, produced no thread id or
terminal Token event and changed no file. It is not a sample.

## Deterministic Host and Worker evidence

- The Host used `delivery prepare` only for Task submission, observation, independent verification
  and frozen review staging. It made no semantic Main decision.
- Grok 4.6 Xhigh native Goal `080fb50b-186a-4ffc-bb6b-fcf61401c8c3` completed in one Attempt
  `5ec98f8c-8ab9-4168-9103-d7d7c3a2eabf` with classifier verdict `achieved`.
- Worker usage is unavailable. Runtime estimate is USD 0.1043273; official cost is unavailable
  because the terminal usage record is missing. No Worker Token or official-cost value is inferred.
- ForkLight independently passed the closed validator in 69 ms and `git diff --check` in 43 ms.
- Candidate Revision: `74f1ba9c-eb3a-47fc-a144-50bb997e19ab`.
- Candidate digest:
  `087352f491ec62d7626b96e3bd2f164fce911e2581cd425c78f3d1eae7eeed0b`.
- Candidate scope: exactly one path, 65 changed lines. Its Workspace artifact is byte-identical to
  the direct artifact and has the same SHA-256 above.

Review Graph `e103ad34-2469-4170-b30b-8e6b6a74782a` has two usable `accept` opinions:

1. Codex Luna Max assignment `2ef6e585-1047-4749-b44b-43d699c172fc`: accept, no findings.
2. DeepSeek Pro assignment `75297ca6-131f-4c3d-ba64-668f5192d4b9`: accept plus one unchanged
   `info` finding. Its original 508-character summary exceeded the 500-character schema limit.
   One accepted same-frozen-Judge summary-only repair Task
   `6018f3e8-403a-4c17-8a3d-fd0daca8691f` shortened only the summary. The original failed result
   remains durable; no Candidate rerun, Judge replacement or third Judge occurred.

## Measured delegated Main failure

- Run: `codex-run:01a012fa-57d8-76d3-b31f-eb26d3fe18a5`, `gpt-5.6-sol / xhigh`.
- Complete terminal counters: 61,716 total input, 38,400 cache-read input, zero cache creation,
  1,381 output, 730 reasoning-output subset.
- Count-only ForkLight sample: `mus-dc878353-7ec0-403d-bc40-5051bb9c82cf`.
- Canonical disjoint gross: **63,097** Tokens.

The session called the permitted `delivery prepare` once. It received the exact Revision, full
untruncated diff, passed independent verification and two usable accepts, then correctly concluded
that the Candidate satisfied the closed contract.

Its sole decision call was malformed:

```text
forklight delivery decide --task 214a49f1-... --revision ...
```

The published CLI requires the Task id as the positional argument:

```text
forklight delivery decide 214a49f1-... --decision accept --revision ...
```

ForkLight therefore returned:

```text
ForkLight error: Unknown delivery decide option: 214a49f1-87bf-4f41-b165-5a3ca4c68c12
```

This is a Main-owned command-construction error. It is not a Grok, Candidate, verifier, Judge or
ForkLight delivery-product failure. The session stopped as required. There is no Main review event,
preflight receipt, Integration receipt/result or artifact in the delegated comparison root; that
root's output remains the original zero-byte base.

## Product report truth

ForkLight durably captured both actual Main terminal events. `main-token status` reports both roles
present. It does not claim success.

`main-token pair-report` reports:

- validity: `cannot-determine`;
- reason: `incomplete-evidence`;
- direct gross: 174,536;
- delegated gross: 63,097;
- signed count difference: 111,439;
- saving: `unavailable`.

The scoped `value-report` agrees: overall `cannot-determine`, listing status `incomplete`, zero
accepted pairs and zero proven-lower pairs. The numerical difference is visible for audit but cannot
admit Hub because the delegated delivery did not complete and no M4-B assessment exists.

## Reusable output and remaining gap

Reusable and protected:

- both isolated pair roots;
- the direct artifact and exact terminal event;
- the verified Grok Candidate Workspace, Revision and digest;
- both usable Judge results and original repair lineage;
- the failed measured Main session and both count-only usage samples.

The only remaining mechanical action would be a fresh exact-bound Main decision and Integration.
The accepted Work Item explicitly forbids retry, re-entry, Host-side fallback and another pair.
Accordingly it is not performed. Any future continuation must explicitly supersede this exact
one-session/no-favorable-replay boundary; standing execution authority is not enough.

## Workspace disposition

Preserve the pair root
`/var/folders/m2/tx2tqs290l913y61zqz413dr0000gn/T/forklight-m4e-host-staged-storage-pair-4r5aVm`
and ForkLight Task Workspace while this incomplete delivery remains unresolved. Do not reclaim the
Task or either comparison root. Old failed evidence remains permanently retained.

## Authorization amendment

一骏 subsequently authorizes exactly one decision-recording-only recovery. The prior stop remains
historical truth, but its no-corrected-decide boundary is superseded for one positional
`delivery decide <task-id>` invocation bound to the same Revision and digest. The failed measured
session's semantic accept is reused; no new comparison-model session, Candidate, verification,
Judge, repair, retry, fallback or replacement pair is allowed. Success may proceed through normal
fresh preflight, safe Integration, deterministic post-state validation and one M4-B assessment.
Failure is terminal.

## Recovery result

The one authorized command succeeded. Main review sequence `1344` recorded the already measured
semantic accept. Fresh preflight receipt `b1815fd9-b890-434d-b2bc-e4412439d0eb` had no rejection;
Integration `91dfa097-3ef3-41f1-918c-77166e128430` applied and independently reran the validator
and diff check. All three artifact copies have SHA-256
`755016be82f5cbf8c960b0fb246238e13d245db799f608a3a5b841c881176be4`.

Assessment `mpa-0eb1cbb7-9c5c-42c7-a5cd-664041d3f7e2` accepts all equivalence/quality gates. The
pair is now valid and proves 111,439 fewer gross Main Tokens (174,536 direct versus 63,097
delegated; 63.848718888939814%). The original malformed command and stop remain durable history;
the recovery adds no new measured semantic session. This result admits only the next current Hub
comprehension evidence pair inside M4. Hub/UI remains untouched and M5 remains closed.
