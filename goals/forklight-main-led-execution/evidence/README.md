# Evidence

This directory indexes compact evidence required to graduate the current Goal. Raw events remain
in ForkLight Store. Evidence files summarize machine truth; they do not form a parallel progress
log.

Current boundary: M1–M4 and M5-A1/A2/A3/B are graduated. M5-B's human-first ForkLight/GoalBoard
family redesign now presents a Goal document with a structural Goal → Plan → Task outline,
contextual creation, real-decision emphasis and responsive mobile/desktop navigation. Its focused
live audit additionally opens only the selected Work branch, contains the 127 parentless Decision
Center items behind one truthful count, resets new System routes to their own reading start, keeps
model technical identities behind requested detail and renders global Limits as consequences with
human duration units. Delivery now also leads with saved human profile summaries, keeping exact
commands, internal identity and destructive action behind requested detail and mounting an editor
only after explicit Create/Edit. Worker profiles additionally accept optional Main-only assignment
guidance through the existing settings truth and a read-only catalog; Main can use it when choosing
the explicit Worker, but it is absent from resolved Worker execution and is never forwarded as a
prompt. No live profile, Task or Store record changed during acceptance. The original Main-direct
record and the later user-friendly/operating refinements, focused/full checks and exact-width
browser evidence are summarized in
[`m5-b-main-direct-user-view-delivery.md`](m5-b-main-direct-user-view-delivery.md). M5-C
unfamiliar-developer and final visual graduation remains the only missing gate, but 一骏 has deferred
its schedule. Packet `65178a8a` predates the final Decision Center, route, Model, Limits and Delivery source
corrections and is historical rather than valid future human-run input.

M5's focused entry result is [`m5-entry-audit.md`](m5-entry-audit.md). It proves matched health,
one current Grok 4.6 Xhigh smoke, the existing clean-package/delivery/read-model foundation and the
functional gaps that A1/A2 closed. Five serial accepted Work Items close setup, backup, installed
real delivery, complete Hub redesign and final human/visual acceptance in that order. The first
four are graduated; M5-C now observes the accepted product without hiding code changes inside the
human run.

M5-A1 Attempt 1 stop and reusable partial evidence are in
[`m5-a1-attempt-1-connectivity-stop.md`](m5-a1-attempt-1-connectivity-stop.md). A real Grok 4.6
Xhigh native Goal changed exactly 16 accepted paths, then lost Provider connectivity before normal
return, leaving no Candidate or verification. Read-only Workspace acceptance passes 117/121 and
proves only one compile error plus signed-in Provider/Runtime atomic selection remain. One fresh
exact-partial recovery is accepted; the failed Workspace remains protected.

The first recovery bootstrap Task stopped before model work when the new Workspace sandbox denied a
direct read of the old Task directory. The accepted replacement keeps the same exact partial in a
Workspace-local patch, validates all 16 paths and runs `git apply --check` before materialization;
it does not weaken isolation or change the A1 product contract.

M2's final exact reclaims preserved durable evidence
with no unknown orphan/process and SQLite integrity `ok/0`. M3-A, M3-B, M3-C1 and M3-C2 are all
integrated after ForkLight independent verification, different-view Judge review and Main serial
acceptance. The M3 boundary full check passes 3,022/3,022, client/Daemon identity is matched and
four natural-history projections cover one supported recommendation plus honest
cannot-determine cases without creating work. The complete boundary evidence and C2
protected-partial recovery lineage are in `m3-routing-graduation.md`. M4-A complete Main usage
capture and M4-B pair validity are integrated and activated. M4-C's final recovery and the authorized
acceptance-contract-only replacement both preserve the same product-valid Candidate. The latter
stopped on a second Main-owned non-Git acceptance error after build, 343 tests and diff validation
passed. The final non-Git replacement reproduced the same exact Candidate and passed ForkLight
verification, two usable Judge accepts and Main accept. Safe Integration then rolled back because
its fresh verification project lacked the Task-local marker required by the post-state command;
build, 343 tests and diff validation still passed. Its one Integration-contract-only recovery then
passed all four safe Integration stages. The full check passes 3,061/3,061, client/Daemon identity
is matched and live CLI smoke is read-only. M4-C is graduated. M4-D has now exhausted all three
authorized units: storage is incomplete at the required Judge/Integration gate, while worker-runtime
and historical Hub comprehension are valid same-scope/non-lower-quality pairs whose delegated Main
Token is substantially higher. The M4 boundary full check passes build plus 3,061/3,061 tests,
client/Daemon identity is matched, the live canonical report exactly matches
`main-token-pairs.json`, and the pre-existing Hub remains unchanged. M4 is not graduated, no saving
is claimed, and M5 remains closed. The focused M4-E recovery is now delivered and activated: one
verified Grok Candidate, two usable Judge accepts, exact Main accept, fresh bound preflight,
four-stage Integration, build plus 3,084/3,084 tests and matched client/Daemon identity. It composes
existing durable delivery truth into two resumable Main decision-boundary calls. One fresh
non-replay worker-runtime pair has now completed and is proven lower: direct Main 171,984 versus
delegated Main 155,804 gross Tokens, with byte-identical artifacts, equal scope/acceptance/non-lower
quality and the full ForkLight quality chain. The disclosed pre-admission sandbox smoke is not a
sample. The admitted fresh storage-lifecycle pair then completed with byte-identical artifacts and
the same full quality chain, but delegated Main used 553,038 versus 154,171 direct gross Tokens.
This accepted valid pair is `higher` by 398,867, so the Work Item's non-positive boundary stops M4
without replay. The boundary check passes build plus 3,084/3,084 tests, matched client/Daemon
identity and exact live/canonical five-comparison report parity. Hub remains dependency-held and
M5 closed. The later explicitly authorized distinct host-staged subject also stops without replay:
its verified Grok Candidate and two usable accepts were ready, but the sole measured delegated Main
used the wrong CLI argument form for its decision. Both actual counters are retained—174,536 direct
and 63,097 delegated—while ForkLight correctly reports `cannot-determine / incomplete-evidence` and
saving unavailable because no Main review, Integration or assessment exists. No Hub work started.
The later explicitly authorized decision-recording recovery reused that frozen semantic accept
without another model session. Fresh preflight and Integration passed, the three artifact copies
are byte-identical, and the M4-B assessment accepts equal scope/acceptance/non-lower quality. The
pair now proves a 111,439-Token / 63.848718888939814% reduction. Canonical six-comparison truth
marks worker-runtime and storage `proven-lower`; one distinct current Hub comprehension pair is the
only remaining M4 unit. Hub/UI remains untouched and M5 closed.
That final pair then completed with one verified Grok 4.6 Xhigh native-Goal Candidate, two usable
Judge accepts, exact Main accept and safe Integration. Direct Main is 120,989 gross Tokens;
delegated Main is 75,277, a proven 45,712-Token / 37.78194711915959% saving. All three families are
now `proven-lower`; the latest boundary record is `m4-main-token-graduation.md`.

Required evidence:

- `m4-e-storage-main-token-amplification-audit.md` — count-only proof that checkpoint payloads are
  small while 15 model turns, 140,436 gross pre-work discovery and 238,842 gross unchanged waiting
  caused the storage regression; it defines the accepted Main-offline episode product boundary.
- `m4-e-main-offline-usage-episode-delivery.md` — activated Grok 4.6 Xhigh native-Goal delivery of
  multi-session count-only role episodes, 351 focused tests, dual Judge, exact Main accept,
  four-stage Integration, 3,094-test full check and the one-new-storage-subject boundary.
- `m4-e-main-offline-storage-admission-stop-decision-packet.md` — valid 128,302-gross direct side,
  two retained 46,366/46,796-gross delegated admission segments, zero Task/sample/comparison truth,
  repeated sandboxed Daemon-start failure and the no-replacement stop before Hub.
- `m4-e-daemon-socket-probe-permission-safety-delivery.md` — two-path Grok native-Goal product fix,
  one bounded validation repair, 198-test independent acceptance, dual Judge, exact Integration,
  3,096-test full check and real EPERM proof that the live socket inode is preserved.
- `m4-e-host-staged-storage-pair-stop-decision-packet.md` — distinct current storage subject,
  174,536-direct / 63,097-delegated complete counters, exact verified Candidate and two usable
  accepts, followed by a malformed sole Main decision command, no Integration/assessment and the
  truthful `cannot-determine / incomplete-evidence` no-replay stop before Hub.
- `m4-e-host-staged-storage-pair-delivery.md` and
  `m4-calibration/forklight-storage-lifecycle-host-staged-checkpoint.json` — the explicitly
  authorized mechanical decision recovery, fresh safe Integration, byte equality, accepted M4-B
  gates and proven 111,439-Token storage-lifecycle saving that admits current Hub comprehension.
- `m4-e-current-hub-comprehension-pair-delivery.md` and
  `m4-calibration/hub-current-graduation-comprehension.json` — distinct current graduation subject,
  byte-identical one-file artifact, Grok native Goal, independent verification, two Judge accepts,
  exact Integration and accepted 45,712-Token / 37.78194711915959% Hub-comprehension saving.
- `m4-main-token-graduation.md` and `main-token-pairs.json` — canonical seven-comparison proof that
  all three required families are `proven-lower`, plus the 3,096-test, matched-build, unchanged-Hub
  M4 boundary and the mandatory wait-before-M5 decision.
- `m4-e-main-efficient-delivery.md` — exact one-Attempt Grok Candidate, 17 frozen-path proof,
  461-test independent acceptance, dual Judge, exact Main decision, fresh receipt-bound four-stage
  Integration, 3,084-test full check, matched activation and the one-pair next boundary.
- `m4-e-fresh-worker-runtime-pair-delivery.md` and
  `m4-calibration/worker-runtime-checkpoint.json` — new M4-E subject, byte-identical direct/delegated
  artifact, disclosed pre-admission smoke, checkpoint-only measured Main, complete native counters,
  dual Judge/Integration evidence and accepted 16,180-Token / 9.407851893199368% saving.
- `m4-e-fresh-storage-lifecycle-pair-stop-decision-packet.md` and
  `m4-calibration/forklight-storage-lifecycle-checkpoint.json` — current protected-versus-
  reclaimable M4-E storage truth, no reclaim, byte-identical artifacts, checkpoint-only measured
  Main, independent verification, two usable Judge accepts, exact Integration and the accepted
  non-positive 154,171-direct / 553,038-delegated boundary that stops before Hub.
- `m4-e-main-observation-audit.md` — terminal cache-read evidence, per-Task public exchange counts,
  current granular submit/review/Integration call chain, reusable decision packet and the accepted
  two-checkpoint minimum product change without a new state or coordination system.
- `m4-e-attempt-1-recovery-decision.md` — exit-0/native-Goal-unknown first Attempt, protected
  accepted-path output, diagnostic 456-test evidence, exhausted immediate same-Task correction and
  the authorized one-shot Workspace-local exact-partial recovery that still requires ForkLight
  acceptance and dual review.
- `m4-e-acceptance-contract-recovery-decision.md` — complete/achieved exact 19-path recovery,
  passing build/456 tests/reverse/diff evidence, one Main-owned source-versus-Candidate phase error,
  exact non-model attribution and the final phase-correct acceptance-only recovery boundary.
- `m4-e-fresh-preflight-stop-decision-packet.md` — verified exact 19-path final recovery, two usable
  Judge accepts, Main's fresh-preflight/exact-reentry binding gap, exact reject/no-Integration truth
  and protected reusable paths. 一骏 has now supplied the required no-further-replacement revocation;
  the accepted two-path correction is
  `../../specs/m4-e-main-efficient-delivery/work-items/fresh-preflight-binding/spec.md`.
- `m4-boundary-stop-decision-packet.md` and `main-token-pairs.json` — final three-family arithmetic,
  boundary build/3,061-test/matched-identity evidence, unchanged Hub truth, exact storage
  dispositions, evidence-supported Main observation/context diagnosis and the decision required
  before any new M4 recovery or M5 work.
- `m4-d-storage-lifecycle-pair-decision-packet.md` — byte-identical comparison preflight, complete
  direct/delegated Main terminal samples, verified Grok Candidate, one usable plus one schema-invalid
  Judge result, Main reject, absent Integration/assessment, canonical incomplete-evidence report and
  protected Workspace disposition. The unit will not be replayed.
- `m4-d-worker-runtime-pair-delivery.md` and `m4-calibration/worker-runtime.json` — byte-identical
  family artifact, complete Main counters, verified Grok native Goal Candidate, two usable accepts,
  Main accept, safe isolated Integration, accepted M4-B quality assessment and canonical valid but
  higher-delegated-Main result. The unit will not be replayed.
- `m4-d-hub-comprehension-pair-delivery.md` and
  `m4-calibration/hub-product-comprehension.json` — six historical future-M5 requirements, unchanged
  pre-existing Hub process truth, complete Main counters, verified Grok Candidate, two usable
  accepts, isolated Integration and valid but higher-delegated-Main pair. All M4-D units are now
  exhausted; the final boundary disposition is in `m4-boundary-stop-decision-packet.md`.

- `m3-entry-audit.md` — natural Store coverage, representative family routing results, current
  call-chain gaps, accepted Work Items and Wave-1 path proof.
- `m3-wave-1.md` — M3-A and M3-C1 exact Candidate, independent verification, dual Judge, Main
  Integration, full-check and activated natural-history results.
- `m3-b-verification-stop-decision-packet.md` — reusable M3-B Candidate, bounded stop, authorized
  exact recovery, fresh dual review, Main accept and safe Integration delivery.
- `m3-c2-infra-pause-decision-packet.md` — Grok authentication loss, protected 14-path partial,
  diagnostic verification, scoped contract gaps, failed same-Task feasibility, authorized
  one-shot recovery and its pre-Goal sandbox-external seed stop.
- `m3-routing-graduation.md` — all four M3 deliveries, M3-C2 protected-partial reuse, full boundary
  check, matched activation and four natural-history projections.
- `m4-entry-audit.md` — current Main-direct/sample/receipt coverage, why legacy exchange arithmetic
  is not complete Main usage, exact M4 gaps and the serial Work Item dependency proof.
- `m4-a-complete-main-usage-delivery.md` — exact native Goal Candidate, independent verification,
  dual Judge, Main decision, four-stage Integration, full-check and activated count-only
  direct/delegated Main capture boundary.
- `m4-b-valid-main-token-pair-delivery.md` — base Candidate reuse, exact legacy-evidence correction,
  fresh independent verification, fresh dual Judge, Main decision, four-stage Integration,
  3,048-test full check and activated pair-validity boundary.
- `m4-c-initial-task-stop-decision-packet.md` — initial family-report Candidate, bounded repair,
  native Goal no-progress stop, no-Worker 342/343 reverification, exhausted same-Task correction
  and the admitted exact-Candidate test-only recovery boundary.
- `m4-c-final-recovery-stop-decision-packet.md` — exact test-only recovery Revision, passing
  build/343-test/diff evidence, deterministic post-seed bootstrap contradiction, non-model failure
  attribution, protected reusable output, the superseded no-replacement boundary and the exact
  input to the authorized acceptance-contract-only replacement.
- `m4-c-acceptance-contract-replacement-stop-decision-packet.md` — byte-identical replacement
  Revision, repeated build/343-test/diff success, non-Git post-state proof failure, second non-model
  attribution, protected Workspaces, the superseded one-shot boundary and exact authority for the
  final non-Git replacement.
- `m4-c-final-non-git-replacement-stop-decision-packet.md` — exact final Candidate, passing real
  non-Git proof/build/343-test/diff evidence, two usable accepts, Main accept, Integration marker
  phase-boundary failure, complete rollback, superseded final stop and the exact one-Integration
  recovery authority.
- `m4-c-family-value-report-delivery.md` — unchanged accepted Candidate, marker-free Integration
  proof, sole recovery preflight and four-stage Integration, 3,061-test full check, matched
  activation, live read-only value-report smoke and current reclaimable storage disposition.
- `m2-delivery-graduation.md` — Coding and non-Coding interruption/recovery, review, correction,
  reuse, Main decision and Integration journeys.
- `m2-storage-lifecycle.md` — protected states, retained evidence, reclaimed space, orphan checks
  and Store integrity.
- `m2-judge-repair-storage-delivery.md` — exact one-time Judge-repair delivery exception,
  same-Judge repair, restored two-Judge storage Integration and current read-only preview.
- `m2-grok-native-goal-foreground-budget-decision-packet.md` — first wrapper-free audit's external
  600-second Grok Planner cancellation and the accepted focused Runtime fix boundary.
- `m2-grok-native-goal-read-only-plan-persistence-decision-packet.md` — one-shot adapter Task's
  reusable two-path output, external Grok authentication pause and frozen no-fallback stop.
- `m2-grok-native-goal-read-only-plan-persistence-delivery.md` — authorized protected-partial
  recovery, dual review, normal Integration, live native Goal audit, serial storage audits and the
  exact six-Task non-destructive preview.
- `m2-storage-read-transport-and-final-reclaim.md` — bounded audit/preview observation delivery,
  exact authorized six-Task reclaim results and final integrity/orphan/process evidence.
- `m3-routing-graduation.md` — three task families, recommendation, confidence, override and exact
  failure attribution.
- `main-token-pairs.json` — canonical three-family direct/delegated Main report with one incomplete
  pair, two accepted negative pairs, quality gating, separate Worker cost and no saving claim.
- `clean-clone-run.md` — installed-package current-user delivery and restart truth; it does not
  claim unfamiliar-developer evidence.
- M5-B desktop/mobile implementation QA and the two final read-only opinions are complete;
  M5-C still owns final graduation captures, accessibility/restart evidence and the genuinely
  unfamiliar-developer timed journey.

Each claim points to durable Task/Attempt/Candidate/verification/review/Integration evidence or a
reproducible command. Missing or incomparable evidence is reported as unavailable.

Current graduated M2 checkpoints:

- M2-A: operational Goal `execution/m2-a/goal.json`, 4/4 gates, exact Candidate
  `318ffa2f-74ff-4b76-b963-f435699d17c1`.
- M2-B: operational Goal `execution/m2-b/goal.json`, 4/4 gates; durable handoff successor
  `7c8d491b-26ac-4466-a7de-8a9929f75a38`; accepted Candidate
  `4b143804-2e60-4e50-8c9e-2848d8043114`; Integration
  `1b54452a-b716-439b-8672-32f890f07bee`; three zero-diff post-Integration audits.
- M2-C: graduated. The original correction-cap Candidate remains historical reuse evidence;
  authorized recovery, legacy-read compatibility and reclaim-observation supplements each used
  exact Candidate verification, independent Judges and safe Integration. Real authorized reclaim
  removed 1,079,179,854 bytes from 248 Tasks; a post-fix natural batch removed another 153,970,084
  bytes from three terminal Tasks and returned successfully past the old 15-second window. That
  wave's audit had zero reclaimable/unknown-orphan entries, zero processes and integrity `ok/0`. See
  `m2-storage-lifecycle.md`.
- M2 Coding journey: original Goal stopped before Integration at its frozen review cap. The final
  successor Candidate passes independent verification and remains protected, but lacks two usable
  exact-Revision Judges. 一骏 authorized the packet's narrow no-behavior-change recovery under
  `execution/m2-coding-review-recovery/`; exact re-materialization, two usable Judges, Main accept
  and safe Integration are complete. Its first Codex read-only audit exposed only a checkpoint/
  sandbox admission mismatch and produced zero diff. Replacement serial zero-change audits run under
  `execution/m2-coding-integrated-audits/`; the first passed, while the second passed build/tests/
  diff but failed a Workspace-local health command that requires the real activated Daemon. The
  accepted second-admission stop fired. A later authorized amended remediation preserved the failed
  Task and passed 3/4 commands, but its replacement incorrectly expected `daemon-unavailable` in
  Main's real Home; it created no delivery disposition. 一骏 then explicitly authorized the final
  path, current DeepSeek/Volcengine/MiniMax smokes passed, and the validated serial final audit Goal
  completed 4/4. Every independent verification passed and every Candidate is the exact empty
  digest, so Main graduates Coding without replaying implementation, review, or Integration. The
  accepted dependent non-Coding Goal is now running through a real Grok 4.6 Xhigh
  `persistent-session` launch. See
  `m2-coding-journey-stop-decision-packet.md` for the immutable original stop and
  `m2-coding-integrated-audit-stop-decision-packet.md` for the current decision boundary.
- M2 non-Coding journey: Grok meaningfully launched, survived one intentional same-Task/Session
  daemon restart, passed independent verification and one focused correction, then used a one-hop
  DeepSeek handoff for the final wording gap. The handoff exposed a real destination-execution
  admission defect. Authorized product Goal `execution/m2-handoff-execution-truth/goal.json`
  completed 4/4 with Grok 4.6 Xhigh, two Judges, safe Integration, the 2,940-test full check and
  three zero-diff audits. Exact recovery Goal `execution/m2-noncoding-candidate-recovery/goal.json`
  then completed 4/4: Revision `dbd23068-0b90-4b19-8eed-624d6d160b23` preserves frozen full digest
  `65deb60f3f4c6f5fe3730076e07a29cca52a0fb76019c18daf6a7d817554373a`, passed one fresh Judge and
  safe Integration, and all original audits passed. The writing/restart/correction journey was not
  replayed.
- M2 final terminal disposition: Main accepted and explicitly resolved the six zero-change audit
  Tasks, but real storage truth still reports `protected/awaiting-main-decision` and exactly
  685,377,515 regenerable bytes because stale decision-packet waits precede the existing resolution
  branch. The accepted strict-serial supplement is
  `execution/m2-resolved-terminal-storage-disposition/goal.json`. Its exact two-path Candidate is
  independently verified twice and has two usable Codex accepts across two Revision-bound Review
  Graphs, but each graph's different-view Judge failed structured admission (DeepSeek, then
  MiniMax). ForkLight stopped at the frozen 2-round review cap; current exact Revision still has
  only 1/2 usable opinions, so no Main accept, Integration, audit, or reclaim occurred. See
  `m2-resolved-terminal-storage-review-cap-decision-packet.md`. M2 delivery graduation remains
  missing pending an explicitly authorized recovery, safe Integration, three audits, exact
  six-Task reclaim, boundary check and final integrity/orphan/process audit.
  On 2026-08-16 the exact Judge-repair exception was consumed once, the original DeepSeek opinion
  became usable under the same frozen identity, and storage Revision `d6c85d59...` then passed
  fresh Main accept plus normal ForkLight Integration `c68d7eab...`. The 2,966-test full check and
  matched Daemon passed. Current exact preview names 134,988,002 regenerable bytes for the delivered
  Task while preserving 4,186,657 durable bytes; no reclaim occurred. Three serial activated-source
  audits were then admitted, but the first stopped before classifier/verification when Grok CLI's
  own 600-second foreground Planner cap canceled still-progressing work. The failed Task remains
  immutable. A focused two-path M2-A Work Item now freezes the native child breaker at 24 hours;
  its exact Candidate passed independent verification, two usable accepts, Main accept, safe
  Integration, activation and the 2,968-test full check. The new wrapper-free audit did not repeat
  the cap, but stopped because read-only native Goal omits the file mutators required to persist its
  Task-local plan. It has no Candidate or verification and stays protected. 一骏 has now authorized
  the separate one-shot read-only plan-persistence Work Item; its accepted Spec and two operational
  contracts are under `specs/m2-a-grok-execution-truth/work-items/grok-native-goal-read-only-plan-persistence/`
  and `execution/m2-grok-native-goal-read-only-plan-persistence/`.
  At that checkpoint a fresh six-Task preview and separate destructive approval still preceded
  any reclaim. See
  `m2-judge-repair-storage-delivery.md` and
  `m2-grok-native-goal-foreground-budget-decision-packet.md`.
  The later explicitly authorized protected-partial recovery has now passed one real native Goal
  Attempt, independent verification, two usable accepts, normal Main Integration, activation and
  the 2,971-test full check. Its new read-only native Goal audit persisted a 5,124-byte Task-local
  plan and reached `complete + achieved` with zero diff; both remaining serial storage audits also
  passed with zero diffs. Exact preview now reports the six named resolved Tasks as
  `reclaimable/main-resolved-terminal`, totaling 685,377,515 regenerable bytes and 31 known targets
  while preserving 9,102,847 durable bytes; unknown is zero for all six, processes are empty and
  integrity is `ok/0`. At that checkpoint no reclaim had occurred and separate destructive
  approval was still required. The later storage-read observation delivery and exact six-Task
  reclaim closed this gate: all 31 targets were removed, all durable bytes remain, and final audit
  is `ok/0` with no unknown orphan or process. See
  `m2-grok-native-goal-read-only-plan-persistence-delivery.md` and
  `m2-storage-read-transport-and-final-reclaim.md`.
- M2 Grok CLI native Goal preflight: official 1.0.3 source plus one real isolated Grok 4.6 Xhigh
  `/goal` run prove native Goal identity, paused-state truth, same-Goal resume and independently
  verified `complete + achieved`; the integrated ForkLight adapter remains honestly
  `persistent-session` until the accepted supplement graduates. See
  `m2-grok-native-goal-preflight.md`.
- M2 Grok CLI native Goal Main review stop: exact Candidate Revision
  `00d99db6-429f-4786-b982-740f19581b31` passed build, 471 focused tests, diff validation and two
  usable Judges after three native `/goal` units, but Main found that the product launch omits
  `GROK_GOAL_USE_CURRENT_MODEL_ONLY=1`. Official Runtime truth therefore allows configured or remote
  role models to escape the frozen Grok 4.6 identity. No Integration occurred; the Candidate and
  Workspace remain protected. 一骏 has now authorized one final exact-Candidate current-model-only
  recovery under `execution/m2-grok-native-goal/03-current-model-only-recovery.yaml`. Its sole
  Attempt stopped before Grok Goal creation because sandboxed Git could not read the user config;
  no Candidate, verification, Judges or Integration followed. See
  `m2-grok-native-goal-main-review-decision-packet.md` and
  `m2-grok-native-goal-current-model-only-recovery-decision-packet.md`.
  On 2026-08-16 一骏 explicitly authorized one wrapper-only replacement whose sole bootstrap delta
  is isolated global/system Git configuration. Replacement Task `847b24ef...` completed a real
  native Goal, passed ForkLight verification, two usable Judges, Main accept, safe Integration,
  activation, 471 focused tests and the 2,949-test full check. New Grok work now resolves
  `native-goal`; the bootstrap Task remains historically `persistent-session` and the meaningful
  no-wrapper live-Task gate is still pending. See
  `m2-grok-native-goal-git-isolated-replacement-delivery.md`.
- M2 exact storage recovery bootstrap stop: first Task `4ad6f4d6...` stopped on inaccessible user
  Git config; the one allowed replacement `0ee15d39...` applied the exact digest-bound two-file
  seed and passed reverse-apply, then stopped on repository-form `git diff`. 一骏 authorized one
  same-Task extra Attempt after a header-proof correction; it passed those checks but stopped before
  Grok because the child bootstrap used a sandbox-inaccessible source-tree absolute path. The
  identical Workspace-local child is proven ready, but base/extra Attempts are exhausted. No
  recovery Revision, verification, Judge, Integration, audit or reclaim exists. The reusable
  partial Workspace and next authority boundary are in
  `m2-resolved-terminal-storage-recovery-bootstrap-stop-decision-packet.md`.
  一骏 has now authorized one final no-fallback replacement after an actual sandbox-profile
  preflight proved the Workspace-local child loads and the old source path does not.
  That final Task produced and independently verified the exact Candidate, but its real native Goal
  ended `user_paused` with zero classifier runs and no `complete + achieved`. Main rejected the
  exact Revision before Judges. A focused Runtime/source audit proves the Goal Planner's required
  plan write had no permitted path: its read-only toolset lacked file write, and all five attempted
  terminal writes were denied by the explicit Bash rule. Grok then emitted
  `goal_planner_fail_closed: missing_plan_file`; its planner-failure path deliberately persists the
  generic `user_paused` status. The no-fallback recovery is terminal and its Workspace is protected.
  一骏随后授权一个新的 M2 Work Item（不是旧 Task 的 retry/replacement）：只开放 native
  Planner 的 Task-local `search_replace/write`，明确禁止 Candidate Workspace `Write/Edit`并
  继续禁止 terminal/Bash。Accepted Spec 与 operational Task 位于
  `resolved-terminal-storage-native-plan-recovery/`; it keeps one base Attempt and no fallback.
  That Task independently passed the exact Candidate suite but its Planner child failed before its
  first turn because three exposed Task-control tools required the forbidden background Bash/Task
  capability. Native Goal truth therefore has no plan, classifier or `complete + achieved`; Main
  rejected before Judges and Integration. See
  `m2-resolved-terminal-storage-native-plan-recovery-decision-packet.md`.
  A focused Grok CLI 1.0.4 semantics audit then proved the Planner is synchronous foreground work
  and the exposed `get_task_output/kill_task/wait_tasks` group is unnecessary. 一骏 authorized one
  new no-fallback Work Item that removes only this group while retaining foreground Agent,
  Task-local plan mutators, Workspace/Bash denials, the exact Candidate and all downstream gates.
  Its accepted Spec and operational contract are under
  `resolved-terminal-storage-foreground-plan-recovery/`; a live `/goal` remains the only success
  proof.
  That live Goal passed plan/classifier/`complete + achieved`, and the exact Candidate passed all
  ForkLight verification. The immutable two-Judge gate still failed because Codex returned one
  usable accept while DeepSeek returned a schema-invalid opinion. Main rejected before Integration;
  no replacement review or fallback was authorized. See
  `m2-resolved-terminal-storage-foreground-plan-recovery-decision-packet.md`.
  Its focused feasibility appendix proves current Review Graphs cannot append/replace a terminal
  Judge and records three real over-limit-summary failures. 一骏 has now authorized the explicit
  one-shot same-Judge schema-only result repair under
  `specs/m2-b-quality-decision-chain/work-items/judge-result-schema-repair/spec.md` and
  `execution/m2-judge-result-schema-repair/01-implementation.yaml`. The accepted slice repairs only
  an otherwise-valid over-limit summary, preserves the original failed assignment, keeps the same
  frozen Judge identity and assignment count, and forbids Candidate rerun, third/replacement Judge
  or automatic retry. The implementation Candidate passed ForkLight verification, but its fixed
  two-Judge gate produced one usable accept and one `extra-fields` unusable result. No Integration
  or real storage repair ran; see `m2-judge-result-schema-repair-decision-packet.md`.

- M5-A1 exact Candidate and Judge-evidence stop: Grok Task `088d5506...` produced Revision
  `56181554...`, digest `dd4f3032...`; ForkLight build, 121 focused tests and diff validation passed,
  and Main accepted the exact 16 paths. Graph `4748202f...` has one usable Codex accept and one
  DeepSeek `unsafe-content` unusable result, so Integration preflight correctly rejected the hard
  two-opinion gate. The Graph is frozen and the summary-only repair is ineligible. One final
  Task-local exact-Candidate delivery replacement is authorized with one Grok Attempt, two fresh
  Judges and zero fallback; any repeated unusable Judge stops A1. See
  `m5-a1-judge-evidence-stop.md`.

- M5-A1 final delivery-replacement bootstrap stop: Task `fbecf3af...` never reached Grok because
  the new Runtime wrapper was `0644` and direct execution returned `Permission denied`. No seed,
  Candidate, verification or Judge was created; the accepted Revision `56181554...` remains
  reusable and protected. The frozen boundary requires an explicit wrapper-mode-only replacement
  decision; no automatic retry occurred. See `m5-a1-delivery-replacement-bootstrap-stop.md`.
  一骏 subsequently authorized that exact continuation: the wrapper is `0755`, executable
  permission is checked before admission, and Task contract
  `execution/m5-wave-1/05-m5-a1-final-wrapper-mode-delivery.yaml` is the single final path.

- M5-A1 final Judge stop: Task `922238a4...` passed native Goal, exact Candidate comparison,
  build, 121 focused tests and diff validation. Codex returned usable accept; MiniMax returned
  structured accept containing the harmless `--api-key` option label, which was rejected as
  `unsafe-content`. Main rejected delivery under the hard two-opinion/no-further-replacement
  contract; no Integration occurred. A focused credential-label same-Judge repair is recommended
  but requires explicit authorization. See `m5-a1-final-judge-stop-decision-packet.md`.

- M5-A1 credential-label repair authorization: 一骏 said to continue. Focused audit proves bare
  `API_KEY`/`--api-key` labels caused the repeated false positive while no secret value existed.
  The accepted Work Item keeps actual values unsafe and reuses one same-frozen-Judge summary-only
  repair; no Candidate rerun, new assignment/Judge or Store schema is allowed. See
  `m5-a1-credential-label-repair-audit.md`.

- M5-A1 credential-label correction-loop stop: Task `4f045547...` first produced a verified
  Candidate, but Main found one out-of-scope handoff path and one missing whitespace-delimited
  actual-value case. The only correction wrote the product/test fix, then looped because its Worker
  toolset had no delete operation and Bash was denied. Main stopped the exact child after repeated
  identical failure. No corrected Revision, Judge or Integration exists; the protected partial
  passes a direct build and 44 focused tests but is not delivery evidence. See
  `m5-a1-credential-label-correction-loop-stop-decision-packet.md`.

- M5-A1Q fresh recovery authorization: 一骏 explicitly authorized the stop packet's one fresh
  three-path recovery. The existing Spec now makes handoff final-response-only. Operational Task
  `execution/m5-wave-1/07-m5-a1-credential-label-fresh-recovery.yaml` starts from current source,
  allows exactly two Core files plus one test, and preserves one Grok native Goal Attempt, unchanged
  acceptance, two Judges, Main serial Integration and zero further recovery mechanisms.

- M5-A1Q fresh-recovery Main-review stop: Task `96d11188...` produced exact three-path Revision
  `d911b924...`; ForkLight build, 411 focused tests and diff validation passed. Main rejected before
  Judges because reconcile never proves the repair summary changed, so a no-op same-Judge response
  can become usable evidence. The Task has zero correction/further replacement; no Judge,
  Integration or live repair ran. See
  `m5-a1-credential-label-fresh-recovery-main-review-stop.md`.

- M5-A1Q final exact-Candidate recovery authorization: 一骏 explicitly superseded only the latest
  no-replacement boundary. The final Task reuses verified Revision `d911b924...` through an exact
  three-path Workspace-local seed and may add only the unchanged-summary guard plus its focused
  regression. It retains one Grok 4.6 Xhigh native Goal Attempt, zero recovery mechanisms, exactly
  two different-view Judges and Main serial Integration.

- M5-A1Q final exact-Candidate Judge stop: Task `87397909...` produced exact three-path Revision
  `364f98ae...`; build, 412 focused tests, diff validation and Main's scoped seed comparison all
  passed. Codex supplied one usable accept, but Volcengine's terminal opinion was rejected as
  `unsafe-content`, leaving the hard graph at usable 1/2. Main rejected for insufficient evidence,
  ran no Integration and protects the reusable Workspace. The final contract forbids replacement,
  third Judge, repair, rerun or further replacement. See
  `m5-a1q-final-exact-candidate-judge-stop-decision-packet.md`.

- M5-A1Q final private-result safety audit: read-only parsing proves the rejected Volcengine
  opinion was exact-revision `accept` with only informational findings, but one finding copied two
  value-shaped test examples. Both current and Candidate parsers correctly reject it; summary-only
  repair cannot change the finding. This is no Candidate defect, but it is not recoverable
  label-only evidence. Any future bootstrap must declare a one-Judge exception honestly. See
  `m5-a1q-final-judge-private-safety-audit.md`.

- M5-A1Q bootstrap exception authorization: 一骏 explicitly authorized one exact-Candidate delivery
  from Revision `364f98ae...` with `requiredJudges: 1` and sole `codex-luna-max` review. It makes no
  product change, uses one Grok 4.6 Xhigh native Goal Attempt, retains zero recovery/further
  replacement and may proceed to Main serial Integration only on unchanged acceptance. This is a
  recorded one-time bootstrap exception, not a general Judge-policy change.

- `m5-a1-graduation.md` — the honest one-Judge A1Q bootstrap Integration, original A1 same-Judge
  repair and two-opinion delivery, final two-test privacy-guard alignment, 3123/3123 full check,
  matched activated Daemon and the exact boundary that advances M5 to A2 without starting Hub UI.

- `m5-a2-graduation.md` — the Grok 4.6 Xhigh native-Goal backup/recovery delivery, bounded Worker
  repair and Main correction, two usable independent accepts, zero-rejection safe Integration,
  3140/3140 full check, 14-test post-Integration temp-Home round trip, matched Daemon and the exact
  boundary that advances M5 to A3 without starting Hub UI or touching the real Home.

- `m5-a3-clean-bundle-cleanup-stop.md` — two pre-fix and one contract-authorized post-fix
  non-publishing clean-bundle cleanup stops; the delivered two-path stale-PID safety guard, its
  independent verification/two-Judge/Integration evidence; and the decision packet requiring
  bounded failure classification before any further A3 recovery. The accepted serial continuation
  is the diagnostic-only `specs/m5-product-graduation/work-items/clean-bundle-diagnosis/spec.md`:
  two writable paths, closed failure fields, no cleanup behavior change and one post-Integration
  evidence run. That run proved the Daemon-only failure; the same evidence file now records the
  113-byte socket against Darwin `sun_path[104]`, passing 93-byte install-only controls and the
  accepted `clean-bundle-short-runtime-home` recovery boundary.

- `clean-clone-run.md` — graduated M5-A3 evidence: successful socket-safe frozen bundle,
  package-local setup/Main MCP and backup, one real Grok native-Goal checkout delivery, independent
  verification, ordinary Judge, exact Main accept and one safe Integration, plus restart-stable
  one-Attempt/one-apply truth. It explicitly remains current-user technical evidence; unfamiliar
  developer and final visual comprehension stay in M5-C.

- `m5-b-final-responsive-stop.md` — final M5-B decision packet. Revision `8454e3cf...` passed
  422 focused tests and four of five independent commands but failed one test-only TypeScript build
  check. More importantly, real 390/360 QA proves the 405.3px shell and 446.6px System menu still
  clip Back and utilities. Desktop remains reusable; no Judge or Integration ran. The three
  `m5-b-final-*.jpg` files are the durable desktop/mobile visual evidence.

- `m5-b-structural-goal-terminal-stop.md` — the structural-mobile Workspace passes Main build and
  424 focused tests, but Grok's host-owned Goal completion classifier canceled both productive
  reviewers at 599,996ms and returned unknown native Goal status. ForkLight correctly captured no
  Revision or acceptance. The exact seven-path output remains protected and is admitted once
  through a read-only documented legacy-driver native `/goal` delivery Task.
  That delivery Task later failed because it created no Task-local native Goal state. 一骏 then
  superseded further UI Workers and directed Main to apply, verify and visually accept the exact
  structural output through ForkLight's `main-direct` record. The same evidence file records both
  terminal causes and the non-fabricated direct continuation.

- `m5-b-main-direct-user-view-delivery.md` — final Main-direct M5-B result plus its reopened
  user-friendly revision: Goal-document reading order, structural Goal → Plan → Task outline,
  contextual creation, folded-branch/error/focus continuity, original 424-focused/3162-full proof,
  revised 269-focused proof, exact-width browser QA, independent accepts and the handoff that opens
  M5-C without fabricating Worker or Integration truth. Final captures are
  `m5-b-user-friendly-desktop-final.png`, `m5-b-user-friendly-mobile-file-final-v2.png` and
  `m5-b-user-friendly-mobile-add-plan-final.png`.

- `m5-c-preflight.md` — first package preflight, exact stale Overview blocker, completed Grok 4.6
  Xhigh runbook quality chain, historical first packet, and the current selected-branch packet at
  `/Users/Shared/ForkLight-Clean-Run.65178a8a`. Technical verification is current; the genuinely
  unfamiliar-operator boundary remains and is not replaced by the package refresh.

- `m5-checkpoint-identity-handshake.md` — pre-publication timing root cause, exact Grok Candidate,
  two-Judge acceptance, safe Integration and the final 3,181/3,181 full-suite proof. Mutation
  compatibility now has a lightweight identity-only preflight while full health and the Daemon's
  own mutation guard remain intact.
