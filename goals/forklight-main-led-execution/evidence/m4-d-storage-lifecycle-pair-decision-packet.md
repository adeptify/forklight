# M4-D1 storage-lifecycle pair decision packet

Date: 2026-08-18 (Asia/Shanghai)

## Current truth

The first authorized M4-D comparison ran exactly once. Both fixed-profile Codex Main runs returned
complete native terminal counters, and both direct and delegated artifact bytes passed the same
closed validator. The delegated ForkLight quality chain nevertheless stopped at the required
two-Judge gate, so no Integration or valid Main Token assessment exists and no saving is claimed.

## Same-source direct result

- Main profile: `codex-main-gpt-5.6-sol-xhigh-v1` (`gpt-5.6-sol`, `xhigh`).
- Run: `codex-run:01a010a9-3ed0-7990-812b-0dae3ae319a7`.
- Output: the isolated `forklight-storage-lifecycle.json` passed the shared validator and
  `git diff --check`; it wrote no other source path and performed no reclaim.
- Usage sample: `mus-b09c1010-7fda-4747-935e-24f5b92602a9` — 33,124 uncached input, 68,608 cache
  read, 2,102 output, 0 cache creation, 103,834 gross Tokens.

The direct and delegated comparison roots were prepared in one operation from the same input,
validator, empty output and relative acceptance command. The direct artifact is retained in the
isolated pair root until this packet and the final M4 disposition are durable; it was never copied
or integrated into the source project.

## Delegated result and reusable Candidate

- Measured delegated Main run: `codex-run:01a010aa-e64f-7741-8b8a-492d3599b12f`.
- ForkLight Task: `ae76df01-44f7-4477-8c31-6e0c4f11d138`.
- Attempt: `797aef2f-0076-47ab-937a-3e97f11d9f82`; one base Attempt, no extra Attempt,
  replacement, fallback, model switch or Competition.
- Fresh Grok 4.6 Xhigh native Goal: `f104d9ca-bbe1-46a5-9059-cc44dba2aad1`.
- Candidate Revision: `40f528d5-6799-4e40-b4e7-e8155ffa0426`; full digest
  `4be2f8be200e42f0893cbfc7ac0876f8310735a78543bffc57eaf11b87bed53d`; exactly one path and
  61 changed lines.
- ForkLight verification event `1332` passed the same artifact validator and `git diff --check`.
- Review Graph `a07feb04-1ca4-4ea5-96c5-f73b4ba52322` completed with one usable Codex Luna Max
  `accept` and one unusable DeepSeek Pro `schema-violation`.

The DeepSeek opinion was otherwise an `accept`, but its third finding used unsupported severity
`low`; the accepted schema allows only `info`, `warning` or `error`. ForkLight's same-Judge result
repair is intentionally limited to an otherwise-identical over-limit summary and cannot change a
finding severity. Main therefore did not rerun the Candidate, replace the Judge, add a third Judge
or call an ineligible repair.

Root Main durably recorded `reject` against the exact Revision because only 1/2 required Judge
opinions was usable. No preflight receipt or Integration operation exists; the delegated comparison
project retains its original empty output while the verified Candidate remains protected in the
Task Workspace.

## Count-only pair evidence

- Comparison: `cmp-m4d-storage-lifecycle-20260818`.
- Delegated sample: `mus-5fc6fa6d-c09c-4d8b-8864-b42455ae65ed` — 92,530 uncached input,
  1,472,000 cache read, 11,317 output, 0 cache creation, 1,575,847 gross Tokens.
- Both roles are complete and bind to the same Task class, family and Main profile.
- Read-only pair report: `cannot-determine / incomplete-evidence`.
- Arithmetic is visible but not a saving claim: direct minus delegated is `-1,472,013` gross
  Tokens (`-1417.6599187164127%` by the canonical signed formula); `saving.status` is
  `unavailable` because delegated delivery did not pass the quality gate.

No M4-B assessment was written: there is no real delegated Integration id, and inventing one only
to persist a rejection would corrupt the evidence contract. This packet is the parent spec's
explicit substitute when a family cannot form a valid pair.

## Stop, retained evidence and next dependency

The Task storage preview is `protected / awaiting-required-review`: 19,559,159 regenerable bytes,
756,892 durable bytes, zero unknown bytes, zero owned processes and Store integrity `ok/0`. No
reclaim ran. Preserve the Task Workspace and isolated pair roots through M4's final disposition.

M4-D1 will not be replayed to seek a usable Judge or favorable Token number. Its exact decision
packet satisfies M4-D2's serial dependency, so `worker-runtime` may start next. M4 cannot graduate
unless the final evidence satisfies the Goal's valid-pair/lower-Main-Token exit or 一骏 later changes
that milestone contract; M5 remains closed.
