# ForkLight Main-led Execution — Progress

Last reconciled: 2026-08-21 (Asia/Shanghai)

## Goal state

- Status: M5 active — M1–M4 complete; 一骏 has confirmed the M4→M5 transition
- Current phase: pre-commit release checks green; unfamiliar-human M5-C gate deferred
- Overall product milestones complete: 4/5 (M1 inherited and archived; M2–M4 graduated)
- External evidence still missing: unfamiliar-human observation requires a genuinely unfamiliar
  willing operator and a clean disposable environment; 一骏 has deferred that gate and no Agent
  simulation or screenshot packet is treated as a substitute
- Main Token saving claim: worker-runtime, storage-lifecycle and Hub comprehension are proven lower

## Current M5 result

2026-08-20 preflight: 一骏 requested one bounded code review and simplification before commit/push.
The accepted Work Item is
`specs/m5-product-graduation/work-items/precommit-code-simplification/spec.md`; it is limited to the
currently changed Worker-guidance and Hub paths, preserves product behavior, and treats fewer
competing call/state paths—not raw line count—as the result. Main will rebuild/restart matched
ForkLight, run one Grok 4.6 Xhigh isolated Candidate plus one Judge, independently inspect and run
the full suite, then use 一骏's explicit commit/push authorization. No source Candidate, commit or
push exists at this checkpoint.

2026-08-20 stop: ForkLight Task `ed7d44c1-7201-4fc8-a457-87c8fe52e8d4` ultimately produced and
safely integrated exact three-path Revision `f427a0f2-ee74-421e-97ae-09bba47cd267`. Two focused
verification rounds, one usable Codex Judge and Main review accepted the simplification. Final
`npm run check`, however, passed 3,180/3,181 and twice reproduced the same out-of-scope checkpoint
start-exchange timing failure (1,306ms in the full suite, 1,693ms focused, required `< 1,000ms`).
The Work Item is stopped before commit/push rather than weakening the gate or changing checkpoint
behavior outside its Spec. Decision packet:
`evidence/m5-precommit-simplification-stop-decision-packet.md`.

2026-08-20 continuation: 一骏 authorized the narrow follow-on rather than publication with a red
gate. Main's read-only segment fixture proves the checkpoint command is not synchronously blocking
start: cold full `health` preflight was about 562 ms, while the actual `checkpoint_start` exchange
was about 1.7 ms and returned `running`. The accepted Work Item now separates mutation identity
safety from complete Runtime/Provider health, keeps both client pre-mutation and server on-receipt
guards, and forbids threshold relaxation or checkpoint behavior changes. No product-code writer
has started yet; `execution/m5-wave-6/02-checkpoint-start-identity-handshake.yaml` is the sole next
Task.

2026-08-21 completion: Grok 4.6 Xhigh Task `3971252c-a6dc-4ca9-a830-6846f7278e86`
delivered exact four-path Revision `045796d9-8486-4e16-a2d4-126ad3a142c4`, digest
`75defc38f686e3ff10ab44ba1dc71e94cb5cf4a039315076fd5eb74591fa4b26`, in one native-Goal
Attempt. Independent verification passed, Codex Luna Max and DeepSeek Pro 1M both returned usable
`accept`, Main accepted the exact Revision, and Integration `632c0b08-c07d-4440-8263-2eb499ffd91b`
passed source apply/verify, artifact build and matched Daemon activation. Final `npm run check`
passes 3,181/3,181, including the unchanged checkpoint timing gate. The release blocker is closed;
commit/push is the only remaining action in the user-authorized publication unit.

Latest truth: M5-A1/A2/A3 and M5-B are graduated while Main continues the evidence-backed
complete-Hub operating audit requested by 一骏. Work and Decision Center retain their accepted
hierarchies. Route reading continuity, the eight-model catalog and the bilingual Limits safety
form remain accepted. Delivery now also starts from what is saved rather than an always-open
configuration form: each profile leads with its human label and three-stage binding summary;
internal ID, exact commands, Edit and Remove stay in requested technical detail; Create/Edit opens
one explicit draft which Cancel can discard without changing Store state. Worker profiles now also
accept optional Main-only assignment guidance: users describe when Main should choose that Worker;
the read-only Worker catalog exposes it to Main, while resolved execution input and Worker prompts
remain unchanged. Build passes, the focused Core/Setup/CLI/MCP/Main-skill files pass 202/202 and all
six accepted Hub files pass 436/436; syntax, diff and temporary-debug checks pass. Desktop plus
actual 433px Chinese/English narrow acceptance has no horizontal overflow, preserves an unsaved
Worker draft across a Worker → Main → Worker round trip and uses 44px narrow controls. No live
Store value, existing profile, Task, Provider call or Worker execution prompt changed.

M5-C packet `/Users/Shared/ForkLight-Clean-Run.65178a8a` remains valid historical technical
evidence for the earlier selected-branch source and passed 3164/3164 prepack, isolated install,
CLI/MCP/Hub identity and clean shutdown. It is stale for a future human run because the accepted
Decision Center, route, Model, Limits and Delivery sources changed afterward. Do not rebuild another packet
until the unfamiliar-human run is actually scheduled, avoiding package churn without claiming that
missing observation.
Detailed M5 history below remains delivery trace.

一骏 has explicitly entered M5 and rejected the incumbent ForkLight Hub as a visual or structural
authority. The existing Hub remains useful only as functional/API evidence and an anti-reference;
M5 will redesign the experience rather than polish it. ForkLight and GoalBoard are confirmed as one
product family with separate truth boundaries: GoalBoard owns what the Goal is and how completion is
defined, while ForkLight owns execution, verification, Main decision and safe delivery truth. M5
does not modify the GoalBoard repository or add a cross-application data connection.

The confirmed human-first direction is durable in `PRODUCT.md`: ForkLight opens on the current
Goal's execution situation, says what is happening, why, whether the user must decide and what comes
next, and moves Runtime/Attempt/Token/log detail out of the primary narrative. Goal/Plan/Task,
Now/History and one Decision Center remain explicit. GoalBoard's product language and interaction
habits are the family reference; the old dark-sidebar/card-console composition is not retained.

Per the mandatory M5 order, the focused audit is accepted at `evidence/m5-entry-audit.md` with five
serial Work Items under `specs/m5-product-graduation/work-items/`. M5-A1 no-Hub setup, M5-A2 local
backup/recovery and M5-A3 installed-package real delivery are graduated. M5-B attempted the
confirmed GoalBoard-family human-first replacement but is now stopped on final mobile-shell
acceptance; it is not integrated. M5-C remains the final human/visual observation and is held on B.
There is no ready Writer or Integration until Main receives a new explicit boundary decision.

Current source health is `ok` with matched client/Daemon identity. The A3 frozen installed build
also remained matched across an isolated Daemon restart. M5-B's focused Hub-only audit found that
the existing work hierarchy, Goal/Task detail, History and safe mutation routes are sufficient; no
backend projection or new state model is admitted. The accepted Spec fixes one 7-path Writer
surface and the actual six-file 413/413 baseline. Original Grok 4.6 Xhigh native-Goal Task
`24f8000a-5810-43e3-9f15-098ccf94203c` is now failed and protected after one correction narrowed
the remaining acceptance failures to a type-invalid test fixture and an exact 80-pixel mobile
Goal-entry offset. Focused recovery Task `0be03566-54d9-4e01-8224-a3b1088c1128` then passed build
and 421/421 focused tests, but Main's real 390x844 QA rejected its permanent shell: product and page
bars still consumed about 196px, 返回 and 工作 wrapped vertically, and Back survived a later desktop
resize. Its verified seven-path Candidate remains reusable. Final responsive-shell Task
`64b4ac59-c3a7-4eb2-bd18-02294045be8b` then used one Grok 4.6 Xhigh native-Goal Attempt and
captured Revision `8454e3cf-ea26-4b8f-892f-234b7835e13c`. Four of five independent commands pass,
including 422/422 focused tests; build alone rejects one `string | undefined` test helper. More
importantly, final real QA proves the same mobile-fit failure remains: both shell rows are about
405.3px wide, Back is clipped at 390px and off-screen at 360px, and the System menu extends to
about 446.6px, clipping its utilities. Main recorded Candidate attribution, ran no Judges or
Integration and protects the Workspace. Evidence: `evidence/m5-b-final-responsive-stop.md`.
一骏 has since granted long-term authority for ordinary follow-on Tasks and made correct UI
interaction/motion a hard requirement. That authority supersedes the replacement stop but not the
failure evidence. The accepted continuation uses a different mobile hierarchy: identity plus three
routes in the product row, Back plus title in the page row, a viewport-fitted System menu,
outside/Escape dismissal with focus return, and restrained reduced-motion-safe feedback. It reuses
the exact Revision through Daemon submission, permits one automatic validation repair and one
evidence-based Main correction/reverification, then requires real 320/360/390/landscape/desktop QA,
two Judges and Main serial Integration. ForkLight Daemon Task
`2fea24e4-b053-4b18-b1b0-fa1f1a3e2d75` ran one Grok 4.6 Xhigh native Goal and produced the
complete structural-mobile Workspace, but it is terminal `failed`: Grok's host-owned completion
classifier canceled both still-working reviewers at 599,996ms and returned unknown native Goal
status, so ForkLight captured no Revision or independent verification. Main's read-only diagnostic
on that exact Workspace passes `npm run build` and all 424 focused tests. This is a terminal
handoff failure, not a UI rejection. The accepted follow-on materializes those exact seven paths,
uses Grok's documented legacy model-facing `/goal` driver with the existing 24-hour foreground
process breaker, permits no Candidate edits/retry/repair/correction, and then resumes ForkLight
verification, real browser QA, two Judges and Main serial Integration. Evidence:
`evidence/m5-b-structural-goal-terminal-stop.md`. ForkLight Task
`78c6fec4-b030-4b30-988b-41d01973cbdd` is now running that exact read-only delivery contract.
M5-C remains held. No commit, push or reset occurred.

M5-A1 Task `245a5dec-eb63-4c6b-9971-2e503c55109d` then produced an exact 16-path partial but lost
Provider connectivity before normal Worker return, so it has no Candidate or ForkLight
verification. Main's read-only Workspace acceptance passes 117/121 focused tests and narrows the
remaining product gap to one unused parameter plus atomic signed-in Provider/compatible-Runtime
selection. Exact evidence and the accepted fresh native-Goal partial recovery are in
`evidence/m5-a1-attempt-1-connectivity-stop.md` and the recovery section of the A1 spec. The failed
Workspace stays protected; no whole-Task restart or source Integration occurred.

The first recovery bootstrap Task `8835a223-117b-4066-981c-cf583e063037` then stopped before a
model turn because the new Task sandbox correctly denied its direct read of the old Task directory.
It produced no product change or Candidate. Main replaced only that operational materialization
method with a Workspace-local exact 16-path patch; its path-set policy test, `git apply --check`,
Task quality 100 and Workspace boundary pass. The next accepted recovery retains the same two
product fixes, Grok 4.6 Xhigh native Goal, acceptance and two-Judge/Main chain.

That Workspace-local recovery has now completed. Task `088d5506-0b79-4de9-bd6d-e230b115b245`
captured exact 16-path Revision `56181554-d009-436b-96a9-1208ea236e87`; build, all 121 focused
tests and diff validation passed under ForkLight, and Main accepted the actual diff. The Review
Graph retained one usable Codex accept, while the DeepSeek output was unusable under the existing
`unsafe-content` gate. Integration preflight correctly rejected the undersized two-Judge evidence.
Because the frozen Graph cannot add/replace a Judge and the summary-only repair is ineligible, one
final exact-Candidate delivery replacement is now ready. It changes no passing behavior, has one
Grok Attempt and zero fallback; any new unusable Judge stops A1. Evidence:
`evidence/m5-a1-judge-evidence-stop.md`.

The final exact-Candidate delivery replacement then stopped before Grok received the Goal. Task
`fbecf3af-9d43-434f-b8fe-422462daacbe` exited `71` because its new wrapper was created as
`0644` and ForkLight correctly could not execute it. There were zero model turns, no seed
materialization, Candidate, verification or Judge. The already accepted Revision remains protected
and source product paths are unchanged. The frozen no-further-replacement boundary now applies;
continuation requires an explicit wrapper-mode-only replacement decision. Evidence:
`evidence/m5-a1-delivery-replacement-bootstrap-stop.md`.

一骏 has now explicitly said to continue, revoking only that boundary for one final Task. The
existing wrapper mode is corrected to `0755`, the policy test now asserts executable permission,
and `execution/m5-wave-1/05-m5-a1-final-wrapper-mode-delivery.yaml` preserves the exact accepted
Candidate, one Grok 4.6 Xhigh native Goal, unchanged acceptance, exactly two fresh Judges and zero
fallback/further replacement.

The final Task `922238a4-ac7c-461f-b37c-2b2384800fee` completed the native Goal and unchanged
acceptance; Revision `f7729c3c-f9ad-45eb-a856-f016c2394200` is byte-identical to the accepted
Candidate, with build and 121/121 focused tests passing. Codex supplied one usable accept. MiniMax
also proposed accept but repeated the harmless CLI option name `--api-key`, which the current
credential pattern rejects as `unsafe-content`. Main rejected delivery under the frozen
two-opinion/no-further-replacement contract; no Integration occurred. M5-A1 is stopped pending an
explicit decision on the focused credential-label same-Judge repair recommended in
`evidence/m5-a1-final-judge-stop-decision-packet.md`.

一骏 has explicitly authorized that focused continuation. Audit
`evidence/m5-a1-credential-label-repair-audit.md` proves the false positive is the bare
`API_KEY`/`--api-key` label pattern, not a secret value. Accepted Work Item
`specs/m5-product-graduation/work-items/credential-label-judge-repair/spec.md` reuses the existing
one-shot same-Judge lifecycle, preserves actual-value rejection and forbids A1 rerun/new Judges.
It is the sole current Writer before A1 can receive a fresh Main decision.

Task `4f045547-f262-46b6-820f-ea95d19e507a` produced independently verified Revision
`78023cfc-9bfa-408d-846a-70784143a390`, but Main correctly requested revision: the Candidate carried
one out-of-scope handoff file and missed a common whitespace-delimited CLI credential-value case.
The sole correction Attempt added the missing detector and regression in the protected Workspace,
then repeated the same unavailable file-delete/shell actions because this Worker exposes no delete
tool and Bash is denied. Main stopped the exact Worker after the no-progress rule was satisfied.
The Task is failed, no corrected Revision/Judge/Integration exists, and the source is unchanged.
Direct diagnostic build plus 44 focused tests pass only as reusable-partial evidence. Exact packet:
`evidence/m5-a1-credential-label-correction-loop-stop-decision-packet.md`. M5-A2/A3 and Hub remain
held until Main receives authority for the packet's one fresh three-path recovery boundary.

一骏 has now explicitly authorized that exact continuation. The accepted Spec clarifies that
handoff exists only in the Worker final response, never as a file. Operational Task
`execution/m5-wave-1/07-m5-a1-credential-label-fresh-recovery.yaml` starts from current source,
permits exactly the two Core files and one test file, requires the whitespace-delimited value case,
and uses one Grok 4.6 Xhigh native Goal Attempt with zero correction, repair, retry, reverify,
adaptation, fallback or further replacement. Independent acceptance, two different-view Judges
and Main serial Integration remain mandatory.

The authorized Task `96d11188-a4c2-4901-9caa-7300487ed125` completed one native Goal and captured
exact three-path Revision `d911b924-3c78-4ca1-bb28-7b2c58c04b15`, digest
`3bdb6e6188f8bff913c853365d57ded7fe3d335cbdc8fb742a2e8baedfb3b2df`. ForkLight independently
passed build, all 411 focused tests and diff validation. Main nevertheless rejected it before
Judges: reconcile proves schemaVersion/Revision/disposition/findings equality but never proves the
summary changed, so an unchanged summary can be counted as a successful repair. The contract
explicitly forbids that. No Judge, Integration, activation or live repair ran. Exact packet:
`evidence/m5-a1-credential-label-fresh-recovery-main-review-stop.md`. M5-A2/A3 and Hub remain held.

一骏 has explicitly superseded only that final boundary. The accepted Spec now admits one last
exact-Candidate recovery from verified Revision `d911b924-3c78-4ca1-bb28-7b2c58c04b15`. Its sole
new behavior is to reject a repair whose summary did not actually change, plus one focused test.
The complete Candidate remains exactly three paths; the Worker may edit only the Core reconcile
path and its focused test on top of the seed. One Grok 4.6 Xhigh native Goal Attempt, zero recovery
mechanisms, two different-view Judges and Main serial Integration remain hard boundaries.

That final Task `87397909-9163-4a74-b0f5-f0b12b667854` succeeded in one native Goal Attempt.
Revision `364f98ae-741b-452e-b5d1-5bc8100c69d1`, digest
`99cc6bb9d1c5c952494440e068109b61b5c5495c9565103488588af8e58d1a84`, contains exactly the three
accepted paths. ForkLight build, all 412 focused tests and diff validation passed. Main's direct
seed comparison confirms the only new behavior is the three-line unchanged-summary guard plus one
focused regression; the third seed file did not change.

Review Graph `0a0e55b5-58cb-4186-ac53-dc85ed1e17b7` retained one usable Codex accept. The Volcengine
result was terminal `unsafe-content`, so the graph is `single-opinion` and Integration remains
blocked at usable 1/2. Main recorded reject without rejecting the reusable Candidate. The final
contract permits no replacement/third Judge, repair, rerun, fallback, Integration or further
replacement. Exact packet:
`evidence/m5-a1q-final-exact-candidate-judge-stop-decision-packet.md`.

A focused read-only private-result audit then evaluated the rejected Volcengine text without
printing or persisting it. The opinion was exact-revision `accept` with four `info` findings and no
requested Candidate change, but finding 0 copied credential assignment and CLI whitespace-value
test shapes. The accepted Candidate intentionally rejects both, so its historical label-only
summary repair is also ineligible. This confirms no Candidate defect while disproving the idea that
activation could repair this specific Judge. Any bootstrap must be an explicitly declared one-Judge
exception, not a promised future two-opinion recovery. Evidence:
`evidence/m5-a1q-final-judge-private-safety-audit.md`.

一骏 has explicitly authorized that minimum bootstrap exception. The accepted Spec now permits one
new exact-Candidate delivery Task with `requiredJudges: 1` and `codex-luna-max` as the sole
independent opinion. It reuses Revision `364f98ae...` byte-for-byte, changes no product behavior,
runs one Grok 4.6 Xhigh native Goal Attempt and has zero correction, repair, retry, fallback or
further replacement. Passing evidence allows Main serial Integration and activation; this exception
does not weaken the normal high-risk two-Judge policy or make the Volcengine result usable.

That exception completed honestly. Bootstrap Task `67467033-b84e-44b4-baa2-2ce5f4a0514a`
produced the exact three-path digest, passed build plus 412 focused tests, received one usable Codex
accept and completed safe Integration `468f36b0-0182-4bde-b05d-675c2e70836c`. Main then invoked the
accepted same-frozen-MiniMax repair exactly once on the original A1 Graph. It preserved the original
failure, produced two usable accepts and allowed the unchanged 16-path A1 Candidate to complete
safe Integration `7d75856f-3eee-42b9-9be2-26e6b077e372`.

The first post-Integration full check found two stale test expectations, not setup behavior defects.
One final two-test Work Item narrowed a routing-only privacy assertion and admitted the bounded
public `setup` projection while preserving exact shape and fake-key/diagnostic bans. Task
`097b7357-fa44-4a53-bfc5-298e416a52b8` changed exactly two test paths and 26 lines, passed all five
independent commands including 3123/3123 full tests, received one usable Codex accept and completed
Integration `a03fbbd3-45cd-4205-b474-d279f8c57351`. Client/Daemon identity is matched. M5-A1 is
graduated; M5-A2 is now the sole ready serial Work Item. Evidence: `evidence/m5-a1-graduation.md`.

M5-A2 then completed through the normal high-risk chain. Grok 4.6 Xhigh Task
`9c5e67ed-86f3-417b-acce-c153ffab1360` used truthful native Goals, one allowed Worker
validation repair and one allowed Main correction. Main reused the passing Candidate and narrowed
only real gaps: byte-read-only inspect, a second owner observation immediately before the Home
switch, and explicit recovery/staging truth when activation plus rollback both fail. Final Revision
`53432923-6a6a-44c6-8e38-5b603077706b`, digest
`0fda88f701b0625c8ff5dbf15464807ad8889561958d3d9b321354f0064484f7`, contains exactly 15
accepted non-UI paths and passed build, 386 focused tests and diff validation.

Fresh Review Graph `8822aa98-c625-447f-a313-a8916abe2d39` reached two usable independent accepts
after the one eligible same-MiniMax schema-only result repair; no third Judge or Candidate rerun was
used. Main recorded a fresh exact accept. Receipt `b61b28a4-7d6b-4bb2-b054-3f1f4c87d835` had zero
rejection and Integration `c0d6ae71-ccde-48c8-a9eb-fe532beac375` applied normally. Post-Integration
`npm run check` passed 3140/3140, the dedicated temp-Home recovery suite passed 14/14 and client/
Daemon identity matches. The real current Home was not restored or replaced. M5-A2 is graduated;
M5-A3 is now the sole ready serial Work Item, while Hub remains dependency-held. Evidence:
`evidence/m5-a2-graduation.md`.

M5-A3's first two fresh external bundle destinations both stopped with the same
`cleanup-failed` result after the installed lifecycle and published nothing. A separate
install-only diagnostic proved the exact Hub exits immediately and the installed public
`daemon stop` returns success with its old PID gone in one second. The focused call chain then
found that bundle cleanup unconditionally probes/signals the stored numeric PID again after that
authoritative stop; after exit, the number can be reused and no longer proves ownership.

The exact two-path recovery was delivered through ForkLight Task
`00a083f1-87f6-42cd-b1c4-260d18e23922`. Corrected Revision
`08b61065-b563-408f-a801-294a1212debc` passed independent verification and two fresh Judges, then
completed safe Integration `0cfebb72-16b7-4c8e-912d-c45b544fa8fa`. The post-Integration full check
passed 3,143/3,143 and client/Daemon build identity matched.

The sole authorized post-fix bundle run nevertheless repeated the same `cleanup-failed` terminal
and published nothing. Because the current error hides the prior verification failure and does not
identify whether Hub or Daemon cleanup stayed false, the original PID-reuse hypothesis is no longer
enough to justify another behavior patch. A3 is stopped under the no-loop rule. Its integrated
guard and all delivery evidence remain reusable; M5-B/C remain dependency-held. The next admissible
boundary is now accepted under 一骏's standing authorization: one diagnostic-only Work Item writes
only the clean-run catch path and focused test, exposes the hidden failure category plus
returned/thrown and `hubGone`/`daemonGone` closed values, and changes no cleanup behavior. One Grok
4.6 Xhigh native Goal, independent verification, two Judges and serial Main Integration precede a
single diagnostic external run. No UI or behavior recovery is ready. Accepted Spec:
`specs/m5-product-graduation/work-items/clean-bundle-diagnosis/spec.md`; prior packet:
`evidence/m5-a3-clean-bundle-cleanup-stop.md`.

That diagnostic Work Item has now completed. Task `91321ce5-c8ae-4154-8da3-a58d87eb5750`, exact
Revision `e5466ab0-d7e2-4d93-83c0-77fcc28cabe0` and two usable Judges reached safe Integration
`e282a286-e92b-4483-ad48-3121ceaa3a22`; the source full check passed 3,147/3,147. The single
diagnostic run returned `prior=daemon-identity-mismatch cleanup=returned hubGone=true
daemonGone=false` and published nothing.

Focused install-only reproduction proves the root cause. The bundle's staging-derived
`forklight.sock` is 113 UTF-8 bytes, but the local macOS SDK defines `sun_path[104]` including the
terminating null. Hub can report ready after its internal Daemon start fails, producing the exact
diagnostic sequence. The same package passes both immediate identity and authoritative-stop flows
from a 93-byte socket path, so no retry or timeout change is admitted.

The sole ready recovery moves only ephemeral `FORKLIGHT_HOME` to one unique owner-private OS temp
root, guards the Darwin byte boundary before launch, and removes only that exact root after owned
process cleanup. npm staging/artifacts/publication and global Daemon/Hub behavior remain unchanged.
It uses Grok 4.6 Xhigh native Goal, independent verification, two Judges and Main serial
Integration. Accepted Spec:
`specs/m5-product-graduation/work-items/clean-bundle-short-runtime-home/spec.md`. M5-B/C remain held.

That recovery and the installed real-delivery journey are now complete. Task
`bc1d331e-bad4-4fe3-8160-689a6b17bf85` passed independent verification and two Judges, then safe
Integration `0b961c0d-42fc-426b-b814-f0ae4e8201c9`; the source full check passed 3,151/3,151. The
single admitted bundle published four artifacts, matched tarball SHA/build truth and left no
`flh-*` runtime root.

The package was then installed into `/Users/yijunwang/code/fl-a3-01`, configured for xAI plus
`grok-4-6-xhigh` native Goal and package-local Grok MCP, and backed up through installed CLI only.
One untouched packaged checkout fixture became Task `30f6bba0-697f-4b32-8ce4-9b9c3775235a`.
ForkLight independently verified its one-file/five-line Candidate; one ordinary independent Judge
accepted it, Main accepted the exact Revision and Integration
`65040e5f-dbe4-4b9b-8830-6a633f731069` applied once. After installed-Daemon restart the Task still
showed one Attempt, one review and one applied receipt/result, and all four tests passed. This is a
current-user technical proof, not M5-C unfamiliar-human evidence. M5-A3 is graduated and M5-B is
the sole ready serial Work Item. Evidence: `evidence/clean-clone-run.md`.

## Current M4 result

The focused audit stopped at the existing Codex terminal usage → direct calibration → Task Token
report → economics → Daemon/CLI/MCP chain. ForkLight already separates Worker Token, runtime
estimate, official native-currency cost and count-only CLI/MCP receipts, so M4 will reuse them.

Current Store truth has 13 completed Main-direct decisions but none records Main Token. It has
11,590 orchestration exchange receipts, which measure only the ForkLight request/response boundary.
The two pre-M4 direct-Codex calibration samples are old task classes with no task family and no
explicit same-scope/same-acceptance/quality-not-lower contract. Their large
`directCodexSavings` ranges remain legacy boundary estimates, not M4 Main Token proof. M4-D has now
replaced that gap with one visible incomplete storage comparison and two accepted negative pairs.
Canonical `evidence/main-token-pairs.json` reports `cannot-determine` with reasons
`incomplete-evidence` and `not-strictly-positive`; it creates no work and supports no saving claim.

Four accepted self-contained Work Items define the minimum complete chain:

- M4-A captures complete role-aware direct/delegated Main terminal usage and makes no claim.
- M4-B admits a pair only through explicit equivalence/quality gates plus current delegated
  verification, Main accept and Integration.
- M4-C reports family Main change beside Worker Token, cost, time and correction truth.
- M4-D creates one explicitly approved calibration pair for each required family, without model
  ranking, repeated favorable reruns or Hub/UI work.

M4-A/B/C share StateStore and public entry files and therefore run serially. M4-D's artifact paths
are disjoint, but one measured Main profile and serial Integration are hidden ordering dependencies,
so its comparisons also run serially. Exact evidence and accepted boundaries are
`evidence/m4-entry-audit.md` and `specs/m4-{a,b,c,d}-*/spec.md`.

M4-D now has three self-contained serial Work Items plus one shared closed validator. Its focused
tests pass for all three family projections, extra-field rejection and private-path rejection.
`gpt-5.6-sol / xhigh` returns the required complete terminal event, and the first comparison roots
were prepared together from byte-identical input/validator/output baselines.

The storage-lifecycle comparison ran once. Direct Main artifact validation passed and sample
`mus-b09c1010-7fda-4747-935e-24f5b92602a9` records 103,834 gross Tokens. Grok Task
`ae76df01-44f7-4477-8c31-6e0c4f11d138` produced verified exact one-path Revision
`40f528d5-6799-4e40-b4e7-e8155ffa0426`, but Review Graph
`a07feb04-1ca4-4ea5-96c5-f73b4ba52322` had only one usable opinion: DeepSeek returned unsupported
severity `low`, which is outside the existing summary-only same-Judge repair. Main rejected the
exact Revision; no receipt or Integration exists. Delegated sample
`mus-5fc6fa6d-c09c-4d8b-8864-b42455ae65ed` records 1,575,847 gross Tokens. The read-only pair is
`cannot-determine / incomplete-evidence`, and no assessment or saving claim was invented. The
Candidate Workspace remains protected with zero unknown bytes/processes and integrity `ok/0`.
Exact stop and reusable evidence are in
`evidence/m4-d-storage-lifecycle-pair-decision-packet.md`. M4-D1 will not be replayed.

M4-D2 worker-runtime completed from a fresh byte-identical pair. Direct Main sample
`mus-4cdfafba-01da-4729-9c5e-006f271179a3` is 152,271 gross Tokens. Grok Task
`e4218692-0cac-409b-a998-378374b3553c` produced exact one-path Revision
`2a742c97-abff-4319-9292-475764378fbf`; verification `1780`, two usable accepts, Main accept and
Integration `aae1189c-dd73-4c2a-bc3b-c76e42def29c` passed with no correction. Delegated sample
`mus-2c768d0b-1133-4cb4-a015-2e499f421d58` is 2,401,089 gross Tokens. Assessment
`mpa-417947aa-42ff-4e7d-8619-dec5aa14f0b5` accepted same-scope/same-acceptance/quality, but the
pair has signed change -2,248,818 and `saving.status: higher`; family value is
`cannot-determine / not-strictly-positive`. Exact artifact and evidence are
`evidence/m4-calibration/worker-runtime.json` and
`evidence/m4-d-worker-runtime-pair-delivery.md`. It will not be rerun.

D3 preflight found an already-existing Hub process at PID 52551, port 62182, reported as
`different-build`. This process predates the calibration and is outside M4 authority. The accepted
D3 boundary is reconciled to require the exact before/after observation to remain unchanged; no
measured run may start, stop, restart, replace, open or modify Hub/UI.

M4-D3 historical Hub comprehension then completed from a fresh byte-identical pair. Direct sample
`mus-0786eabc-37bf-4bf2-9f1c-96f1044a0e13` is 119,202 gross Tokens. Grok Task
`db2caa85-e8e7-4fcd-aaf8-6dc84ca4660b` produced exact one-path Revision
`bacb156c-38d1-430c-a70e-bb191309ff55`; verification `1311`, two usable accepts, Main accept and
Integration `e4208850-e99b-4467-b673-82a3c35a8ab6` passed with no correction. Delegated sample
`mus-55ae7497-bde1-4572-8e0a-2ff11bb36af0` is 2,128,876 gross Tokens. Assessment
`mpa-5bda4279-7e59-4676-a01e-08f889f7363d` accepted equivalence/quality, but signed change is
-2,009,674 and `saving.status: higher`; family value is
`cannot-determine / not-strictly-positive`. The Hub process remains the same PID 52551, port 62182,
state `different-build`. Exact artifact and evidence are
`evidence/m4-calibration/hub-product-comprehension.json` and
`evidence/m4-d-hub-comprehension-pair-delivery.md`.

All three one-run M4-D units are now exhausted and none contributes proven-lower Main Token. D1 is
incomplete evidence; D2/D3 are accepted negative pairs. No replay, new Judge, model switch or extra
calibration is allowed. The M4 boundary full check passes build plus 3,061/3,061 tests; after
restart, client and Daemon identities match build `c454d894...` and source digest `00c675fe...`.
Fresh canonical report output is byte-identical to `evidence/main-token-pairs.json`,
`git diff --check` passes, and the pre-existing Hub remains PID 52551 / port 62182 /
`different-build` without interaction. M4 is not graduated and M5 remains closed.

The measured delegated runs are dominated by repeated cache-read/context Token across the long
Worker → verification → dual-Judge → Main decision → Integration observation chain. This is the
focused evidence-supported diagnosis, not a claim about unread private history. Main recommends a
new M4 observation/resume-efficiency Work Item only if 一骏 explicitly authorizes revising the
accepted M4 plan/spec; otherwise the Goal stops here. Exact evidence and storage dispositions are
in `evidence/m4-boundary-stop-decision-packet.md`.

一骏 explicitly authorized the recommended M4 recovery on 2026-08-18. Focused source and live CLI
audit now narrows the gap to repeated Main interaction boundaries, not Worker quality: the two
accepted Tasks expose 26 and 15 attributed ForkLight receipts, with Review Graph calls additional;
current delivery requires separate submit/wait/inspect/review/main-review/preflight/apply/wait
steps. Existing `MainDecisionPacket`, idempotent exact Review Graph, Candidate binding and safe
Integration can be composed without a new state entity or coordination mechanism.

The accepted serial Work Item is `specs/m4-e-main-efficient-delivery/spec.md`. It adds two resumable
CLI/API checkpoints: prepare runs deterministic execution/review plumbing and stops for Main;
decide exact-binds Main's decision and integrates only an explicit accept. Observation timeout
never cancels or fails underlying work. One Grok 4.6 Xhigh native Goal Writer owns the overlapping
Core/Daemon/CLI/MCP paths; no parallel writer is orthogonal. After activation, one fresh meaningful
worker-runtime pair must prove real improvement before any additional family calibration starts.
Exact audit: `evidence/m4-e-main-observation-audit.md`.

Operational Task `6928dd28-bf5a-413c-9545-59ce96648b99` is now queued under Session
`65b1032c-2436-4f1f-87b6-f66874279149`. Admission passed quality 100, clear Workspace boundary and
safe Integration feasibility; Grok 4.6 Xhigh resolves `native-goal`. The Task has no duration or
Token ceiling, one base Attempt, current bounded validation repair/Main correction policy and no
parallel Writer.

The base Attempt exited `0` after 92 turns but did not close the native Goal: a Grok foreground
subagent was auto-backgrounded at its internal await boundary, the parent turn ended, and ForkLight
truthfully recorded `Grok native Goal status is unknown`. No Candidate or independent verification
was invented. The protected Workspace has exactly 19 changed files, all within the accepted paths,
and no Hub/UI change. Main diagnostic build and the focused suite pass 456/456; per-path diff check
finds only two README trailing spaces. The accepted one Main correction is now used to preserve the
same Task/Session/Workspace, remove only those spaces, avoid subagents and explicitly close the
native Goal. ForkLight must still perform the unchanged independent acceptance before review.
Decision: `evidence/m4-e-attempt-1-recovery-decision.md`.

The reserved Main correction was admitted as ordinal 2 but failed immediately with exit `1`, zero
model turns, no changed file and the same unknown native Goal state. The original Task is now
stopped and protected. Applying 一骏's standing later-Task authorization, Main prepared one fresh
exact recovery: `execution/m4-e/retained-candidate.diff` contains the 19 accepted paths with only
the two proven README spaces removed; its source-base check passes. The Workspace-local wrapper
and policy test use only the fresh Task snapshot, Grok 4.6 Xhigh native Goal and Git config isolation.
Operational Task `execution/m4-e/02-protected-partial-recovery.yaml` passes quality 100, native-Goal
resolution, unlimited runtime/Token policy, clear Workspace and safe Integration feasibility. It
has one Attempt and zero repair/correction/reverify/adaptation/retry/fallback/further replacement.
ForkLight admitted Task `7894fcfc-f0d3-4493-b065-28cef14f281a` under fresh Session
`a7a865f7-1e02-4273-a775-b5c9e31a82f5`; the original failed Task remains protected.

That recovery completed native Goal `f57ff860-d1d3-4f93-8f77-2b6266718a39` as
`complete/Idle + achieved` with no subagent and captured Revision
`bec48c2e-084c-4a0b-be79-5be9729b49dc`, digest
`14091d2f34503f81bda4641a87569236027804d08591fe8746a3a0956564b0fd`. Build, 456 focused tests,
exact 19-path reverse applicability and diff validation passed. Only the Main-owned wrapper policy
test failed because it used forward source mode after materialization. Exact event `3513` records
`acceptance-contract/non-model`; no Judge or Integration started.

The final accepted recovery is `execution/m4-e/03-acceptance-contract-recovery.yaml`. Its
operational test now requires explicit `--source` before admission and `--candidate` during
acceptance; the retained product patch is unchanged. Source-mode policy, retained source-base,
Task quality 100, native Goal resolution, clear Workspace and safe Integration feasibility must all
pass before submission. One Attempt, zero repair/correction/reverify/adaptation/retry/fallback or
further replacement. Evidence: `evidence/m4-e-acceptance-contract-recovery-decision.md`.
ForkLight admitted Task `2d774265-344f-43ea-8f69-79e2624765d3` under fresh Session
`b3adc009-fd2f-44cb-ab7a-3bc9727cef94`.

The Task completed native Goal without a subagent. Verification `2986` passed the corrected
Candidate-phase policy check, build, all 456 focused tests, exact 19-path reverse applicability and
diff validation. Revision `1910682c-398d-471a-8abd-02f297a5b500`, digest
`14091d2f34503f81bda4641a87569236027804d08591fe8746a3a0956564b0fd`, received two usable
independent accept proposals in Graph `34d87eee-410c-477c-ac60-44d7fd21f2bb`.

Main actual-diff review nevertheless found one untested contract gap: `decideMainDelivery` records
accept, then reuses any latest Task preflight receipt. An older rejected granular preflight created
before the accept can poison the first valid decide call forever, while an unrelated unconsumed
receipt is not proven to belong to this bound decide flow. The contract requires one fresh
post-accept preflight on the first call and reuse only on exact re-entry. Main rejected the exact
Revision; no preflight or Integration started. The final Task forbids further replacement, so M4-E
stops pending explicit revocation. Packet: `evidence/m4-e-fresh-preflight-stop-decision-packet.md`.

一骏 has now explicitly confirmed that revocation and authorized exactly one focused replacement.
Accepted Work Item
`specs/m4-e-main-efficient-delivery/work-items/fresh-preflight-binding/spec.md` and operational
contract `execution/m4-e/04-fresh-preflight-correction.yaml` reuse the verified 19-path Candidate.
Only `src/core/main-delivery.ts` and `tests/main-delivery.test.ts` may change on top of the seed;
the operational gate proves the other 17 paths remain exact. The design uses existing ordered Task
events, receipt ids and operation receipt ids to create one fresh post-accept preflight and bind
exact re-entry. It adds no schema/entity, lock/hash/lease/version mechanism or automatic retry.
Task validation passes quality 100, clear Workspace boundary, native-goal resolution, unlimited
duration/Token/no-progress policy and safe Integration feasibility. Submission, independent
verification, two fresh Judges and Main serial Integration are the current chain; M5 remains closed.

ForkLight admitted Task `3e2740eb-4c4e-4a55-9a80-86c51c35a5b5` under Session
`04ef556b-465e-43f7-8dd5-44171857fda4`. Current Grok 4.6 Xhigh direct smoke returned
`M4_E_FRESH_PREFLIGHT_GROK_OK`; the Task is queued as the only Writer with one base Attempt and
zero fallback. Original rejected/recovery Tasks remain immutable and protected.

The unique Attempt completed real Grok native Goal and ForkLight independent verification without
repair or correction. Revision `fe127fd9-d4c3-42c2-826e-21eea52af2f4`, digest
`42a6a2b51220d33a96bea8d279a6f6e068d89c595a8ab61b17f06b1d7998a371`, contains the retained 19
paths and 3,719 changed lines. Operational verification proves all 17 non-editable paths remain
exact; build, 461/461 focused tests and diff validation pass at sequence `2646`. Main's scoped
two-path review confirms the implementation chooses the first receipt after the matching Main
review, requires matching operation receipt identity and adds five mixed granular/delivery
regressions.

Fresh Review Graph `013c2594-c9cf-4f3c-ab15-e30a1239b5bf` then completed with two usable accept
proposals from Codex Luna Max and DeepSeek Pro. Main inspected the real two-path delta and accepted
the exact Revision/digest. Fresh preflight receipt `2b0075f8-b7fa-4dd0-b6e4-1b9de1e9b844` had no
rejection reason; Integration result `c3879c21-0b57-4b64-be68-3dd666942181` passed source apply,
source verification, artifact build and runtime activation. No repair, correction, reverify,
retry, fallback or replacement was used.

Post-Integration `npm run check` passes build plus 3,084/3,084 tests, `git diff --check` passes and
root CLI help exposes the two accepted delivery commands. The full check regenerated build identity,
so Main performed the accepted idle Daemon restart; health is now matched at build `e8e5e35e...`
and source digest `79c7aa0c...`. M4-E is delivered and activated. Exact evidence:
`evidence/m4-e-main-efficient-delivery.md`. M4 is not graduated and no saving is claimed. Its sole
next ready gate is one fresh meaningful non-replay `worker-runtime` pair; M5 remains closed.

That next gate is now specified at
`specs/m4-e-main-efficient-delivery/work-items/fresh-worker-runtime-pair/spec.md`. It projects the
new M4-E delivery into `worker-runtime-checkpoint.json`, rather than replaying the old M3-C2
`worker-runtime.json`. The direct and delegated roots receive byte-identical bounded input,
validator, empty output, task text and acceptance. Both Main sides use `gpt-5.6-sol / xhigh`;
delegated Main may use only one `delivery prepare` and one exact `delivery decide`, with exact
re-entry only if observation times out. Focused preparation and the existing three-family validator
regression pass; no product, old evidence, Hub/UI or Store path changed. This is the sole active M4
comparison and no later family is admitted yet.

Direct Main completed once and passed the shared validator plus diff check with only the expected
69-line artifact; its native terminal usage is complete. The first delegated invocation exposed a
pre-admission harness gap: Codex workspace sandbox denied ForkLight's local control-plane path and
ended before any Task existed. Store confirms zero Task for the new class, and the delegated output
remains empty. That terminal event is retained as non-comparable smoke cost, never captured as the
pair sample. The accepted Spec now permits one measured delegated run with only ForkLight Home
added to `workspace-write` and local control-plane access enabled; no source write root or full
access. A second sandbox/admission failure stops the pair.

The corrected measured delegated run completed without re-entry or granular fallback. Task
`da6a7615-2b2a-483b-bb1c-3b8a1269e65b` produced one 69-line Candidate; verification `1804`, two
usable Judges, exact Main accept, receipt `df5826bb-8bd8-4c00-bad9-223502a6ecc0` and Integration
`09167633-ff29-4117-80a5-5f4372886760` all passed. Direct/delegated artifacts and the retained source
evidence are byte-identical and pass the shared validator.

Complete terminal samples are `mus-b066cb86-d25e-489d-8e66-10115b8fed37` (direct 171,984 gross)
and `mus-8d5a0754-0457-4d7e-a15c-1e64d24c63f1` (delegated 155,804 gross). Assessment
`mpa-ae1799a1-7958-4c99-8549-3a8f131a0da7` accepts all three M4-B gates; the canonical saving is
16,180 / 9.407851893199368%, and the scoped value report is `proven-lower`. Exact evidence:
`evidence/m4-e-fresh-worker-runtime-pair-delivery.md`. This admits fresh storage-lifecycle next;
M4 and M5 remain ungraduated/closed.

The admitted storage Work Item is now accepted at
`specs/m4-e-main-efficient-delivery/work-items/fresh-storage-lifecycle-pair/spec.md`. Three fresh
exact previews establish one `protected / unresolved-partial` rejected reusable Task and two
`reclaimable / integration-delivered` Tasks, all with zero unknown bytes/processes and Store
integrity `ok/0`. Its new task class/output cannot reuse the incomplete M4-D storage evidence.
Writable surfaces are one isolated artifact per side plus Main-owned calibration/SSOT paths; they
do not overlap product, Hub/UI, old artifacts or Store mutation. The pair therefore runs serially
under the same two checkpoints. No reclaim is authorized or performed by this Work Item.

The pair then completed once. Direct Main used 154,171 gross Tokens; delegated Main used 553,038.
The artifacts are byte-identical, ForkLight verification passed, two usable Judges accepted, Main
accepted the exact Revision and isolated Integration passed. Assessment
`mpa-c5b26b85-6853-4882-998b-12db3b04b9a8` accepts same scope, same acceptance and delegated
quality not lower, so this is valid negative evidence rather than an infrastructure failure. The
canonical change is -398,867 / -258.71726848758846%, saving status `higher`. No correction,
reverify, retry or replacement is admitted. Exact stop packet:
`evidence/m4-e-fresh-storage-lifecycle-pair-stop-decision-packet.md`. Hub remains held and M5
closed pending a future product/contract decision.

The M4 stop boundary is reconciled: `npm run check` passes build plus 3,084/3,084 tests, the
restarted client/Daemon identities match build `931e36fc...`, the source storage artifact matches
both comparison artifacts, and the live five-comparison value report is byte-identical to
`evidence/main-token-pairs.json`. It retains all old contrary evidence plus both fresh pairs:
worker-runtime is `proven-lower`; storage and Hub are `cannot-determine`. No Hub/UI work ran.

Goal continuation keeps the original M4 exit instead of relaxing it. A focused count-only audit
proves checkpoint payload size is not the cause: storage prepare/decide responses are 7,871/5,108
bytes versus 7,971/5,133 in the positive worker pair. The storage delegated Main instead had 15
model turns; five discovery turns cost 140,436 gross and six unchanged wait wake-ups cost 238,842.
The concrete gap is that current role capture accepts one terminal run, so Main cannot go offline
during a long Task and later sum all resumed segments honestly.

Accepted next Work Item:
`specs/m4-e-main-efficient-delivery/work-items/main-offline-usage-episode/spec.md`. One Grok Writer
will add bounded count-only episode capture while reusing current observation-only prepare re-entry.
It changes no quality gate, Task/Review/Integration authority, product Hub/UI or distributed state.
Exact audit: `evidence/m4-e-storage-main-token-amplification-audit.md`.

That Work Item is now activated. Task `6676be3a-24b4-4bbc-a8c0-7f2a3079e848`, Session
`bf05caf1-e293-4a71-9fd5-d95f866a188b`, Attempt
`9b242708-7f08-4f75-9725-fe82a3651232` completed one Grok 4.6 Xhigh native-Goal execution.
ForkLight independently passed build, 351 focused tests and diff validation for Revision
`f7c5f5ec-2dce-418e-af21-66c1d8da8587`, digest
`105d2cd53219ad3c35d37070e570085a324c5bf47ff360c165c022a05845c77e`, 14 paths / 1,569
changed lines. Review Graph `878ff74e-5642-4078-94bd-4d187cb94c3d` returned two usable accepts;
Main inspected and accepted the exact Candidate. Receipt `6c6b8ace-7ec8-465c-ac5d-b1b6604d9a54`
had no rejection and Integration `376f0980-97c4-446f-abd5-0d886ac033e1` passed apply,
verification, build and activation.

Activated behavior records two to sixteen resumed Codex terminal segments as one count-only
schema-v2 role sample while leaving all single-run schema-v1 rows, pair gates and value reports
compatible. CLI, Daemon and MCP share the Core path; duplicate/content/malformed/overflow/identity
failures persist nothing. Staged prepare timeout/re-entry keeps one durable Task/Graph and remains
observation-only. The post-Integration full check passes 3,094/3,094; restarted client/Daemon
identity matches build `6a4a9940...` / source `adfc0de0...`. No Hub/UI or old comparison changed.
Exact delivery evidence: `evidence/m4-e-main-offline-usage-episode-delivery.md`.

The next ready M4 Work Item is exactly one new current storage-lifecycle subject measured through
staged Main-offline delivery plus complete episode capture. The old 154,171/553,038 pair stays
visible and immutable. Only a valid strictly positive new result can admit Hub; otherwise M4
stops with both results. No reclaim, commit, push or reset occurred.

That Work Item is now accepted at
`specs/m4-e-main-efficient-delivery/work-items/main-offline-storage-lifecycle-pair/spec.md`. Three
fresh serial previews establish one protected rejected partial (`2d774...`) and two reclaimable
integration-delivered Tasks (`4709...`, `6676...`), all with zero unknown bytes/processes and Store
integrity `ok/0`. The new Task class, comparison and output differ from every prior pair.
Direct/delegated roots are byte-identical; delegated Main ends after dispatch and review staging,
waits through non-model durable observation and counts every resumed terminal segment in one
episode. No product Writer, reclaim, Hub/UI or old evidence mutation is in scope.

Pair preflight and the rendered delegated Task both pass: quality 100, new task class, one output
path, Grok 4.6 Xhigh native Goal, no Worker duration/Token/no-progress limit and two Judges. Direct
Main run `codex-run:01a0121e-f4b1-7a63-93f9-8dc1f19a8b9c` used `gpt-5.6-sol / xhigh`, wrote only
the 65-line artifact, passed the closed validator and `git diff --check`, and returned exact usage
125,854 input / 106,240 cache read / 2,448 output / zero cache creation: 128,302 gross after the
canonical disjoint normalization. The direct root and terminal event are preserved but no Store
sample is recorded before the paired Task exists.

Delegated pre-admission is temporarily blocked by external auth truth. Immediately after the
successful implementation Task, matched health changed xAI from local-sign-in to
`authentication-missing`; the operator Grok home contains only `auth.json.lock`, with no
`auth.json`. Main did not read/copy credentials, launch a doomed Task, switch Runtime or count this
as pair evidence. No delegated Task, Attempt, sample, assessment or comparison exists yet. Resume
the same frozen pair after 一骏 runs `grok login --oauth` and health again reports Grok 4.6 Xhigh
launchable.

Focused read-only attribution now closes the cause without reading credentials or conversation
content. Grok's unified log records `RefreshTokenRejected`, followed by its own
`cleared credentials after permanent refresh failure` and `auth disk state: entry lost` events.
The attributed shell session lives under the operator `~/.grok/sessions/%2FUsers%2Fyijunwang/...`
root (working directory `/Users/yijunwang`), while no matching session reference exists under
ForkLight's Task run roots. This is therefore an external operator-login expiry, not evidence that
the Task-local Runtime Home removed operator credentials. Authorization cannot repair it; only a
fresh `grok login --oauth` can restore launchability. All frozen pair roots and the completed direct
terminal event remain reusable and unchanged.

一骏重新登录后，matched health and a direct Grok 4.6 Xhigh smoke returned `AUTH_OK`; the external
auth blocker is closed and the Codex Goal resumed active. The first measured delegated dispatch
segment `codex-run:01a01238-68b1-7313-8e88-cedd9d0687f7` then called only the accepted 30-second
`delivery prepare`, but the host Daemon socket had disappeared before admission. The isolated
Codex sandbox could not create that listener (`listen EPERM`), so no Task, Attempt, sample or root
change exists. Its exact 46,366 gross usage is retained rather than discarded. The accepted Spec
now permits one host-side Daemon restoration and one same-command continuation dispatch; both
segments will be included in the delegated episode, and a repeated admission failure stops the
Work Item without replacement.

The authorized continuation run `codex-run:01a0123b-211a-70e0-a743-e947e6985dec` repeated the
same exact single prepare command after Main restored a matched host Daemon. It still failed before
Task creation and returned no JSON checkpoint. Its complete terminal event is 46,312 input / 33,280
cache read / 484 output, or 46,796 gross; together both retained delegated segments total 93,162.
No Task, Attempt, sample, assessment or comparison exists. The accepted repeated-failure clause
therefore stops this Work Item with no replacement and no Hub admission. Direct 128,302-gross
evidence and both roots remain protected. Exact packet:
`evidence/m4-e-main-offline-storage-admission-stop-decision-packet.md`.

Focused audit then proved a reusable product bug: socket probe collapsed `EPERM`, timeout and
`ECONNREFUSED`; the sandboxed secondary bootstrap could unlink the healthy endpoint before its own
listen failed. Main terminated only the two exact orphan Daemon PIDs and restored one matched
instance. The separate accepted blocker Work Item changed only `src/daemon/server.ts` and
`tests/daemon.test.ts`.

ForkLight Task `b664e69e-ee30-4268-8de3-1f7c07fb808d` completed Grok 4.6 Xhigh native Goal work
plus one allowed validation repair. Final Revision `d2066be2-ae14-420d-b09d-103eb0019561`, digest
`c98adc67e5525033e6c861fda9d4bd2937588d3eed523d78bad21e03d4a9a638`, changes 2 paths / 124
lines. Independent build, 198 Daemon tests and diff check passed; Review Graph
`2b394964-36f8-4b12-a025-6087375818b7` returned two usable accepts. Main accepted, preflight had no
rejection and Integration `b3bcaf00-f15f-4fbc-93fe-3648fbc254d8` applied/activated.

Post-Integration `npm run check` passes 3,096/3,096. A real socket-denied second instance now exits
1 with the bounded EPERM diagnostic while the exact live socket inode stays unchanged and health
stays true. Restarted client/Daemon identity matches build `2399b5af...` / source `9bad0039...`.
This fix does not reopen the stopped comparison. M4 remains ungraduated and Hub/M5 closed. Exact
delivery evidence: `evidence/m4-e-daemon-socket-probe-permission-safety-delivery.md`.

A follow-up read-only feasibility audit prepared, but did not authorize, the next decision. Fresh
previews keep `2d774...` protected and identify `6676...` plus newly delivered `b664...` as the two
current integration-delivered reclaimable Tasks; all have zero unknown bytes/processes and Store
integrity `ok/0`. This is a genuinely different subject because it replaces old Task `4709...`, but
it requires new direct and delegated evidence and cannot reuse the stopped pair's samples.

The audit also proves no further product code is required to remove model-active waiting. A host
can deterministically submit the frozen Task, wait for verification, create the frozen Review Graph
and wait for both Judges. Existing `delivery prepare` records no Main decision or Integration.
Exactly one measured Codex Main session can inspect the ready Candidate/diff/Judges and alone call
exact-bound `delivery decide`; every actual model session would still be captured. The earlier two
failed runners omitted `sandbox_workspace_write.network_access=true`, while the successful M4-E
runner included it with only ForkLight Home as an extra writable root. No Task, pair root, sample,
reclaim or Hub work was created. Explicit revocation of the no-replacement boundary remains the
next required decision; exact preparation is appended to
`evidence/m4-e-main-offline-storage-admission-stop-decision-packet.md`.

一骏 has now explicitly revoked that no-replacement boundary and authorized exactly one distinct
subject while preserving every old result. The accepted Work Item is
`specs/m4-e-main-efficient-delivery/work-items/host-staged-storage-lifecycle-pair/spec.md`. Fresh
serial previews confirm `2d774...` protected and `6676...` plus `b664...` reclaimable with the
frozen byte counts, zero unknown bytes/processes and Store `ok/0`. New input, output, Task class and
comparison identities are disjoint from all old pairs. Main is preparing byte-identical roots;
Host staging will perform no semantic review, and one measured Codex Main session alone retains the
exact accept/reject and Integration authority. No reclaim or Hub work is in scope.

Pair preflight passes quality 100 with native Goal, two Judges, safe Integration feasibility and no
Worker duration/Token/no-progress limit. Direct Main run `codex-run:01a012ee-f5b3-7f30-a449-4e1158163e25`
passed the closed validator and diff check with one 65-line artifact; exact terminal usage is
171,412 input / 142,592 cache read / 3,124 output / zero cache creation, or 174,536 gross.

Deterministic Host staging created Task `214a49f1-87bf-4f41-b165-5a3ca4c68c12`. Grok 4.6 Xhigh
native Goal completed, ForkLight independently passed both commands and captured exact one-path
Revision `74f1ba9c-eb3a-47fc-a144-50bb997e19ab`, digest `087352f4...`. Codex Luna returned a usable
accept. DeepSeek Pro returned the same accept plus one `info` finding, but its 508-character summary
exceeded the 500-character schema bound. The original assignment remains failed/immutable. Main
admits exactly one existing same-Judge summary-only repair; no Candidate rerun, changed finding,
replacement/third Judge or measured Main session has occurred.

The same-frozen-Judge repair succeeded and Review Graph
`e103ad34-2469-4170-b30b-8e6b6a74782a` now has two usable accepts. Measured delegated Main run
`codex-run:01a012fa-57d8-76d3-b31f-eb26d3fe18a5` received the exact Candidate, passed verification
and both opinions, and correctly concluded `accept`. Its sole decision command used `--task <id>`
instead of the required positional Task id, so ForkLight rejected the command before Main review,
preflight or Integration. The delegated root remains at its zero-byte base.

ForkLight durably captured the two complete Main events: 174,536 direct and 63,097 delegated gross.
`pair-report` and the scoped `value-report` both return `cannot-determine / incomplete-evidence`;
saving is unavailable despite the visible 111,439 count difference. Main did not retry, resume,
run a Host-side decision, create a replacement or assess the pair. Both roots, the verified exact
Candidate and Judge lineage remain protected. This one authorized distinct subject is exhausted;
M4 is not graduated, Hub remains held and M5 remains closed. Exact packet:
`evidence/m4-e-host-staged-storage-pair-stop-decision-packet.md`.

一骏 has now explicitly superseded only that decision-call stop and authorized one exact-syntax
decision-recording recovery. Fresh preflight confirms matched client/Daemon build
`2399b5af...`, Task stage `awaiting-main-review`, exact Revision/digest, two usable Judge accepts,
zero Integration receipt/result and both unchanged complete samples. No new measured model session
will run: the failed measured transcript already selected semantic `accept`, and this recovery only
records that frozen result with the published positional Task syntax. One command may create the
fresh Main review, preflight and safe isolated Integration; any failure is final. Candidate,
verification, Judges and samples cannot be rerun or changed. Hub remains held pending valid strictly
positive pair truth.

That exact recovery succeeded without another model session. ForkLight recorded Main accept,
preflight receipt `b1815fd9-b890-434d-b2bc-e4412439d0eb` and Integration
`91dfa097-3ef3-41f1-918c-77166e128430`; both accepted commands passed after apply. Direct,
Candidate and delegated artifacts share SHA-256 `755016be...`. Assessment
`mpa-0eb1cbb7-9c5c-42c7-a5cd-664041d3f7e2` accepts equal scope/acceptance and non-lower quality.
The canonical pair is now `accepted / saving`: 174,536 direct versus 63,097 delegated, a
111,439-Token / 63.848718888939814% reduction. The six-comparison canonical report now marks
worker-runtime and storage `proven-lower`; only Hub remains `not-strictly-positive`. The next ready
serial M4 unit is one distinct current Hub comprehension pair. No Hub process or UI is touched and
M5 remains closed.

That final current-family Work Item is accepted at
`specs/m4-e-main-efficient-delivery/work-items/current-hub-comprehension-pair/spec.md`. It is not a
replay of the historical Hub calibration: the new bounded subject turns six current M2–M4
graduation truths into future-M5 acceptance requirements with new input/output/task/comparison
identities. Focused preparation passes, direct/delegated roots are byte-identical, Task admission
scores quality 100, Workspace ownership is clear, Integration is feasible, Grok 4.6 Xhigh resolves
`native-goal`, two Judges are required and Worker duration/Token/no-progress limits are null. The
actual pair root is
`/var/folders/m2/tx2tqs290l913y61zqz413dr0000gn/T/forklight-m4e-current-hub-pair-tJPcfH`.
Read-only Hub truth remains PID 52551 / port 62182 / `different-build`. Direct, deterministic Host
staging and delegated Main will run serially; no Hub/UI or M5 action is permitted.

The unit completed with no repair or correction. Direct run
`codex-run:01a01346-7bba-78e3-8a6c-432e91e1cc31` used 120,989 gross Tokens. Deterministic Host
staging then ran Task `074084d4-4f13-4333-9f9d-52d7971a96ff`; one Grok 4.6 Xhigh native Goal
produced exact 53-line Revision `60c9fbc8-a29a-4602-adbf-a26dc1f65299`. ForkLight verification
passed, Review Graph `00f1258c-60d3-459e-b50c-a82526fce9bb` returned two usable accepts and the
measured delegated Main accepted plus integrated through operation
`556105c5-95da-4b8a-b117-51f981c9d019`. Its complete terminal event is 75,277 gross Tokens.

All artifact copies share SHA-256 `899d84d8...`; Hub remains unchanged. Assessment
`mpa-cd6c4af0-3fac-4f61-9122-6ec66c60cf26` accepts all M4-B gates and proves a 45,712-Token /
37.78194711915959% saving. Canonical seven-comparison truth is `proven`, with all three families
`proven-lower`. Boundary `npm run check` passes build plus 3,096/3,096 tests; restarted client and
Daemon match build `550f82dd...` / source `9bad0039...`; live/canonical reports are byte-identical;
Hub stays PID 52551 / port 62182 / `different-build`. M4 is graduated. Main now stops before M5 for
一骏's required confirmation. No commit, push, reset or reclaim occurred.

M4-A Task `62689bed-7e0e-453c-8c78-5decdd3ae6f9` completed one Grok 4.6 Xhigh native Goal Attempt
with exact Revision `f88331a0-38c7-42dd-b36b-16f8f551b5cd`. ForkLight passed build, 363/363 focused
tests and diff validation; Codex Luna Max and DeepSeek Pro both returned usable accepts. Main
accepted the exact Revision and Integration `3bba8cc1-a9a3-4b03-b9fb-d5a759f839dd` passed all four
stages. The post-Integration full check passes 3,034/3,034 and the restarted idle Daemon matches
build `8fb88fbd...` / source `f83ff587...`. Complete capture is now activated, but no pair or saving
has been claimed. Exact evidence is `evidence/m4-a-complete-main-usage-delivery.md`.

M4-B operational contract `execution/m4-wave-1/02-m4-b-valid-main-token-pair.yaml` passes quality
100, clear Workspace boundary and safe Integration feasibility. Task
`558357e4-7700-4ae0-bed6-e6d1a2ceda9c`, Session `47fdf09d-25a9-4512-a3be-3a90cfefa9d8`, completed
through Grok 4.6 Xhigh native Goal. Its base Candidate passed 375 focused tests. Main found one
real gap after the first dual accept: publication-only legacy evidence was not yet recognized as
legacy. The single approved same-Worker correction changed only `src/state/store.ts` and the two
focused test files, producing exact Revision `c3aaeddf-8df4-481f-9617-c0cb5cee9e71`, digest
`d6015f92fc150aa130a043e81a4ae5e22db36642458c453bf9aed69cbc6599c3`, 12 files and 2,212 lines.

Fresh independent verification passed build, 377/377 focused tests and diff validation. Fresh
Review Graph `6f6b8a36-3ad1-4b8d-8a81-dcb5132f9f9c` returned two usable accepts. Main accepted the
exact Revision; safe Integration `2d08097e-6b7e-41e5-b975-c803a340b263` passed source apply,
source verification, artifact build and runtime activation. The full check passes 3,048/3,048;
the restarted idle Daemon matches build `83bd948d...` / source `9b9fd543...`. M4-B is graduated,
M4-C is dependency-ready as the sole serial Writer, and M4-D remains held. Exact evidence is
`evidence/m4-b-valid-main-token-pair-delivery.md`.

M4-C operational contract `execution/m4-wave-1/03-m4-c-family-value-report.yaml` passes quality
100, exact ten-path ownership, matched health and safe Integration feasibility. Task
`89e60adc-d9ca-4560-9940-b423698c51f0`, Session `3293796f-c4d3-4b79-90b7-2b9645e0ce13`, is the sole
serial Writer through Grok 4.6 Xhigh native Goal. It has one base Attempt, no
duration/Token/no-progress ceiling, no generic extra Attempt or adaptation, one bounded
validation repair/Main correction, independent verification, two different-view Judges and Main
serial Integration. M4-D remains dependency-held.

The first M4-C Task stopped after one native Goal validation repair, one no-Worker reverify and one
Main correction authorization. Latest Revision `61dc45c0-e4b2-49c3-95db-f54a3a4d7861`, digest
`6f7d174e5c1efdb4d859902e3a25edcd51f16933cba5a77d5e637a211a8a5687`, contains 10 approved paths
and 1,891 lines. Reverification passed build and diff validation; 342/343 focused tests pass. The
sole gap is a brittle source-text regex; the real CLI behavior test passes. The successor native
Goal paused after two classifier non-achievements, and the structured correction then repeated the
same non-continuable Goal-status failure before edits. The old Task is immutable/protected with no
review or Integration. Exact evidence is `evidence/m4-c-initial-task-stop-decision-packet.md`.

一骏's standing authority admits one fresh recovery at
`specs/m4-c-family-value-report/work-items/exact-candidate-test-only-recovery/spec.md`. It uses a
Workspace-local exact Candidate seed, a fresh Grok 4.6 Xhigh native Goal and only the named test
edit. It has one Attempt, no validation repair/correction/adaptation/replacement and at most one
no-Worker reverify. M4-D remains held.

Recovery Task `812df915-788c-463c-8ee0-208d71ff9d58`, Session
`33d3bbc9-83d0-4663-b06f-2529c4e24481`, is now the sole Writer through a fresh Grok 4.6 Xhigh
native Goal. Matched health and the Workspace-local bootstrap policy passed before submission.
It produced exact Revision `debbf06b-a10f-4206-8382-60c1ea450ed4`, digest
`d7327f8c6a0ccd5232b950af4a2ed59143f2c0ef81c846d5287c6305352d92ab`, with only the authorized
behavior-test delta from the retained seed. Independent build, all 343 focused tests and diff
validation passed. The remaining command then failed because the pre-application bootstrap test
tried to apply the same seed again in the already-seeded post-Candidate Workspace.

ForkLight failure attribution event `3165` records `acceptance-contract / non-model`, not a Worker
or Candidate failure. The unchanged no-Worker reverify was not consumed because it would repeat
the same deterministic contradiction. Amendment remediation was not used because it verifies
current source rather than the unintegrated Candidate; using it after manual source application
would bypass safe Integration. The final recovery spec forbids another replacement, so M4-C is
stopped with both Workspaces protected, no Review Graph/Main accept/Integration, and M4-D held.
Exact evidence is `evidence/m4-c-final-recovery-stop-decision-packet.md`.

一骏 explicitly revoked the no-replacement boundary and authorized exactly one
acceptance-contract-only replacement. Accepted spec
`specs/m4-c-family-value-report/work-items/acceptance-contract-only-replacement/spec.md` and
operational contract `execution/m4-wave-1/05-m4-c-acceptance-contract-replacement.yaml` preserve
the exact final ten-path Candidate as two Workspace-local patches. The Grok Worker is read-only;
the only changed truth is that independent verification proves the already-applied Candidate by
path and reverse checks instead of trying to apply it twice. Policy preflight passes, contract
quality is 100, health is matched and Grok resolves `grok-4.6` / `xhigh` / `native-goal`. This is
the sole M4-C Work Item; M4-D remains held.

Replacement Task `4a967967-ac35-48e0-b89a-fd1e7c636e5d`, Session
`e7d111ff-afce-49e6-a7d7-ccbd73e64aac`, produced Revision
`5dd98f60-f6a1-42c7-86fa-a75c3ef2c7cf`, digest
`d7327f8c6a0ccd5232b950af4a2ed59143f2c0ef81c846d5287c6305352d92ab`, exactly identical to the
prior recovery Candidate. Independent build, 343/343 tests and diff validation passed. The
Main-owned post-application proof failed before reverse checks because its Node child invoked
ordinary `git diff` inside a plain Candidate Workspace rather than ForkLight's verifier Git
context. Event `2318` records `acceptance-contract / non-model`.

The Task allowed no reverify, repair, correction, adaptation, fallback or further replacement, so
Main stops it with no Review Graph, accept or Integration. All three M4-C Workspaces remain
protected and M4-D remains held. Exact evidence is
`evidence/m4-c-acceptance-contract-replacement-stop-decision-packet.md`.

On 2026-08-18 一骏 explicitly revoked that exact one-shot boundary and authorized one final
non-Git post-state-proof replacement. Accepted spec
`specs/m4-c-family-value-report/work-items/non-git-post-state-proof-replacement/spec.md` and
operational contract
`execution/m4-wave-1/06-m4-c-non-git-post-state-proof-replacement.yaml` reuse the same two exact
Workspace-local patches. The Grok Worker is read-only. Independent post-state proof uses only the
Task-local marker, reverse-check of the final one-file delta, and reverse-check of the other nine
base paths; it never queries Git repository metadata. ForkLight's own Candidate Revision supplies
the exact affected-path record.

The final replacement has exactly one Attempt and zero retry, reverify, Worker repair, Main
correction, adaptation, fallback or further replacement. Current health is matched and Grok
resolved to `grok-4.6` / `xhigh` / `native-goal`. It was the sole ready M4-C Work Item; M4-D remained
held until successful activated Integration. Any failure produces a final decision packet and ends
the replacement chain.

Task `e476516f-d653-4ec4-b919-8cb425a6f18d`, Session
`3cb9a9e4-5e30-4fa6-9d8e-64842107bdbc`, is now the sole M4-C execution. Pre-submission policy,
contract quality 100 and a realistic plain-directory post-state proof passed before launch.

The Task used its one Attempt and completed a real Grok 4.6 Xhigh current-model-only native Goal.
Exact Revision `12499c76-33bc-4578-9e9b-1878a81ba4b4`, digest
`d7327f8c6a0ccd5232b950af4a2ed59143f2c0ef81c846d5287c6305352d92ab`, contains ten paths and
1,925 lines and is byte-identical to the prior final Candidate. ForkLight passed the non-Git
post-state proof, build, all 343 focused tests and diff validation. Review Graph
`3a7027de-ccf2-4927-8783-1293aceb9fa9` returned usable Codex Luna Max and DeepSeek Pro accepts;
Main accepted the exact Revision.

Safe preflight receipt `005005c3-0fbe-4510-81d8-daa565cf5900` passed. Integration operation
`4a8515d4-b9d9-407b-9024-f7f2159a8450` applied the Candidate, then failed only because the fresh
source-verification project did not contain the Task-local `.forklight` marker required by the
post-state command. Build, 343 tests and diff validation passed in that same Integration phase.
ForkLight rolled the Candidate back completely; current source hashes match preflight, the two new
files are absent, project diff validation passes and client/Daemon identity is still matched.

The final contract grants zero retry, reverify, correction, fallback or further replacement after
any Integration failure. Main therefore preserves the Task as `protected / unresolved-partial`,
starts no further work and keeps M4-D held. Exact evidence is
`evidence/m4-c-final-non-git-replacement-stop-decision-packet.md`.

一骏 then explicitly revoked that stop only for one Integration-contract-only recovery. The same
accepted Revision, digest, ten paths, independent verification, two Judge accepts and Main accept
remain authoritative. Main will change no Candidate product/test path, create no Worker Task and
rerun no Grok, verification, Judge or Main review. The amended post-state proof removes only the
Task-local marker dependency, derives the exact seed digests/path set from the accepted patches,
and retains both reverse checks. It must pass in the protected Candidate Workspace and a fresh
Integration-like plain project with neither `.git` nor `.forklight` before one new preflight and
one new safe Integration. M4-D remains held; a failure is final.

The amended policy test passes. The exact proof also passes against the protected Candidate
Workspace and against `/tmp/forklight-m4c-integration.RJr1nO`, a fresh Integration-like project
where the accepted Candidate was applied and both `.git` and `.forklight` are absent. The sole new
ForkLight preflight/Integration is now admissible.

Fresh receipt `08d66a6e-ae59-413a-ae90-dd2537ac9afc` bound the unchanged Candidate and current
source with no rejection. The only authorized Integration operation
`1f9eac08-1b15-496b-a55d-78ddaaaf66e0` passed source apply, all four source-verification commands,
artifact build and both runtime-activation commands. The full check passes 3,061/3,061. After the
expected post-build restart, client/Daemon identity matches build `fb41811a...` and source digest
`00c675fe...`. Activated CLI smoke returns `cannot-determine / uncovered-family`, zero comparisons
and `createdWork: false` for an uncovered family.

M4-C is graduated. The delivered Task is reclaimable with 140,609,448 regenerable bytes,
7,378,311 durable bytes, zero unknown bytes, no process and Store integrity `ok/0`; no reclaim ran.
M4-D is now the sole dependency-ready Work Item. Exact evidence is
`evidence/m4-c-family-value-report-delivery.md`.

## Current M3 result

一骏 explicitly confirmed the M2 → M3 transition on 2026-08-16. Goal status is active. A current
direct Grok 4.6 Xhigh smoke returned `M3_GROK_46_XHIGH_OK`; after the baseline build, Main restarted
the idle Daemon once so client/Daemon build identity `243478d8...` matched. Health is `ok` and
`grok-4-6-xhigh` resolves `native-goal`.

Focused source/Store audit stopped at the M3 call chain rather than scanning the repository. The
current foundations pass `npm run build` and 443/443 focused tests across routing, statistics,
failure attribution, readiness, Competition, Review Graph, Task admission and preview. Store
coverage is 313 eligible terminal Tasks, 313 with taskClass, 266 with taskFamily and 81 with a
complete frozen routing decision. Only 2 decisions carry comparable multi-Worker family evidence;
59 record multi-Worker Main judgment with scope `none`. This is truthful history, not a reason to
rerun Tasks.

Four read-only routing queries establish the natural baseline. `forklight-storage-lifecycle`
returns a family-scoped DeepSeek Pro recommendation with confidence `0.909090...`.
`worker-runtime` and `hub-product-comprehension` both return unknown because their score gaps are
below policy; a new family returns scope `none`, insufficient evidence and no Competition under
intent `none`. Historical Hub-labeled Tasks are read only; no Hub/UI work is authorized before M5.

The accepted M3 Work Items are:

- M3-A executable routing advice: complete executable identity/readiness and stable
  cannot-determine semantics.
- M3-B durable routing choice/manual override, depending on M3-A.
- M3-C execution-strategy and exceptional-policy learning. Its exact two-path C1 Competition
  execution-truth slice is orthogonal to M3-A and may share Wave 1; C2 is serial after M3-A/B/C1.

Specs are under `specs/m3-a-executable-routing-advice/`,
`specs/m3-b-durable-routing-override/` and `specs/m3-c-strategy-policy-learning/`. Exact audit
evidence is summarized in `evidence/m3-entry-audit.md`. Main will use ForkLight isolated Writers,
Grok 4.6 Xhigh native Goal, independent verification, two Judges per meaningful routing/policy
Revision and serial safe Integration. No commit, push, reset, synthetic rerun or parallel status
system is authorized.

Wave 1 is now admitted through ForkLight. M3-A Task
`293724e0-5873-4bb1-9eef-3d685cf56bcf`, Session
`3696733d-e512-4322-8c35-fd6b22bc11dd`, owns the eight executable-routing paths. M3-C1 Task
`4112a313-566b-4d24-988f-e1bfb2c57735`, Session
`35d0e839-8db4-4110-8101-50815589efd6`, owns only `src/core/competition.ts` and
`tests/competition.test.ts`. Both validated at quality 100 with clear Workspace boundaries, resolve
Grok 4.6 Xhigh native Goal, freeze one base Attempt with bounded evidence-driven repair/correction,
and have no duration, Token or no-progress ceiling. Their routing decisions record scope `none` and
user-specified Grok selection; neither claims historical recommendation or Competition intent.
Wave 1 has now completed. M3-C1 Task `4112a313-566b-4d24-988f-e1bfb2c57735` delivered exact
two-path Revision `e2cf75bd-ad83-4721-bf8f-4b4a4cebb793`; two usable Judges, Main accept and safe
Integration `eac989dd-5a91-4862-8fe3-bc9f0cd4b483` passed. Candidate Profiles now freeze their own
execution preference and Runtime-resolved mode before Competition persistence, including
all-or-nothing rejection of unsupported forced modes.

M3-A Task `293724e0-5873-4bb1-9eef-3d685cf56bcf` delivered exact eight-path Revision
`c1538142-703e-4994-9869-745cde9739da`. C1 activation interrupted its first Attempt, and the product
resumed the same Task/Session/Workspace rather than replacing it. ForkLight verification passed;
one overlong DeepSeek Judge result was repaired once under the authorized same-Judge schema-only
path, after which both Judges were usable accepts. Main accepted the exact Revision and safe
Integration `f249f15d-44ce-41ee-8fae-84220cdc95f1` passed all four delivery stages.

The source full check passes 2,984/2,984 and the Daemon/client build identities match. Activated
natural-history queries now return one complete launchable recommendation for
`forklight-storage-lifecycle`, two explicit `score-gap-too-small` cannot-determine results, and one
`insufficient-relevant-samples` cannot-determine result. All retain Competition intent `none`.
M3-B's dependency is satisfied and it is the next serial Writer; M3-C2 remains held until M3-B is
integrated. Compact evidence is `evidence/m3-wave-1.md`.

M3-B is now admitted from
`execution/m3-wave-2/01-m3-b-durable-routing-override.yaml`. Current read-only advice over Grok
4.6 Xhigh, DeepSeek Pro and Codex Luna Max returns `cannot-determine` /
`insufficient-relevant-samples`, with all three currently launchable and Competition intent
`none`; the Task therefore freezes `user-specified` Grok selection honestly. Validation passes
quality 100, safe Integration feasibility, the 14-path/1,800-line warning budget and a clear
Workspace boundary. ForkLight Task `f5481469-2d82-4223-972d-2f250470e15f`, Session
`ad6f786c-83da-4be4-b245-ad4e90bdbb0d`, is queued on Grok 4.6 Xhigh `native-goal` with no duration,
Token or no-progress ceiling. It has one base Attempt, no generic extra Attempt or adaptation,
bounded evidence-driven repair/correction, independent verification and a frozen two-Judge gate.
No M3-C2 Writer is running because its shared paths and dependency make execution serial.

M3-B then produced a reusable 11-path Candidate. The base Attempt failed build only because a new
MCP fixture was inferred without the optional advisory field; its single frozen validation-repair
fixed that and Revision `4c36ce15-8267-46af-b8ad-722005f04b4e` passed all three independent
commands. The first Review Graph returned one usable Codex accept and one schema-invalid DeepSeek
result. Main did not repair the soon-to-be-stale Judge output: scoped diff review found that the
Candidate conflated distinct Worker Profiles sharing one executable identity and allowed a frozen
selected execution mode to disagree with the admitted Task. Main recorded `revise` and returned
only those gaps to the same Grok Worker with all 11 paths reusable.

The correction materially closed both gaps. Final protected Revision
`23b7f864-6f6f-44ac-9726-6b0c83bbfe50`, full digest
`7fb1fdcf9f156964ac4444c1611e91c77c6b958538eaf34088b9c8780bf30a3a`, changes the same 11 paths
and 1,711 lines. Build passes, `git diff --check` passes, and 465/466 focused tests pass. The sole
failure is a new MCP negative test whose top-level task family is absent while its nested routing
fixture has `main-orchestration-metadata`; the existing family guard therefore correctly wins
before the expected new execution-mode guard. No production behavior failed in that run, but the
accepted suite is red. The Task's one validation repair and one Main correction are both consumed;
the earlier Review Graph is stale, so there is no current Judge gate, Main accept or Integration.
Workspace and exact Candidate stay protected. See
`evidence/m3-b-verification-stop-decision-packet.md`.

Recovery submission preflight completed before authorization. The protected artifact digest
matches the final Revision, its exact 11 paths forward-apply cleanly to the current source, the
Daemon/client build identity is matched, and Grok 4.6 Xhigh is launchable as `native-goal`. At that
checkpoint Main had not created a wrapper, copied the seed, submitted a Task or mutated the Store.

一骏 explicitly granted that authority on 2026-08-17. The accepted M3-B Spec now freezes one new
Task that materializes the exact 11-path Candidate, permits only the single
`tests/mcp.test.ts` family-fixture alignment relative to it, and retains one Attempt with zero
repair, correction, reverify, adaptation, fallback or replacement. The failed Task stays immutable
and protected. Submission, independent verification, two fresh Judges and Main serial Integration
are the current chain; M3-C2 remains closed until delivery.

Operational preflight now passes: the copied seed and protected artifact share digest
`7fb1fdcf9f156964ac4444c1611e91c77c6b958538eaf34088b9c8780bf30a3a`; wrapper syntax/policy,
exact 11-path parsing, forward source apply, build-matched health, Grok native-goal resolution and
Task validation all pass. The contract scores quality 100 with a clear Workspace boundary and no
duration, Token or no-progress ceiling. Recovery Task
`aa524fdd-1558-4d92-8d5a-ae49f738c9b4`, Session
`88a6557a-ee6d-40fc-887c-4abe1fb5dd19`, is queued on Grok 4.6 Xhigh. Ten retained Candidate paths
are model-write-denied; only `tests/mcp.test.ts` may differ from the seed. No second Writer or
M3-C2 Task is running.

The recovery has now delivered without fallback. Its only Attempt completed Grok native Goal with
`complete + achieved`; Revision `72fe6e93-764c-4872-9a49-e5d825fbc775`, digest
`41243125a7c78b49524d90db9cdb8c4bdfe421d749dd73f566c6ec136e8fc5b9`, changes the retained 11
paths/1,712 lines. Direct Workspace comparison proves the ten protected paths are byte-identical
and the MCP test has exactly one authorized line. All 466 focused tests pass. Two fresh usable
Judges accepted, Main accepted, and Integration `8b45d56a-7b7a-4ff6-9e5e-3223bac0fa87` passed
source apply/verify, artifact build and activation. The high-risk source check passes 3,008/3,008;
Daemon/client build identity is matched. M3-B is complete and M3-C2 is the only next Writer.

M3-C2 focused audit confirmed the remaining product gap without widening scope. Full-Worker
routing evidence currently keys provider/model/Runtime/effort but omits execution mode, so one
Worker's `single-run`, `persistent-session` and `native-goal` history can merge. Existing
Competition intent already remains explicit and `none` returns not advised; current Store truth
also has natural three-mode families, legacy missing-mode records, one reasoned Competition and
Review Graphs with usable/unusable Judge results. These records are read-only evidence, not a
reason to manufacture work.

Operational contract `execution/m3-wave-3/01-m3-c2-strategy-policy-advice.yaml` passes quality 100,
clear Workspace-boundary and safe-Integration preflight. Its frozen routing advice honestly says
`cannot-determine / insufficient-relevant-samples`, selects recently proven Grok 4.6 Xhigh by Main
judgment, records Competition intent `none`, and resolves real `native-goal`. It has no duration,
Token or no-progress ceiling, one base Attempt, bounded evidence-driven validation repair/Main
correction, no generic extra Attempt or adaptation, one Writer and a two-Judge gate. Task
`bbec21f9-ae21-4bfd-9cc4-9476aea3f73a`, Session
`2c79cd11-60aa-489f-8678-50b3102b3210`, is now running serially; Main has started no parallel
writer and will integrate only an exact accepted Revision.

That base Attempt later stopped after 75 effective Grok turns when the CLI proxy returned HTTP 401
for invalid/expired credentials. Native Goal `9b022da8-45a9-46de-be6a-c104272fcf94` is durably
`infra_paused / Executing` with `not_achieved`; there is no Worker completion claim, Candidate
Revision, ForkLight verification, Judge or Integration. Main observation timeouts did not cancel
the Task.

The protected Workspace retains 14 allowed paths and 1,915 changed lines. Main Workspace-only
diagnosis found two new TypeScript fixture errors; the eight-file suite otherwise runs 556/557,
with the sole failure caused by a new negative regex matching the desired phrases “does not start”
and “no inferred requirement”. Scoped diff review also found that intent `none` still exposes
historical Competition counts, trigger matching is not yet compatible-trigger scoped,
`legacy-unknown` may enter recommendations, and strict missing-evidence policy is not preserved.
No source edit or new Task was made. The exact reusable output, evidence and recommended same-Task
recovery boundary are in `evidence/m3-c2-infra-pause-decision-packet.md`.

Read-only feasibility then proved the initially proposed same-Task extra Attempt cannot run:
`maxExtraAttempts` and `maxAdaptationRounds` are both frozen at zero, while Main correction requires
the missing Candidate Revision. The honest next option is one new Task that materializes the exact
14-path partial, starts a fresh Grok native Goal and keeps the original verification/two-Judge/Main
Integration chain. No recovery artifacts or Task have been created; this corrected authority
boundary is recorded in the same decision packet.

Three consecutive Goal continuations now observe the same external state: the failed Task still
has exactly one Attempt, no Candidate Revision/verification/Review Graph, and protected Workspace
disposition; Grok authentication has not been reported restored and the one-shot recovery has not
been authorized. Codex Goal supervision is therefore blocked at this exact authority/external-auth
gate rather than continuing unchanged polling. Resuming the Goal requires both restored Grok CLI
login and 一骏's explicit authorization of the decision packet's one-shot exact-partial Task.

一骏 then reported Grok re-login and explicitly authorized that one-shot recovery. Build-matched
ForkLight health passed, and one direct Grok 4.6 Xhigh request returned
`M3_C2_RECOVERY_AUTH_OK`. Main created the sole operational contract
`execution/m3-wave-3/02-m3-c2-exact-partial-recovery.yaml`; its focused bootstrap policy test proved
the protected baseline-to-Workspace delta is exactly the accepted 14 paths and that current
product/test source still matches the old baseline. The contract validated quality 100, resolved
fresh `native-goal`, froze one base Attempt, zero extra Attempt/adaptation/fallback/replacement,
at most one validation repair and one Main correction/reverification, and two Judges.

Recovery Task `588fc40f-afad-448e-8306-e05859166842`, Session
`39f770e6-58d0-4b81-8cd8-b96dfbab869a`, stopped before Grok created native Goal state. Attempt
`1861dff6-316b-410f-b386-b8876f5230e0` ran the wrapper inside Grok's Workspace sandbox; the wrapper
received `EPERM` while scanning the old Task's absolute protected baseline path. This is an
operational seed-location failure, not renewed authentication loss, Worker implementation output
or acceptance evidence. The new Workspace has no materialization marker, no product/test delta,
no Grok session/Goal files, no Worker process, Candidate, verification or Judge. The old 14-path
partial remains byte-untouched and protected.

The failed recovery's single base Attempt is consumed. Validation repair was correctly skipped
because the Worker did not return normally, correction has no Candidate Revision, and the Task
allows no extra Attempt or adaptation. Main will not edit the failed Workspace or create another
Task under the exhausted authorization. A representable next option requires new explicit
authority for one replacement whose exact seed patch lives inside its own Workspace before Grok's
sandbox starts; all product scope, seven gaps, one-Attempt policy, dual Judge gate and serial
Integration boundary can otherwise remain unchanged.

Read-only feasibility now proves that replacement shape without consuming authority: one combined
binary patch stream for the protected 14 paths forward-checks against current source,
reverse-checks against the old partial, and reports the same 1,904 additions/11 deletions. The
patch can enter a future Workspace through ordinary source snapshot preparation and be read wholly
inside Grok's sandbox, matching the already delivered M3-B recovery pattern. Main did not persist
the patch, create a contract or submit a Task; the explicit replacement decision is still required.

The original failure turn and two automatic Goal continuations now form three consecutive turns
with the same authority blocker. Final reconciliation still shows the recovery Task failed at
event 17, no Workspace-local patch artifact exists, and no new user decision arrived. Codex Goal
status is therefore formally `blocked`; this prevents empty polling while preserving the complete
M2–M5 objective. A later explicit authorization resumes from this exact packet as a fresh blocked
audit.

一骏 then explicitly authorized all subsequent Goal tasks and requested continuous progress. The
Codex Goal resumed `active`. Main persisted the previously proven exact 14-path partial as
`execution/m3-wave-3/m3-c2-protected-partial.patch`, added a Workspace-local bootstrap and focused
policy test, and created
`execution/m3-wave-3/03-m3-c2-workspace-local-seed-replacement.yaml`. The patch forward-checks
current source, reverse-checks the original protected partial and reports 14 files with 1,904
additions/11 deletions. Wrapper/test whitespace, Task quality 100, clear Workspace boundary,
native-goal resolution and build-matched health all pass.

Replacement Task `68f16585-1fec-447c-8799-4867a4731b0f`, Session
`a218f235-4a26-4514-affe-45953cd75b22`, is queued on Grok 4.6 Xhigh. The seed now enters through
normal source snapshot preparation and is read only from the new Task's own Workspace, so the
previous sandbox-external failure cannot recur. It retains the exact seven gaps, 14 writable
paths, one base Attempt, bounded validation repair/Main correction, no duration/Token/no-progress
ceiling, independent verification, two Judges and Main serial Integration.

The Task is now running. Workspace marker truth lists exactly those 14 paths and the local seed
artifact; Grok durable native Goal `847f8b7c-afdd-42ed-be1a-5fdb893614f1` is `active / Executing`.
More than five minutes of effective model events confirm the prior pre-Goal sandbox blocker is
closed. No observation timeout has changed or canceled execution.

The replacement has now delivered. The same native Goal reached `complete / achieved` after one
Worker round. Exact Revision `5d3ee57c-be94-48ee-bdc2-458dcadc302c`, accepted digest
`d42635d3a9bba5b142863ae23c34976424558b2b0c72a6e7219d347f3fc65bad`, changes the accepted 14
paths/2,223 lines and closes all seven scoped gaps. ForkLight independent verification passed the
Workspace-local bootstrap policy test, build, 560/560 focused tests and diff check. Both frozen
different-view Judges returned usable accepts; Main accepted the exact Revision and safe
Integration `a0d07d2d-2261-4036-ba65-da124663567c` passed source apply, four source verifications,
artifact build and Runtime activation.

The M3 boundary `npm run check` passes 3,022/3,022. After the check rebuilt the CLI, Main restarted
the idle Daemon once; client and Daemon now match build `33666e662a...` and source digest
`f29db348...`, with no active or queued Tasks. Four read-only natural-history projections preserve
the entry truth: storage recommends DeepSeek Pro at confidence `0.909090...`; `worker-runtime` and
historical `hub-product-comprehension` return `score-gap-too-small`; an unseen family returns
`insufficient-relevant-samples`. Mode-aware strategy advice says `cannot-determine` where natural
family coverage is incomplete, Competition intent `none` creates no work, Judge history never
votes or infers a requirement, and Main-direct records stay outside Worker comparison. M3 is
graduated; compact evidence is `evidence/m3-routing-graduation.md`.

Later, recorded once without Task creation: direct internal M3-A composition with an absent
readiness map can produce an empty reason list; a future asynchronous Runtime doctor would need
explicit readiness handling although every current adapter doctor is synchronous; and reviewer
packet generic 1-file/1-line guidance did not reflect M3-A's accepted 8-file/1,200-line warning
budget. None is reachable as a current public-path blocker or changes the accepted Candidate.

### Historical M2-A recovery checkpoints

The following block preserves earlier recovery truth and authorization boundaries. It is historical;
the current M2 result is reconciled below.

Main verified the protected Revision artifact digest exactly equals
`e12f45e8d2b9daceebc1b5d53929a455e7ae0965110853b3e677960a0fd42f62` and forward-applies cleanly
to the current source. The existing Work Item Spec now contains the final recovery boundary and
`execution/m2-grok-native-goal/03-current-model-only-recovery.yaml` freezes one Attempt, zero
fallback and the exact 14 retained paths. Wrapper syntax/policy, forward seed apply, 12 retained
non-delta paths and Workspace boundary review passed; the three visible ignored roots are empty
0-byte directories. The exact native paths have zero intersection with the protected 16-path
Judge-result Candidate and two-path storage Candidate.

ForkLight Task `08832868-30e1-4950-81c4-d575adf98117`, Session
`5f1de2fd-4bb7-4c1c-b066-e8330394194b`, is now queued on Grok 4.6 Xhigh. Store truth remains
`persistent-session`; the operational wrapper invokes one genuine current-model-only Grok CLI
native `/goal`. No fallback is available.

The unique Attempt `d008a116-4a43-40cc-bc91-aefa131e9844` then failed with exit 66 before Grok
created a native Goal. The exact seed still passes SHA-256 and forward apply in both current source
and the failed Task Workspace outside sandbox. A read-only sandbox reproduction returns
`fatal: unable to access '/Users/yijunwang/.gitconfig': Operation not permitted`; the wrapper did
not neutralize global Git configuration and collapsed that process failure into the inaccurate
source-base-mismatch message. There is no Goal state, Candidate diff, verification, Judge,
Integration or Provider usage evidence. Zero fallback is exhausted; no Task repair or replacement
was started. Decision packet:
`evidence/m2-grok-native-goal-current-model-only-recovery-decision-packet.md`.

One additional read-only sandbox check set only `GIT_CONFIG_GLOBAL=/dev/null` and
`GIT_CONFIG_SYSTEM=/dev/null`; the same exact seed check then passed with exit 0 without applying
the patch. The next possible remedy is therefore proven wrapper-only Git configuration isolation,
not a Candidate, model, Runtime or product-scope change. It remains unauthorized.

一骏 subsequently authorized exactly that wrapper-only replacement. The failed wrapper remains
unchanged at SHA-256 `e51851d02acde5aec0d2de7af8bca98636ca813f8bbd6c530ec80613cdcfd687`;
new operational files are
`current-model-only-git-isolated-replacement-bootstrap.mjs` and
`04-current-model-only-git-isolated-replacement.yaml`. The only wrapper delta is the two proven Git
configuration environment variables. Historical-wrapper hash, exact seed digest, equivalent
sandbox apply, Task validation, single-Attempt/zero-fallback policy, Grok readiness and empty
ignored-root review all passed.

ForkLight replacement Task `847b24ef-0ac9-4958-9959-9a64b0bc466e`, Session
`726ff5ff-db5c-4d36-8224-b0822a36e39a`, is queued on Grok 4.6 Xhigh. It honestly records
`persistent-session`; the wrapper invokes a genuine current-model-only native `/goal`. No further
replacement is authorized.

Later: failed no-Candidate Tasks with `requiredJudges` currently project an `await-required-review`
packet even though there is no Candidate to review. This did not cause the failure and is recorded
once without creating a Task.

## Inherited truth

- M1 graduated context-first Goal/Plan/Task and its CLI/API entry paths.
- The prior Goal and all M1 evidence are preserved under
  `goals/archive/forklight-main-token-leverage-m1/`; they are historical, not a current roadmap.
- Historical full backups were compacted after approval to one verified 421 MB pre-cleanup SQLite
  audit database. Current Task-space lifecycle remains an M2 product responsibility.

## Current M2 result

M2-A, M2-B, M2-C and both reviewed-delivery journeys are graduated. The six explicitly resolved
zero-change audit Tasks have now passed fresh activated-source preview and exact confirmed reclaim:
31 regenerable targets and 685,377,515 bytes were removed, while 9,102,847 durable bytes remain
readable. Final per-Task previews show zero regenerable/unknown bytes and no processes; the global
audit has no unknown-orphan classification, no process and SQLite integrity `ok/0`. Repository
planning has one current Goal, an evidence-based stop policy, orthogonal-wave rules, Grok-first
Runtime policy, independent Judge policy and a product/process boundary. M3 remains closed until
一骏 confirms the Milestone transition.

The focused source/runtime audit at M2 start recorded the implementation gaps that drove the work
below:

- Grok Task-bound resume is now truthfully `persistent-session`: first launch uses `--session-id`,
  continuation uses `--resume`, and the currently integrated `nativeGoal` remains unsupported. Saved Profile
  `grok-4-6-xhigh` materializes `grok-build/xai/grok-4.6/xhigh`; old omitted Profile semantics stay
  `single-run`.
- Self-check, independent verification, bounded validation repair, exact Candidate Revision,
  Review Graph, partial handoff, Main Review and safe Integration now compose into a delivered
  explicit per-Task Judge requirement and one compact Main decision packet.
- Historical Task-space cleanup is complete evidence, but there is no product storage
  audit/preview/reclaim lifecycle, terminal disposition, orphan projection, or CLI/API surface.
- Multi-user/distributed consistency, locks, leases, checksums and version handshakes are not M2
  gaps. Current Store truth, simple path ownership and serial Main Integration are the boundary.
- M2-A/B/C share public types and CLI/API/daemon boundaries, so the first implementation wave is
  serial A → B → C rather than falsely parallel.

## Active work

M2-A runtime Goal `goals/forklight-main-led-execution/execution/m2-a/goal.json` completed 4/4.
Implementation Task `00f1892a-815e-4aaa-b281-4d3673b0e655` reused one Grok Session across the
initial Attempt, one automatic whitespace repair, and one bounded Main documentation correction.
ForkLight independently passed build, 360 focused tests and `git diff --check`; two fresh Judges
accepted exact Candidate Revision `318ffa2f-74ff-4b76-b963-f435699d17c1`. Main accepted and safe
Integration applied 19 business files while `dist/**` stayed outside the Candidate. Source verify,
artifact build, Daemon activation and activation check all passed; the high-risk post-Integration
`npm run check` passed 2,862 tests.

Three zero-diff post-Integration Tasks then independently verified execution truth, legacy/public
compatibility, and generated-output/source scope. Their deterministic machine gates passed; a Judge
was intentionally skipped because each was read-only, produced no Candidate change, and reran the
approved commands. Live health now reports `grok-4-6-xhigh` launchable with
`auto -> persistent-session`, while `local-grok-builder` remains launchable with legacy
`single-run`. M2-B may now start; it remains serial before M2-C because their writable public and
daemon/CLI surfaces overlap.

ForkLight accepted the validated M2-B operational Goal at
`goals/forklight-main-led-execution/execution/m2-b/goal.json`. Implementation Task
`f82bd08b-6fa0-439e-84b0-43a4c87a1bdc` is Integration-gated; registered serial audit Tasks are
`43838016-bec7-4d64-a7ab-632d3e69bc3f`, `a158d87a-23c2-49cc-899f-a1407d489662`, and
`d1c5b8bb-833c-4e2c-924b-ce05a4615b60`. Validation froze the implementation Worker exactly as
`grok-build/xai/grok-4.6/xhigh`, `auto -> persistent-session`, `generatedPaths: ["dist/**"]`, and
no duration, Token, or no-progress hard limit.

### Superseded ordinary-Task evidence

M2-A Task `3e3eb258-4791-43e7-9122-9f8a8b2cebba` ran as the exact live
`grok-build/xai/grok-4.6/xhigh` selection in one retained Grok Session. Its frozen development
policy had no absolute duration, Token, no-progress, file-count or changed-line hard limit. The
local independent-verification command timeout was raised from five to 30 minutes; Main
observation-call expiry was treated only as an observation boundary.

All seven currently saved Worker Profiles now explicitly use `noProgressTimeoutMs: null`, including
the DeepSeek and Codex Judge Profiles that previously inherited a 30-minute no-progress stop. The
global 30-minute value remains only a legacy/unprofiled fallback; new profiled development work is
governed by the evidence-based stop rule in the Goal.

The Worker produced the intended 18 business paths. ForkLight found two trailing-whitespace errors,
resumed the same Session for validation-repair Attempt
`d5c95050-5ec2-4fd6-a884-a9648494ee3a`, then independently passed `npm run build`, the focused
suite, and `git diff --check`. Candidate Revision
`c5f07541-ce45-4a94-b82c-5a506275402d` is source-compatible. Review Graph
`2de42135-07d8-40f0-afdb-4801ec5ce9ae` used Codex Luna Max and DeepSeek Pro; both proposed accept.

Main recorded `revise`, because the immutable Task workspace policy omitted `dist/**`. The
acceptance build therefore added 38 ignored/generated `dist/` files to the exact Candidate, making
the reviewed patch 56 files even though the accepted Work Item allows only source, tests, and
narrow docs. Raising Integration limits would apply out-of-scope generated files and is rejected.
The same-Task correction allowance is preserved, but cannot fix an immutable admission/path-policy
error: every unchanged acceptance rerun would recreate the same `dist/` output.

### Original M2-A decision packet (resolved by the authorized replacement)

- Completed and reusable: the 18 intended files under `src/`, `tests/`, and `docs/`; exact live
  Grok identity; same-Session validation repair; passed ForkLight verification; two usable Judge
  opinions.
- Remaining gap: create a Candidate whose immutable Task policy classifies `dist/**` as generated,
  so independent build output stays outside the reviewed Integration patch.
- Attempts made: initial Worker, same-Session validation repair, complete independent verification,
  two different-view Judges, and fresh Main review. No Integration was attempted.
- Workspace disposition: retain the full original Task Workspace, Candidate Revision, Review Graph,
  verification, and logs until a safe reusable successor or explicit rejection is durable.
- Main decision: 一骏 authorized the bounded successor with the same accepted source boundary and
  `generatedPaths: ["dist/**"]`. Manual patch filtering/application and merely raising size limits
  remain rejected.

A second focused audit confirmed that ForkLight Store currently has no registered Goal and this
Candidate is an ordinary direct Task. Selected-path whole-file import exists only for durable Goal
milestones and Competition Candidates; there is no supported Task adoption, ordinary-Task handoff,
or reviewed-subpatch Integration API. Retroactively editing Store ownership or the signed Candidate
would be unsafe. This narrows the decision but does not justify manufacturing a new product path
solely to recover one wrongly admitted Task.

The same condition remained true across the original stop turn and two automatic Goal
continuations, so Codex correctly marked the long Goal blocked instead of repeatedly polling. 一骏
then explicitly authorized the recommended replacement. The validated runtime graph lives under
`goals/forklight-main-led-execution/execution/m2-a/`: the implementation milestone has an
Integration gate and three downstream milestones are read-only post-Integration truth, compatibility,
and generated-output/scope audits. All four Task contracts freeze `generatedPaths: ["dist/**"]` and
no absolute development duration, Token, or no-progress limit. The old ordinary Candidate remains
retained and rejected; it is not treated as the new Goal lineage.

M2-B Goal `goals/forklight-main-led-execution/execution/m2-b/goal.json` completed 4/4. The original
Grok 4.6 Xhigh implementation Candidate passed independent verification and two Judges, but Main
found one missed compatibility blocker: a legacy or explicit-skip Task with an existing Review
Graph could receive a packet action that contradicted Integration preflight. Main recorded
`revise`; the frozen same-Task correction allowance was exhausted, so ForkLight durably handed all
18 verified paths and one exact gap to successor Task `7c8d491b-26ac-4466-a7de-8a9929f75a38`.

The successor used a separate Grok Worker, retained `persistent-session`, and after one same-Session
validation repair passed build, 524 focused tests, and `git diff --check`. Two fresh Judges accepted
Candidate Revision `4b143804-2e60-4e50-8c9e-2848d8043114`; Main accepted it and safe Integration
operation `1b54452a-b716-439b-8672-32f890f07bee` passed source apply, source verification, artifact
build, Runtime activation, and activation check. Three later Grok 4.6 Xhigh audits independently
passed with zero business/generated/Integration diff. The post-Integration full suite passed 2,882
of 2,883 tests; its sole `ENOTEMPTY` temporary-directory teardown failure was unrelated to M2-B and
the exact checkpoint-operation file then passed 13/13 on one focused rerun.

M2-C is now ready. Its first new high-risk Task must freeze `requiredJudges: 2` through the delivered
admission path, proving M2-B on real destructive-lifecycle-adjacent work rather than retroactively
rewriting old Task truth.

ForkLight accepted M2-C operational Goal
`goals/forklight-main-led-execution/execution/m2-c/goal.json`. Implementation Task
`850b2e03-cc8f-436d-8424-96ea9c48a388` froze the exact Grok 4.6 Xhigh persistent-session identity,
no duration, Token, or no-progress hard limit, and immutable `requiredJudges: 2` with a destructive
lifecycle/Store-integrity reason. Registered serial read-only audits are
`42f08041-6b30-4865-a68e-c0b0f34bc8c1`, `412ff7c4-2409-422e-8392-2c1e9c46dba6`, and
`5690e53d-07c5-4b58-98d0-9195a17efde2`; each explicitly records `requiredJudges: 0` because its
deterministic machine gate changes no product file. Real local audit, preview, and ordinary reclaim
remain Main-run post-Integration acceptance, never Worker or automated-test mutation.

M2-C then produced a reusable 17-path storage-lifecycle Candidate through the Grok 4.6 Xhigh
persistent Session, one bounded correction, and durable one-hop successor
`67f255f9-2729-401d-b9c8-2864fca9f189`. Current Revision
`99435c50-1815-414b-9430-df6eef1b619b` passed independent build, 346 focused tests and
`git diff --check`. Two fresh Judges disagreed. Main confirmed five remaining in-contract gaps:
all-eligible preview hides global unknown/protected entries; duplicate PID ownership is not treated
as ambiguous; symlinked physical runs/Task roots are not refused; retain lacks its integrity
precheck; and retain output lacks bytes/prior eligibility reason.

The accepted spec now freezes those scenarios. ForkLight refused another correction because the
operational Goal's single correction round is exhausted; the supported handoff is already one-hop.
Both original Workspaces and exact Candidate evidence remain protected, and the full stop packet is
`evidence/m2-c-stop-decision-packet.md`.

一骏 then explicitly authorized the recommended new narrow Work Item. Main's focused provenance
audit proved the exact Revision artifact digest
`237cbfe2403fc43519e1b467d542c74b5a02525ebe5dda98b592e89abb0c3b0f` and `git apply -p2 --check`
against current source. ForkLight's existing handoff materializer uses that same patch format and
apply mode. The accepted recovery spec is
`specs/m2-c-task-storage-lifecycle/work-items/correction-cap-recovery/spec.md`, and its operational
Goal is `execution/m2-c-recovery/goal.json`. It has one Grok 4.6 Xhigh implementation Writer,
`requiredJudges: 2`, no duration/Token/no-progress ceiling, and three serial post-Integration audits.
The temporary seed is read-only Task input; Main does not patch product source or rewrite Store.

ForkLight accepted the recovery Goal and registered implementation Task
`ebbf4c6a-2b34-416e-8bb5-2ca2e2dab22e` plus serial audit Tasks
`608cde93-6ecd-4b39-966e-7fbc8acbf0ea`, `13ba678b-4369-4ce4-ab6f-d13fb3fa5c0f`, and
`93f3b4b2-9ebd-4990-b884-6e5c1a1055e2`. The implementation Task started as exact
`grok-build/xai/grok-4.6/xhigh`, `auto -> persistent-session`, with the frozen two-Judge requirement
and no duration, Token, or no-progress hard limit. Workspace preparation copied the seed and Main
verified the Workspace copy's full digest before removing the temporary source-tree copy; the
original Store artifact and both prior M2-C Workspaces remain protected.

The recovery Worker reused the seed, closed all five frozen gaps, and after one evidence-based
same-Session correction produced Candidate Revision `5e90879d-0609-48e8-8845-f30d0e469bd2` with
digest `e1a906abf346...`. ForkLight independently passed build, 353 focused tests and diff check;
Codex Luna Max and DeepSeek Pro both accepted it. Main accepted the exact Revision. The first
Integration attempt safely rolled back because Main had removed the temporary seed before source
verification reran its identity command. Main restored the exact digest-bound input and a new safe
preflight; Integration `5febafdd-88ee-44d0-86ab-de5b9dbff75f` then passed source apply/verify,
artifact build and Runtime activation. The temporary source seed was removed again. All three
post-Integration audits succeeded with zero Candidate changes, and the source `npm run check`
passed 2,916/2,916 tests.

The required real-home `storage audit` and `storage preview` then both failed read-only with
`Cannot read properties of undefined (reading 'kind')`. A focused stack trace localized the
failure to `projectCandidateHandoff` during Main decision composition. The real Store contains one
authentic legacy Competition handoff with old top-level Competition identity and no typed
`origin`; three newer handoffs already use current origins. No mutation or reclaim occurred.
The accepted blocker spec is
`specs/m2-c-task-storage-lifecycle/work-items/real-store-legacy-handoff-compat/spec.md`.

The legacy compatibility Worker first returned a passing Candidate, but Main found that its strict
reader broke the existing unknown-origin Work Hierarchy contract. Main updated the accepted spec,
returned one exact gap through structured correction, and reused the accepted paths. Corrected
Candidate `f587fd8d-deab-4b60-bccc-012011e18355` passed ForkLight's independent build and 189
focused tests; Codex Luna Max and DeepSeek Pro both accepted it. Main accepted the exact Revision
and safe Integration `aff3a159-fccc-4f1e-a161-19e9b17a88d3` passed source apply/verify, build and
activation. Grok authentication then became unavailable, so Main did not repeatedly probe it or
mislabel another Runtime. Replacement DeepSeek audit Goal
`execution/m2-c-legacy-audits/goal.json` completed all four serial zero-diff gates.

Real `storage audit` and all-eligible preview then succeeded. Before reclaim they reported 338
entries: 86 protected, 248 reclaimable and 4 already reclaimed; zero unknown-orphan roots or
processes; 1,079,179,854 reclaimable bytes; and Store integrity `ok/0`. Preview named exactly 363
known regenerable targets while preserving 3,651,519,529 durable bytes and 5,192,460 unknown bytes
inside eligible roots. 一骏 explicitly authorized ordinary all-eligible reclaim.

The confirmed request durably recorded 248 reclaimed outcomes between 09:31:38Z and 09:32:10Z,
removed all 363 named targets and 1,079,179,854 bytes, stopped/refused/escalated zero processes,
and recorded `quickCheck: ok` with zero foreign-key violations on every result. Post-reclaim audit
has 251 currently reclaimed and 87 protected entries, no reclaimable or unknown-orphan entry, no
process, and integrity `ok/0`. One older previously reclaimed Task is currently protected because
an operation is active; ForkLight correctly leaves its small regenerated verifier target intact.

The CLI that initiated the successful batch returned a 15-second transport timeout before the
roughly 32-second daemon response. Durable work was neither cancelled nor replayed, but the user
did not receive that invocation's result. This is the final M2-C gap and directly matches the
accepted timeout boundary: `daemonRequestTimeoutMs` must give `storage_reclaim` a non-premature
observation window without changing operation truth. The accepted Work Item is
`specs/m2-c-task-storage-lifecycle/work-items/reclaim-transport-observation/spec.md`; its serial
operational Goal is `execution/m2-c-reclaim-transport/goal.json`. Source `npm run check` passed
2,922/2,922 before this final supplement.

The reclaim-transport implementation Task `2586676a-e679-47f8-875d-3e1de9276426` produced exact
Candidate `6d28c588-3fa8-4610-a20e-be8df0497d1d` (2 files, 40 lines). ForkLight passed build, 266
focused tests and diff check. Codex Luna Max and MiniMax M3 both proposed accept; Main accepted and
Integration `ca098a68-2f5f-44b7-93a7-a4fe14846816` passed source apply/verify, build and activation.
The operational Goal completed 4/4. Source `npm run check` then passed 2,924/2,924.

A post-fix exact preview found only the naturally terminal implementation and two Judge Tasks:
17 targets, 153,970,084 regenerable bytes, 2,626,162 durable bytes to retain, zero unknown and zero
processes, integrity `ok/0`. The already authorized ordinary reclaim returned successfully after
more than the former 15-second window: 3/3 applied, all 17 targets removed, durable evidence
preserved, integrity `ok/0`. Final audit has 254 reclaimed and 90 explicitly protected Tasks, zero
reclaimable, zero unknown-orphan, zero processes and integrity `ok/0`. M2-C is graduated; evidence
is `evidence/m2-storage-lifecycle.md`.

Main completed the focused second-wave audit and accepted
`specs/m2-delivery-journeys/spec.md` plus self-contained Coding and non-Coding Work Items. The
Coding slice safely continues only source-only Integration operations after a durable passed
`source-applied` stage; build and activation crash recovery remain explicitly unsupported. The
non-Coding runbook depends on integrated Coding truth, so physical path separation does not make
the Work Items semantically orthogonal and they will run serially. DeepSeek Pro passed a current
explicit Provider smoke after the preferred Grok 4.6 Xhigh Profile remained blocked by known
missing xAI authentication. The four-milestone Coding Goal at
`execution/m2-coding-journey/goal.json` validates read-only and is ready for submission with no
duration, Token, or no-progress ceiling.

The original Coding Goal is durably `waiting/review-cap`. The original DeepSeek Task proved intentional
restart continuation on the same Task/Session, completed one independent-verifier pass and one
same-Session correction, then used one supported cross-Worker handoff after the correction cap.
Volcengine successor Revision `ebd14c70-400f-4f99-aab6-adc938e54938` reuses the complete four-path
Candidate, closes the final ancestor-symlink rollback risk, and independently passes build, 276
focused tests and diff validation. It is not integrated: the first review round had one usable
opinion and one schema violation; the second had two schema violations; ForkLight then refused a
fresh final-Candidate Review Graph at the frozen two-round cap. The exact decision packet is
`evidence/m2-coding-journey-stop-decision-packet.md`. Final Workspace is protected for
`awaiting-required-review`, preview has zero eligible targets/unknown/process and integrity `ok/0`.
The non-Coding journey stays dependency-blocked. 一骏 explicitly authorized the packet's narrow
exact-Candidate recovery. Main proved the private Revision and bounded source input both match full
digest `a862bfe4...054f651`, and `git apply -p2 --check` succeeds against current source. The accepted
spec is `specs/m2-delivery-journeys/work-items/coding-review-cap-recovery/spec.md`; its serial four-
milestone operational Goal is `execution/m2-coding-review-recovery/goal.json`. The implementation
Task may only materialize the exact four-path Candidate, freezes two Judges, one review round, zero
corrections, and no duration/Token/no-progress deadline. At admission no Main accept or Integration
had occurred; the completed result follows below.

That recovery has now delivered the exact code. New Revision
`57b295fc-3c08-4445-a8a5-d615dc6431bd` has the same full digest `a862bfe4...054f651`, four paths and
1,291 changed lines; independent build, 276 focused tests and diff validation passed. Codex Luna Max
and DeepSeek Flash both returned usable exact-Revision accept opinions. Main accepted it, and safe
Integration `15bd69bc-d5cf-4668-a75a-ab0aa74f2057` passed source apply/verify, artifact build,
activation and matched health.

The first integrated audit Task `15458a3c-54f2-4e16-9b3e-ac925a6abf67` failed before ForkLight
verification with zero source diff: its Codex native-goal read-only sandbox rejected `dist/` and tsx
cache writes, lacked `.git`, and that Runtime has no checkpoint MCP. The original recovery Goal is
therefore waiting at `milestone-failed`; no product command failed under the independent verifier.
The accepted serial replacement is
`specs/m2-delivery-journeys/work-items/coding-integrated-audit-recovery/spec.md` with operational Goal
`execution/m2-coding-integrated-audits/goal.json`. It uses current smoke-verified checkpoint-capable
Claude-Code Runtimes, zero edits/Judges/corrections, and the unchanged machine gates.

That supplement is now stopped at its own second-admission-failure boundary. Task
`8e368413-8871-494f-bd81-d6689ee63dc1` completed the first gate with zero diff, independent build,
276 focused tests and diff validation. Task `f17333fc-8ec8-45a1-b082-082b92ba7d0d` then passed
Worker checkpoint and independent build, 250 focused tests and diff validation with zero diff, but
its Workspace-local `health` assertion failed because that isolated Task Home had no running
Daemon. Main's real-source health and Integration activation evidence both remain matched. The
accepted spec explicitly stops on a second Runtime/admission failure; the Goal is therefore
`waiting/milestone-failed` at 1/4, downstream audit Tasks have not run, and all unresolved
Workspaces remain protected. Exact evidence and the required decision are in
`evidence/m2-coding-integrated-audit-stop-decision-packet.md`.

一骏 has completed Grok authentication. Current health reports xAI local-sign-in ready and
`grok-4-6-xhigh` launchable as `persistent-session`, but its connection is still unverified until a
real Grok 4.6 Task launches. This does not reopen the stopped audit automatically.

一骏 then authorized one acceptance-contract-only resolution. Main revise event 111 binds the exact
failed verification/Candidate, and attribution event 114 records `acceptance-contract/non-model`.
Amended remediation check `eb46f89b-03a7-446e-be4f-90d87f922f8d` preserved the three passing
commands and used no Worker, but passed only 3/4: remediation inherits Main's reachable ForkLight
Home, so health returned the already-proven `matched` identity while the replacement incorrectly
required `daemon-unavailable`. No delivery disposition was created and the machine gate remains
failed. The four-milestone draft `execution/m2-coding-final-audits/goal.json` passes read-only
validation, but was not submitted after the accepted single-failure stop. Its four Tasks do not
exist in Store. The new exact decision is in
`evidence/m2-coding-integrated-audit-stop-decision-packet.md`; non-Coding remains blocked and Grok
has not been ceremonially launched. Latest preflight reports matched source/Daemon identity and
protected Workspace integrity `ok/0`. 一骏 explicitly authorized final submission; DeepSeek,
Volcengine and MiniMax each passed one current explicit Provider smoke and their retained Profiles
became `ready`. ForkLight admitted `execution/m2-coding-final-audits/goal.json` with strict serial
Tasks `213ba48f-8286-4146-b899-ad2b4627e7d4`,
`12b90678-ff0a-46d7-b694-c8cb1b8c56d8`,
`aa86c052-1be9-48a9-af6e-39a979a6041c`, and
`88ad747a-fdfb-44e0-826f-95614b610b68`. Every gate freezes zero edits, Judges, corrections and
reroutes. All four Tasks passed ForkLight independent verification with exact empty Candidates and
the Goal completed 4/4; real source/Daemon identity remained matched. Main therefore graduates the
Coding journey without another Candidate, review or Integration replay.

The accepted dependent non-Coding runbook now executes through
`execution/m2-noncoding-journey/goal.json`. Task `fd41a1dc-ca0e-4918-8002-f9932908c35d` launched as
the first meaningful authenticated `grok-build/xai/grok-4.6/xhigh` Task with
`auto -> persistent-session`, no duration/Token/no-progress limit, three allowed documentation
paths, one required Judge and serial Integration. Its durable Session is
`b1d682d1-f296-4b66-89fe-60081fc45207`; Main will restart the daemon only after useful partial
documentation exists. Registered downstream zero-change Tasks are
`0f850d6c-923b-430f-a86b-43b82e48326f`,
`8a37ac23-5555-4120-8e87-4abee1c95f1b`, and
`8f2972cb-0bb6-4175-b79e-37e1e775a7c0`.

The original Grok Task then proved the required restart continuation, passed independent
verification, used its only Main correction to compress the runbook from 265 to 132 lines, and
received one usable MiniMax accept opinion on exact Revision
`8e6ba44c-3278-4319-9c73-719d97500ca8`. Main found one exact remaining wording error: the document
conflated `unknown-orphan` with `protected`. ForkLight used the allowed one-hop handoff to DeepSeek
Flash successor `91d34265-fdca-4623-8aa1-646c90ff1e36`, reusing all three paths and only that gap.
Successor Revision `04e736e7-b595-44a4-96b1-b82d2dd6cb5b` closes it and independently passes all
four commands.

Main nevertheless rejects Integration because the successor's immutable Task combines
`claude-code/deepseek-flash`, `executionPreference: auto`, and the source Grok Task's
`executionMode: persistent-session`; canonical capability truth requires `single-run` for Claude
Code. The handoff builder replaces Runtime identity but preserves the already frozen source mode,
then stores the successor without normal admission re-resolution. A Worker cannot repair immutable
Task truth, the one-hop path is exhausted, and product code is outside the docs-only Spec. The Goal
therefore waits at 0/4 with all downstream audits blocked. Exact evidence, reusable Candidate and
workspace disposition are in
`evidence/m2-noncoding-handoff-execution-mode-stop-decision-packet.md`.

一骏 explicitly authorized the decision packet's recommended two-step recovery. Main reloaded the
current AGENTS/SSOT, confirmed the source/Daemon build identity remains matched, and preserved the
dirty worktree. Focused read-only tracing confirmed one exact root cause: the shared successor
builder replaces destination Provider/Runtime/Profile but retains the source Task's frozen
`executionPreference` and `executionMode`; normal Task admission already resolves the same fields
correctly through canonical Runtime capabilities. The adjacent stale Quality-policy Profile id is
recorded but not promoted into this execution-only blocker.

The exact successor documentation Revision was independently rechecked at full digest
`65deb60f3f4c6f5fe3730076e07a29cca52a0fb76019c18daf6a7d817554373a`; it contains only
`README.md`, `docs/main-led-delivery.md`, and `docs/operations.md`, with 145 insertions, 3 deletions,
and a successful current-source `git apply -p2 --check`. Accepted strict-serial specs are
`specs/m2-delivery-journeys/work-items/handoff-destination-execution-truth/spec.md` and
`specs/m2-delivery-journeys/work-items/noncoding-exact-candidate-recovery/spec.md`.

The first operational Goal at `execution/m2-handoff-execution-truth/goal.json` validates read-only
with four strict dependency waves. Its implementation freezes
`grok-build/xai/grok-4.6/xhigh`, `auto -> persistent-session`, no duration/Token/no-progress
ceiling, exactly three writable paths and `requiredJudges: 2`; each later activated-source audit
declares zero edits and zero Judges with deterministic commands. Exact documentation recovery
remains dependency-closed until this Goal passes verification, two usable Judges, Main accept,
safe Integration, activation, full source check, and all three audits.

ForkLight admitted that Goal with implementation Task
`67c403a7-1afb-4758-a0f1-16a58765cc0b` and serial audit Tasks
`f5d1142a-6eca-4d4c-8c43-801d0b284056`,
`ed0f3982-1c23-408a-a82f-4bc7ee9c43a5`, and
`03090843-548b-4aec-8271-f5307344b17a`. The first Task is the sole product Writer; downstream
Tasks remain dependency-blocked until Main accepts, reviews and integrates it.

The initial Grok Attempt used Session `42e848f9-7188-4a82-9e1f-a6d77322eb5e`, produced exact
three-path Revision `a2b29241-a659-40cd-9830-025dfd8694e9`, and ForkLight independently passed
build, the full focused Goal/Competition/Task/Runtime surface, and diff validation. Main's actual
diff review found one remaining in-scope schema mismatch before opening Judges: destination Profile
omission deleted `executionPreference`, although current newly admitted Tasks must explicitly
freeze canonical `single-run` and reserve absence for historical records. Main tightened the
accepted spec, bound an exact `revise`, and returned only that assertion/assignment gap to the same
Grok Task and Session while reusing all accepted paths. No Judge or Integration has started.

That correction completed in the same Grok Session. Final product Revision
`e6b017c1-3f24-470c-819d-ae8dab1e588b` has full digest
`b279bfcc1e2432421987c9b0e8008eb43d9ff81dd8bbae1b39dc1120e42cbb50`, three paths and 342 changed
lines. ForkLight independently passed build, focused tests and diff validation; Codex Luna Max and
DeepSeek Pro both accepted it. Main accepted and safe Integration
`419aa653-0c57-43a8-9516-a35365c1a9ba` passed source apply, verification, build and activation.
The source full check passed 2,940/2,940 and all three zero-change audits completed, so the product
Goal is 4/4.

The exact non-Coding recovery then materialized frozen full digest
`65deb60f3f4c6f5fe3730076e07a29cca52a0fb76019c18daf6a7d817554373a` as Revision
`dbd23068-0b90-4b19-8eed-624d6d160b23`; its patch is byte-identical to the accepted seed, contains
only the three documentation paths and 148 changed lines, and passed all five independent commands.
One fresh Codex Luna Max Judge accepted it. Main accepted and safe Integration
`05825fa8-3c0b-4865-b6c2-2260717f1b7f` passed all stages; the original three non-Coding audits then
passed with exact empty Candidates. This Goal also completed 4/4. Main explicitly resolved all six
audit Tasks after recording their accepts.

The first terminal disposition pass reclaimed five already eligible implementation/Judge Tasks,
removing 350,389,871 regenerable bytes while retaining 4,574,077 durable bytes. Store quick-check
is `ok`, foreign-key violations, unknown-orphan entries/bytes and processes are all zero. The six
resolved audit Tasks nevertheless remain `protected/awaiting-main-decision`, retaining exactly
685,377,515 regenerable bytes. Focused source tracing localized the blocker to
`classifyKnownTask`: active and safety guards are correct, but stale Candidate review/Main/
Integration waits return before the existing resolved-terminal eligibility branch.

The accepted final M2 supplement is
`specs/m2-delivery-journeys/work-items/resolved-terminal-storage-disposition/spec.md`, with strict-
serial operational Goal `execution/m2-resolved-terminal-storage-disposition/goal.json`. It permits
one Grok 4.6 Xhigh `persistent-session` Writer on only `src/core/storage-lifecycle.ts` and
`tests/storage-lifecycle.test.ts`, requires two Judges, safe Main Integration, and three zero-change
audits. After activation Main may preview/reclaim only the six exact Task ids. M3 remains closed.

Implementation Task `71850bde-917a-426c-b92a-164790b1abbd` produced current Revision
`172d6a80-bdd7-487a-9e97-b938a67f10c5`, full digest
`48a73c6b6db1071fd62f2dfffa3799df81805cdc1e6d4a46cb163bb9ebdafcda`, two paths and 181 changed
lines. It ran in Grok Session `a901c093-a301-4e6f-8304-fa331cc5dc3d`; initial implementation and
one evidence-only same-Task correction both independently passed build, 295 focused tests and diff
validation with the exact same Candidate digest.

Review Graph `3fdffa7d-bb1f-4c8d-85b1-a57fc20845d2` returned one usable Codex accept and one
DeepSeek schema violation. The final allowed Graph `7c6a032e-9cc5-4d75-adb9-f77d9293a366`
returned one usable Codex accept and one MiniMax schema violation. ForkLight then refused another
review before assignment and durably set the Goal to `waiting/review-cap` with correction 1/1 and
review rounds 2/2. The current two-Judge gate remains undersized; Main did not accept or integrate,
and no downstream audit or reclaim ran. The exact decision packet is
`evidence/m2-resolved-terminal-storage-review-cap-decision-packet.md`.

一骏已明确授权复用该精确storage Candidate，同时澄清优先目标是Grok CLI自己的`/goal`
模式，不是ForkLight Goal合同。Main完成了官方1.0.3源码审计和真实隔离Smoke：Session
`94c508c7-7ebf-4d97-b762-f5c8c1968bc9`中的native Goal
`f88df868-008a-43ae-a4ec-1dd5881d1f44`先因planner依赖失败停在`user_paused`，尽管模型
文案声称已完成；保留shell工具注册但禁止实际Bash，并只开放task-local Goal计划、状态和
scratch写入后，同一Session/Goal经`/goal resume`最终到达`complete`、一次classifier运行
和`achieved`，三个独立skeptic均未反驳。源README与隔离副本hash相同。终态报告
470,355 total tokens、26 model calls、USD 0.5794094；这是一次性启动证据，不触发更多
自证Task，也不新增默认Token或墙钟上限。

已接受自包含M2-A补充Spec
`specs/m2-a-grok-execution-truth/work-items/grok-native-goal/spec.md`。它把Grok新Task的
`auto`真相升级为native `/goal`，只以task-local durable Goal state中的
`complete + achieved + readable owned details`允许进入ForkLight独立验收，并保留显式
`persistent-session`兼容；两位不同视角Judge和无桥接器真实ForkLight Task是毕业门槛。
其产品修改路径与冻结storage Candidate的两个路径不重叠，Main仍串行Integration。storage
Candidate及全部Workspace继续保护，native Goal产品真相毕业后才执行已授权恢复。M3关闭。

ForkLight implementation Task `010812a2-0939-4315-9e19-ae7b892e677b` then used Grok Session
`085b9926-b171-4ffa-ad96-dc76a3548ad3` and three real Grok CLI native Goals. Goal
`6b724715-a358-480f-9844-98c8560edb4b` delivered the scoped Candidate; validation-repair Goal
`28e1ef0e-734e-480c-bfab-1290ebdbd398` reduced five compile errors to one; typed correction Goal
`2df56a31-2a0d-402e-9954-df5f4ae690d8` fixed that last error. Every accepted transition ended in
native `complete + achieved` evidence. Current Candidate Revision
`00d99db6-429f-4786-b982-740f19581b31`, digest
`e12f45e8d2b9daceebc1b5d53929a455e7ae0965110853b3e677960a0fd42f62`, changes 14 allowed paths.
ForkLight independently passed build, 471 focused tests and `git diff --check`. Review Graph
`749b5210-fae8-4a6f-bde2-74eb4f41c178` returned two usable exact-Revision accepts.

Main nevertheless recorded `revise`. Official Grok 1.0.3 source makes
`GROK_GOAL_USE_CURRENT_MODEL_ONLY` default false and permits configured or remote planner,
strategist and skeptic model pairs. The Candidate sets `GROK_GOAL=1` and `GROK_WORKFLOWS=1` but
does not set that kill-switch, so it cannot prove the frozen `grok-4.6/xhigh` identity governs every
native Goal role. No Integration or unwrapped live product Task ran. The Task has exhausted its one
validation-repair and one Main correction; Main will not recreate it automatically. The protected
decision packet is `evidence/m2-grok-native-goal-main-review-decision-packet.md`.

This stop does not invalidate the already authorized storage recovery: its two writable paths are
orthogonal to all native Goal Candidate paths, its source Candidate remains unchanged, and Main
Integration remains serial. Main therefore proceeds to design and admit one exact-Candidate
storage review-cap recovery under the prior explicit authorization; M3 stays closed.

The exact storage recovery then stopped before Grok native `/goal`. Operational Goal implementation
Task `4ad6f4d6-7196-494d-9b94-6db118c7cbf5` could not read the user Git config inside its sandbox;
its immutable zero-extra policy prevented retry, so Main stopped that Goal. One allowed replacement
Task `0ee15d39-3f4a-418e-b5dc-6026033e6de6` neutralized only that config dependency, applied the
digest-bound seed, passed reverse-apply and produced exactly the expected two baseline file
differences totaling 181 lines. It still stopped before Grok because the wrapper used repository-
form `git diff` in ForkLight's `.git`-free Workspace.

This is the second purposeful bootstrap round. Main did not use the replacement Task's unused
extra-Attempt capacity, create another Task, launch Grok, verify, review, integrate, audit or
reclaim. The original Candidate and both recovery Workspaces are protected with no processes. The
reusable exact partial and smallest wrapper-only continuation are recorded in
`evidence/m2-resolved-terminal-storage-recovery-bootstrap-stop-decision-packet.md`. Continuing now
requires a new explicit decision; M3 remains closed.

一骏 then explicitly authorized that exact continuation. Main's preauthorization proof confirmed
the digest-bound patch headers contain only the two allowed baseline/workspace path pairs and the
protected Workspace still passes reverse-apply. The wrapper now derives the path set from those
headers instead of calling repository-form `git diff`; no Candidate byte, product path, acceptance
command, model or Session changed. Main may consume the one explicit extra Attempt on replacement
Task `0ee15d39-3f4a-418e-b5dc-6026033e6de6`; no third Task or M3 work is authorized.

ForkLight durably granted and consumed that extra Attempt as
`68faaf51-3ed7-4adb-9400-a442a87c61c5` on the same Session. All materializer identity checks passed,
but the wrapper then tried to load the native bootstrap through its absolute source-project path;
the Worker sandbox correctly returned `EPERM` because only the isolated Workspace is readable.
Grok never launched, no native Goal or recovery Revision exists, independent verification did not
run, and reported Worker Token remains unavailable with zero observed gross Worker tokens.

The protected Workspace-local bootstrap exists at the same relative path, is executable, passes
`node --check`, and has the exact same sha256 as the source copy. The technical correction is thus
one workspace-relative path join, but Task `0ee15d39...` now has two failed Attempts against frozen
`baseMaxAttempts: 1`, `maxExtraAttempts: 1`, zero corrections and zero adaptations. Main will not
change Store authority or create another Task automatically. Exact evidence and the next decision
are appended to `evidence/m2-resolved-terminal-storage-recovery-bootstrap-stop-decision-packet.md`;
M3 remains closed.

一骏 explicitly authorized one final replacement Task with no fallback. Main reproduced ForkLight's
actual Grok sandbox profile and proved it loads the identical Workspace-local child bootstrap while
rejecting the former source-tree absolute path. The wrapper now joins that relative child path to
the Task Workspace. The final Task freezes one base Attempt and zero extra Attempts, validation
repairs, corrections, reverifications and adaptations; it keeps the exact seed, Grok 4.6 Xhigh
current-model-only native Goal, read-only Worker, independent verification, two-Judge gate and
serial Main Integration. Any failure stops permanently; M3 remains closed.

Final Task `65876057-5f0c-4676-a5dc-4a80e5425cb0` successfully crossed every bootstrap boundary.
It created real native Goal `a71f91df-ec00-499e-b852-144f4f8d4bc9`, materialized exact Revision
`70daf39b-5acd-42ac-a20f-667d083e3aeb` at frozen digest `48a73c6b...dafcda`, and ForkLight
independently passed seed/reverse proof, build, 321 focused tests and diff validation on exactly two
paths and 181 lines. The native Goal itself ended `user_paused/Executing` after 508,287 ms with
99,777 token high-water, zero classifier runs, and no `complete` or `achieved` state.

Main therefore durably rejected the exact Revision before Judges. Worker exit 0 and independent
verification cannot replace native Goal terminal truth. The final contract freezes zero extra
Attempt, validation repair, correction, reverification, adaptation or replacement, so no further
execution exists. ForkLight usage is incomplete (10 turns, runtime estimate USD 0.10822778, missing
official Token usage) and is not reported as measured zero. Storage preview protects the full final
Workspace with zero unknown bytes and zero processes. No Judge, Integration, audit or reclaim ran;
M2 remains open and M3 remains closed.

Focused read-only audit closed the pause cause. Grok 1.0.3 fired its Goal Planner at 11:11:00Z;
the planner subagent ran 508,298 ms and made 56 tool calls, but its effective read-only toolset had
no file-write tool. It tried five `run_terminal_command` writes to the exact native plan path; every
call was denied by the wrapper's explicit Bash deny. At 11:19:28Z the Runtime emitted
`goal_planner_fail_closed: missing_plan_file` and, by its own planner-failure implementation,
persisted `user_paused` with `Planning failed; resume with /goal to retry.` It then ran an ordinary
turn whose completion prose falsely claimed the Goal was achieved. The concrete blocker is therefore
the final Task's read-only/no-Bash contract conflicting with native Goal's required task-local plan
artifact, not authentication, timeout, Candidate behavior, source path or model identity. Fixing it
would change the frozen execution boundary; the no-fallback decision forbids that here.

一骏随后授权新的M2 Work Item，而不是重开旧Task或调整M2毕业边界。Accepted Spec是
`specs/m2-delivery-journeys/work-items/resolved-terminal-storage-native-plan-recovery/spec.md`。
Focused Grok 1.0.3源码与当前ForkLight argv审计证明：native Planner使用
`search_replace/write`作为文件mutator；CLI deny在always-approve下仍优先。因此新operational
bootstrap只增加这两个Planner工具，保留`run_terminal_cmd` disallowed和Bash deny，并增加
`Write/Edit(<Workspace>/**)` deny。Task-local Goal plan可写，Candidate Workspace对模型仍只读；
冻结seed仍由启动前materializer独立物化。新Task只有一个base Attempt，所有extra/repair/
correction/reverification/adaptation/handoff/reroute/replacement为零，双Judge和Main Integration
保持冻结。Preflight health matched，Grok/xAI可用，当前无active/queued Task。

新 Work Item 的唯一 Task `53c01375-25f7-432d-98cf-635f097a25d8` 已终止。Main-owned
materializer 原样生成 Revision `1d00fc0e-6ab6-4234-98ba-7341c1f688c3`，完整 digest 仍为
`48a73c6b...dafcda`；ForkLight 独立通过 bootstrap policy、seed/reverse、build、321 个聚焦
测试和 diff check，Candidate 仍只有两路径、181 行。native Goal
`ec54061a-82ee-436a-a543-27bbb660ce3c` 则在 5 ms 后持久化为
`user_paused/Executing`，plan 为空、classifier 为 0，没有 `complete + achieved`。

Planner 子会话 `01a00541-20f1-78e3-838d-6e27562ad5f5` 的 durable meta 给出精确根因：
`kill_task`、`get_task_output` 和 `wait_tasks` 被暴露，但它们要求 background-capable Bash
或 `task`；本合同正确禁止了这些执行能力，所以子会话在初始化时以 requirements
unsatisfied 失败，0 turns、0 tool calls。Main 已绑定唯一 Attempt、verification sequence
1180 和精确 Revision 执行 reject；没有启动 Judge 或 Integration。Task 没有 extra、repair、
correction、reverification、adaptation、handoff、reroute、model switch 或 replacement 权限。
Workspace 继续 protected，unknown 0 bytes、processes 0、Store quick check `ok/0`。见
`evidence/m2-resolved-terminal-storage-native-plan-recovery-decision-packet.md`。M2 继续打开，
M3 关闭。

后续只读闭环确认该 Attempt 实际运行当前 Grok CLI `1.0.4`（commit
`d846eb93d94d`），不是早期 native Goal 源码审计使用的 1.0.3。真实 parent tool projection
包含前台 `spawn_subagent`、Task-local `write/search_replace`和两个映射后的后台控制工具，但
没有 terminal。1.0.4 随附文档明确：subagent 的 `background` 默认 `false`；output、wait 和
kill 只用于后台 command/subagent。因此未来最小闭合工具集是保留前台 Agent 和 plan
mutators、成组移除三个后台控制工具，而不是重新开放 terminal/Bash。当前授权仍不允许修改
wrapper或启动新Task；只有下一次真实`/goal`才能最终证明Planner初始化成功。

一骏随后明确授权一个新的自包含 Work Item：
`specs/m2-delivery-journeys/work-items/resolved-terminal-storage-foreground-plan-recovery/spec.md`。
前序 Task 和 Attempt 保持永久终止；新合同只删除
`get_task_output/kill_task/wait_tasks`，保留前台 `Agent`、Task-local
`search_replace/write`、Workspace `Write/Edit` deny、terminal/Bash deny、精确 seed、一个
base Attempt、零 correction/repair/reverification/adaptation/handoff/reroute/model switch/
replacement。Preflight 已确认 ForkLight health/build/Daemon/MCP identity matched、Grok
4.6 Xhigh/xAI ready、当前无 active/queued Task；本地 wrapper 和 Task 合同验证正在进行。

本地 wrapper policy、两个脚本语法、精确 seed sha256、当前源正向 apply、Task 合同和
`git diff --check` 全部通过；Task 合同质量 100，ForkLight 执行真相仍诚实标记为
`persistent-session`。唯一 Task `6fbf8e70-a48a-4e7b-8cb8-3dc07e836085`、Session
`76c834d2-315a-466f-a978-3ee30415c490` 已提交到 `grok-build/xai/grok-4.6/xhigh`。它只有一个
base Attempt且没有 duration、Token或no-progress ceiling；正在等待真实 `/goal` 和ForkLight
独立验收终态。

该 Task 已完成唯一 Attempt `de6d0576-bae7-441a-9467-47e698e5e867`。精确 Candidate Revision
`d6c85d59-10dc-4325-a0fc-e627b62130c2` 的完整 digest 为
`48a73c6b...dafcda`，仍只有两路径、181行。native Goal
`e1941538-caf8-4c0b-bdf1-5c5241bf3476` 首次闭合：plan 7,281 bytes、classifier 1次、
durable `complete/Idle`、last verdict `achieved`。ForkLight verification sequence 1899独立通过
wrapper policy、seed、reverse apply、build、321个聚焦测试和diff check。

双 Judge Graph `c9721c94-9740-4ff2-9502-b26ce8a75a9c` 绑定上述精确 Revision。Codex Luna Max
返回 usable `accept`；DeepSeek Flash 1M 的 reviewer Task 完成，但结果违反 Review schema，
ForkLight 正确标记 unusable。冻结门需要两份 usable opinion且合同不授权replacement或新Graph，
Main 已绑定唯一 Attempt、verification 1899、精确 Revision/digest执行`reject`。没有
Integration、activation、audit或reclaim。Task空间139,071,602 bytes继续protected，unknown 0、
process 0、Store quick check `ok/0`。详见
`evidence/m2-resolved-terminal-storage-foreground-plan-recovery-decision-packet.md`。M2保持打开，
M3关闭。

后续聚焦只读审计确认，当前产品不能对同一 Candidate Revision 简单补一位 Judge：一个
Revision只能有一个冻结Review Graph，相同Profile集合仅幂等返回，任何变更/扩展/重排都会在
写入前拒绝；terminal assignment只解析一次，reviewer Task又冻结一个base Attempt和零
extra/correction/adaptation。Integration的两份usable opinion门也不能被Main accept覆盖。

这次DeepSeek原始结果其实是正确Revision的完整`accept` JSON，唯一schema失败是summary
507字符超过冻结上限500；所有字段、findings和相对evidence path均合法。历史同一storage链的
DeepSeek Pro和MiniMax schema失败也分别是summary 558和733字符超过500，其余shape完整。
这已经是第三个真实case。重复精确Candidate只为获得新Graph会重做已经毕业的执行/验收，且
不会修复产品缺口。最小对齐方向是一个Main显式、一次性、同Judge/同Revision/同packet的
schema-only result repair：保留原失败记录，不允许Candidate访问、model switch或自动retry，
只有新的schema-valid结果才补足原独立意见。该行为尚无accepted Work Item或授权，M2仍等待
一骏决定。

## Evidence status

- M2 delivery graduation: missing
- M2 storage lifecycle graduation: original M2-C graduated — `evidence/m2-storage-lifecycle.md`; final delivery-Task terminal disposition supplement active
- M3 routing graduation: missing
- M4 valid Main-Token pairs: insufficient
- M5 clean-clone and final Hub: missing

Missing evidence means not graduated; it does not authorize synthetic reruns.
## 2026-08-15 — One-shot same-Judge schema-only repair authorized and specified

一骏明确授权创建并执行新的M2-B产品Work Item，同时冻结四项边界：不得重跑Candidate、切换
Judge身份、增加第三位Judge或自动重试。Main重新核对Git/Daemon真相：CLI与Daemon build
identity matched，Grok 4.6 Xhigh launchable；所有现有未提交成果保持不动。

聚焦调用链审计确认最小完整产品面是Review Result admission，不是Candidate或Review Graph
重建。当前DeepSeek Flash原始JSON有精确五个root keys、正确Revision、`accept`、安全相对
路径和bounded findings，唯一失败是summary 507 > 500；历史同链另有558和733两个同类case。
Accepted Spec为
`specs/m2-b-quality-decision-chain/work-items/judge-result-schema-repair/spec.md`，operational
Task合同为
`goals/forklight-main-led-execution/execution/m2-judge-result-schema-repair/01-implementation.yaml`。

设计只允许原Reviewer Task成功、原assignment为`schema-violation`、且专用eligibility parser
证明唯一缺陷是summary超限时创建一次repair。原assignment status/failure/task/raw Attempt
证据不覆盖；repair是同assignment下同冻结Profile/Provider/model/Runtime/effort的只读派生Task，
不增加assignment或Judge数。presence of repair永久耗尽一次额度；Daemon restart只能恢复同一
Task。当前native-Goal Candidate focus paths、storage两路径和本Work Item产品路径互不重叠，但
Main仍串行Integration。M3保持关闭。

## 2026-08-15 — One-shot repair implementation stopped at immutable two-Judge gate

ForkLight implementation Task `b4d1fcab-215d-498f-bf8c-486ff1cc20b7` used Grok 4.6 Xhigh in
honest `persistent-session` mode while its operational wrapper completed native `/goal`
`9cdfd2f5-e366-45c4-9812-01a1893b40dd`. Main found three blocking gaps in Candidate Revision
`2d81f218-0819-4584-b36b-b76bb3483f7a` and used the single authorized correction on the same
Task/Session; successor native Goal `9be4561a-d7eb-41da-a44d-ab58f33d18af` completed `achieved`.
No correction allowance remains.

Final Candidate Revision `3fb7d72d-7d19-4b34-b2ff-42dd108429f3`, digest
`4f746b2e73df3e0f8b1037e70f6fad36c75045b1f4403e348f69e363ef68809a`, contains 16 accepted
product/test paths. ForkLight independent verification sequence 13330 passed build, all 441 focused
tests and diff check. Main re-inspected the corrected parser, packet admission/copy, append-only
Store transaction, frozen identity, one-Attempt policy, restart recovery, semantic equality,
Integration gate and privacy-safe CLI status; the three prior blockers are closed.

Review Graph `02427b99-cf08-4283-9c5f-7cd51948aa9e` froze exactly two assignments for that Revision.
Codex Luna Max assignment `faf2f463-5833-4242-896b-7047ae8371d5` is usable `accept`.
Volcengine GLM 5.2 assignment `4bab381d-45d2-4aa6-9da1-5901fdc839db` ran successfully, but its
structured result contained a malformed/extra field and ForkLight correctly classified it
`extra-fields`, unusable. This is not an otherwise-valid over-limit summary, so the accepted repair
boundary cannot repair it. Aggregation is `single-opinion` (1 usable, 1 unusable).

The confirmed stop rule now applies: no Candidate rerun, Judge identity switch, third/replacement
Judge or automatic retry. Main did not accept or integrate the product Candidate and did not invoke
repair on storage assignment `16123390-e749-466b-a53c-2035913defa8`. Both Candidates and all review
evidence remain protected. Decision packet:
`evidence/m2-judge-result-schema-repair-decision-packet.md`. M2 and the Codex Goal remain active;
M3 stays closed pending a new explicit decision.

## 2026-08-16 — Native Goal product Candidate delivered; no-wrapper live gate remains

Authorized replacement Task `847b24ef-0ac9-4958-9959-9a64b0bc466e` used its sole Grok 4.6 Xhigh
Attempt and completed real native Goal `c170a1a9-ebb7-4bea-a748-59594f4754f7` with one Worker round,
one classifier run, durable `complete/Idle`, `achieved` and readable Task-owned classifier details.
The wrapper changed only internal Git global/system config isolation; its historical predecessor
remains immutable.

Fresh Revision `6696e0b5-49f1-4f32-a68a-985068c2906e`, digest
`251e45550486c39fb7ec9de0aff795d8bebebfe2adb6d22a4b90af2a12ca258d`, retains the accepted 14
paths and differs from Revision `00d99db6...` only in `src/workers/grok.ts` and
`tests/worker-runtime.test.ts`. ForkLight verification sequence `1843` passed all five commands and
471 focused tests. Exactly two fresh Judges—Codex Luna Max and DeepSeek Flash 1M—returned usable
`accept`; Main accepted the exact Revision and digest.

Safe Integration `daa61736-5012-4ed3-9724-bd4dfeaf2b4d` passed source apply/verify, build and
activation. The high-risk `npm run check` then passed 2,949/2,949 tests; after restarting the Daemon
for that final rebuild, client/Daemon identity is matched and new `grok-4-6-xhigh` work resolves
`auto -> native-goal`. No commit, push, reset or reclaim occurred. Full evidence is
`evidence/m2-grok-native-goal-git-isolated-replacement-delivery.md`.

The bootstrap Task's durable execution mode remains honestly `persistent-session`; a no-wrapper
product live Task has not yet run, so this M2-A supplement is not graduated. Main did not create a
ceremonial self-proof Task. The only meaningful ready M2 chains—resolved-terminal storage and
Judge-result schema repair—remain protected by their separate no-replacement/two-usable-Judge stop
decisions, and the current authorization explicitly adds no authority to revive either one. M2
therefore remains open at a Milestone-level decision; M3 stays closed.

## 2026-08-16 — One-time 1-Judge exception authorized for exact Judge-repair Candidate

一骏 selected Main's recommended first option. The exception is bound to Task `b4d1fcab...`,
verification `13330`, Revision `3fb7d72d...` and digest `4f746b2e...809a`; it does not make the
Volcengine `extra-fields` result usable or change the frozen Store requirement. Current product has
no one-Task waiver operation, so Main will not edit immutable Task truth. Normal preflight must
first prove its only remaining rejection is the authorized review-depth exception; Main then
reproduces all other exact-patch/source/verification guards, applies once, runs the accepted and
full checks, and records the delivery honestly as a manual exception rather than a ForkLight
Integration result. The downstream storage Candidate retains its normal two-usable-Judge gate.

## 2026-08-16 — Judge repair delivered and storage Candidate safely integrated

Main bound the exception to exact Judge-repair Revision
`3fb7d72d-7d19-4b34-b2ff-42dd108429f3`, digest
`4f746b2e73df3e0f8b1037e70f6fad36c75045b1f4403e348f69e363ef68809a`. Normal preflight receipt
`a99441a6-652a-40d8-b71a-751fb3dfc05c` had exactly one rejection: the frozen Task had only one
usable terminal Judge opinion. All 16 current source paths matched the Task source manifest, the
patch digest matched, and isolated Git apply-check passed. Main then performed the authorized
one-time exact manual apply; no Store field or general Integration gate changed and no ForkLight
Integration result is claimed for this exception.

Build, all 441 accepted focused tests, diff check and the 2,964-test full suite passed. After
activation, Main invoked the product operation exactly once on assignment
`16123390-e749-466b-a53c-2035913defa8`. Repair Task
`bbf11b7f-e3bf-4e88-828c-06845a6027de`, Attempt
`76b6cb4e-17bc-4807-b448-465d7c771d09`, succeeded under the same frozen DeepSeek Flash identity.
It used one Attempt, no fallback, 50,157 gross Worker Tokens and quoted official cost USD
0.0058094736. The original Review Graph now has two usable `accept` opinions and requires a fresh
Main decision.

Main re-inspected storage Revision `d6c85d59-10dc-4325-a0fc-e627b62130c2`, full digest
`48a73c6b6db1071fd62f2dfffa3799df81805cdc1e6d4a46cb163bb9ebdafcda`, confirmed the imported
decision-packet export and active/missing-Revision safety tests, and recorded fresh `accept`.
Preflight receipt `72a9c635-7489-4d85-a347-31d710f6a4b4` had no rejection. Normal ForkLight
Integration `c68d7eab-f24c-46df-af82-7a0f10a82854` passed source apply, six independent acceptance
commands, build, activation and checks. Final `npm run check` passed 2,966/2,966; Daemon/client
identity is matched and `grok-4-6-xhigh` resolves wrapper-free `native-goal`.

Read-only audit and exact Task preview classify the delivered storage Task as
`reclaimable/integration-delivered`: 134,988,002 regenerable bytes are named and 4,186,657 durable
bytes are preserved; processes are empty, SQLite quick-check is `ok` and foreign-key violations are
zero. No reclaim, commit, push or reset occurred. The next serial wave is the three existing
zero-diff activated-source audits. Audit one now uses Grok 4.6 Xhigh native `/goal` because it is a
meaningful accepted lifecycle gate and can supply the still-missing no-wrapper live evidence.

## 2026-08-16 — First wrapper-free audit stops on Grok's external 600-second Planner cap

Audit Task `385e9c92-e3bb-4eb9-8008-e5ae255f1b31`, Session
`0e01f126-7880-4414-bbb2-9636cd44f0fa`, Attempt
`41d554a5-6358-4f2c-bbb2-ccd28e870299` truthfully resolved to Grok 4.6 Xhigh `native-goal`. Its
ForkLight duration, Token and no-progress limits were all unset. Two Main observation waits ended
without canceling the Task, and the Planner made 124 useful read calls.

Grok CLI 1.0.4 itself then canceled that foreground Planner at `budget_ms=600000`. Native Goal
`215e6043-365b-4ac9-885c-946f608f63e5` is durably `user_paused/Executing`, with zero classifier,
worker or verification rounds and pause message `Planning failed; resume with /goal to retry.` No
verification, Candidate, Judge or source change exists. The failed Task remains immutable and its
one-Attempt/no-retry contract is exhausted.

Installed Runtime evidence exposes official variable `GROK_FOREGROUND_BLOCK_BUDGET_MS`; Grok
accepts `86400000`. The accepted M2-A Work Item
`specs/m2-a-grok-execution-truth/work-items/grok-native-goal-foreground-budget/spec.md` changes only
`src/workers/grok.ts` and `tests/worker-runtime.test.ts`: native Goal children freeze a 24-hour
process safety breaker, while non-native children delete inherited drift. It adds no public setting,
Task deadline, retry, coordination mechanism or storage change. After independent verification,
two Judges, Main safe Integration, full check and activation, Main will create one new audit Task;
the remaining two audits stay serial. Exact evidence is in
`evidence/m2-grok-native-goal-foreground-budget-decision-packet.md`.

## 2026-08-16 — Foreground Planner fix delivered through normal gates

ForkLight Task `460ee0af-8709-416a-9e38-0859aec77bb0`, Session
`c516066c-a481-4ae1-a7c6-468c283f62b5`, Attempt
`eb50fd41-9c04-411a-bf74-a51519d2fc3b` completed one real Grok 4.6 Xhigh native Goal
`7813fd8f-2700-4dc4-87ea-68ad096deb9` with classifier verdict `achieved`. Exact Revision
`c0fb927a-65b0-4d84-8534-a04adc3f29e8`, digest
`f5577b0224a8dfd618bd055f7229b6946a6682e2fe861d62d74c8f548abf12f5`, changes only
`src/workers/grok.ts` and `tests/worker-runtime.test.ts` (46 changed lines). ForkLight independently
passed build, all 134 runtime tests and diff check; no validation repair or Main correction ran.

Review Graph `e8bdc35b-db27-4840-9696-5ec844993db4` returned two usable `accept` opinions from Codex
Luna Max and DeepSeek Flash. Main resolved both non-blocking count warnings against the accepted
2-file/120-line budget and exact `git apply --numstat` 5+41 result, then recorded fresh accept.
Preflight receipt `7fb72a96-03e0-4c76-9cd8-b0f0fa0f12d7` had no rejection. Normal Integration
`ba60264f-d1c3-48d9-aa7d-b2bf253a1d27` passed source apply, three independent commands, build,
activation and activation check. Final `npm run check` passed 2,968/2,968; the restarted Daemon and
client identities match and Grok 4.6 Xhigh still resolves `native-goal`.

Worker usage is unavailable from the Grok terminal; ForkLight reports only Runtime estimate USD
0.31896658 and no official cost or Token-saving claim. A new activated-source audit was then
admitted as the required end-to-end proof; its distinct terminal stop is recorded below.

## 2026-08-16 — Post-fix audit stops on read-only native Goal plan persistence

New audit Task `f20334a0-b8bd-4886-977b-62fa3553e501`, Session
`08c5d7f5-b3e1-4724-a962-a1885180896f`, Attempt
`174e03b9-c750-4221-a58c-4a94f459c6de` did not reproduce the old timeout. Its Planner ran
505,496 ms with 73 tool calls and returned a complete, usable audit plan in durable subagent
`01a0068c-990b-7ff0-9940-84fbfcd76a86` output. There is no foreground-budget cancellation event.

The distinct failure is plan persistence. Current `grokNativeAllowTools(false)` exposes only read,
Agent, terminal and Task-control registrations; it omits `search_replace/write`. The Planner's own
prompt requires its only write to Task-local `grok-home/.../goal/plan-*.md`. It explicitly reported
that file tools were unavailable and returned the plan to its parent instead. Native Goal
`13a67662-170b-40dc-a758-ad79487f1528` therefore has `plan_file: null`, zero classifier/Worker/
verification rounds and durable `user_paused/Executing` with the generic planning-failed message.
The later ordinary turn's correct audit prose does not change Goal truth.

The Task used one Attempt with no validation repair, correction or retry; it produced no Candidate
and ForkLight ran no acceptance command. Runtime estimate is USD 0.14667396; terminal Token usage
is unavailable to ForkLight, so no official cost or saving claim exists. Read-only storage preview
classifies its 117,490,429-byte Workspace as `protected/unresolved-terminal`, with 0 unknown bytes,
no process, SQLite `ok/0`; no reclaim occurred.

This is outside the foreground-budget Spec's two-path environment boundary, so that Work Item
stopped correctly. 一骏 has now authorized the separate accepted M2-A Work Item
[`grok-native-goal-read-only-plan-persistence/spec.md`](../../specs/m2-a-grok-execution-truth/work-items/grok-native-goal-read-only-plan-persistence/spec.md).
It always registers Task-local plan mutators for native Goal, adds exact Workspace `Write/Edit`
deny rules only when `allowEdits:false`, preserves current credential/state/Bash and background-
control policy, and proves editable native plus persistent-session read-only behavior. Execution is
one Grok 4.6 Xhigh Attempt with zero repair/correction/retry/replacement, independent acceptance,
two Judges, Main serial Integration and one new live audit.

A final read-only feasibility check removed design uncertainty. The existing operational
`foreground-plan-bootstrap.test.mjs` passes, and historical Task
`6fbf8e70-a48a-4e7b-8cb8-3dc07e836085` still provides durable real-Runtime proof: native Goal
`e1941538-caf8-4c0b-bdf1-5c5241bf3476` is `complete/Idle`, its Task-local plan is 7,281 bytes and
classifier verdict is `achieved`, while exact Workspace `Write/Edit` and Bash remained denied.
The next product Work Item can therefore stay limited to the normal Grok adapter plus focused
runtime tests; it need not change schema, storage, settings or current background-control policy.

## 2026-08-16 — One-shot plan-persistence implementation stops on Grok authentication

Authorized Task `d3c41fff-7bad-4af9-99fd-6e1c95657b9c`, Session
`24335d0e-675a-4948-b361-353cbae9f351`, Attempt
`a7c280c0-d768-4077-8c8f-74248377c2d2` used Grok 4.6 Xhigh native Goal
`8a8dbe45-8cb9-4c60-a11f-6b84bbb4517a`. The Task-local 5,986-byte plan persisted and one Worker
round wrote the intended two-path patch. During classifier execution, OAuth refresh returned
`invalid_grant`; all three classifier calls and the parent then received 401. Durable Goal truth is
`infra_paused/Executing`, `not_achieved`, one classifier attempt and zero verify rounds.

ForkLight used exactly one Attempt, skipped disabled validation repair, captured no Candidate
Revision and ran no independent acceptance. Main therefore did not create the required Review
Graph, accept, integrate or submit the new live audit. The protected Workspace differs from its
baseline only at the two allowed paths (143 changed lines) and is reusable but unverified. Fresh
storage preview is protected with 0 unknown bytes, no process and SQLite `ok/0`; no reclaim ran.
The frozen no-retry/no-replacement boundary now stops execution. Decision packet:
`evidence/m2-grok-native-goal-read-only-plan-persistence-decision-packet.md`.

一骏随后完成Grok重新登录，并明确授权复用同一Task/Session/plan/Workspace的一次resume。
直接只读Grok 4.6 Xhigh烟测已返回`AUTH_OK`，所以外部认证阻塞已经解除。但Task Store快照同时
冻结`maxExtraAttempts: 0`与`maxAdaptationRounds: 0`；ForkLight只读adaptation preview尝试把
前者设为1时返回`stopped/adaptation-disabled`。因此没有authorization grant、第二Attempt或
Task状态变化。Main没有修改Store、改变全局settings、停Daemon走不同代码路径或创建未授权
replacement。精确partial仍受保护，等待一个可由产品表达的新恢复边界。

一骏现已明确修改no-replacement边界，授权一个新的单次recovery Task采用旧Task受保护的精确
两文件成果。Main已固化`retained-candidate.diff`；聚焦检查证明它只含两个允许路径、能对当前
source正向应用，并能从旧Workspace精确反向还原。新Task继续使用Grok 4.6 Xhigh真实native
Goal，一个Attempt、零repair/correction/retry/replacement，独立验收、双Judge、Main串行
Integration和原新live audit。原Task保持immutable/protected。

## 2026-08-16 — Protected partial delivered; activated audits and exact preview passed

Recovery Task `89829dc6-be26-44ea-8517-ba901e0f1be3`, Session
`da1ab17e-34a4-49bb-ab11-13f8f95cb50d`, Attempt
`a03061b4-129f-47c7-9b23-e900ddda4c91` adopted the protected two-path partial through a real
Grok 4.6 Xhigh native Goal. Goal `f1a3cbc9-1b8a-4d37-96a3-b13614383993` is `complete/Idle`, its
Task-local plan is 5,358 bytes and its sole classifier verdict is `achieved`. ForkLight captured
Revision `f934cc84-36bf-40f6-93e4-a7c849d6901c`, full digest
`56a6a4aef264a530b7c5d8e1193ca4c458c04f1841d485b6beb52319247e83fe`, exactly two files and
143 changed lines. Build, all 137 focused runtime tests, retained-Candidate reverse apply and diff
validation passed independently at verification sequence `2002`.

Review Graph `60ea83d9-7bf1-4cb5-9eea-ca6ba5eb001d` returned two usable independent `accept`
opinions from Codex Luna Max and DeepSeek Flash. Main inspected the actual diff and accepted the
exact Revision. Preflight receipt `6b8592af-603a-4996-b92c-8e9e37bff7f8` had no rejection;
Integration `18f71ac9-10e7-4e04-9225-22c12b3368d5` passed source apply, four source-verification
commands, build and runtime activation. `npm run check` then passed 2,971/2,971 and the restarted
Daemon matched the client build.

The new live audit Task `b3b78cfc-d3e8-4335-9707-a872b6d9561b` completed one wrapper-free Grok
4.6 Xhigh native Goal `e9bf72b0-957e-4efc-841e-7278768f6c5a`: `complete/Idle`, classifier
`achieved`, 5,124-byte Task-local plan, three independent commands passed and a zero-byte Candidate
diff. Public storage audit `196931bf-15f2-4212-b90b-ebce0a853de6` and final scope/safety audit
`d4c8d73f-e64a-47c6-9f28-2b7903fd6bff` also passed independently with zero-byte Candidates;
Main accepted all three under their explicit deterministic Judge skips.

Serial CLI preview of the six already-resolved historical Tasks now reports all six as
`reclaimable/main-resolved-terminal`: 685,377,515 regenerable bytes across 31 known targets,
9,102,847 durable bytes preserved, zero unknown bytes and zero processes. Every preview reports
SQLite `quickCheck: ok` and zero foreign-key violations. Global audit has 427 known entries
(97 protected, 70 reclaimable, 260 reclaimed), zero `unknown-orphan` classification, zero process
and integrity `ok/0`; 5,196,678 bytes of unknown content inside known roots remain protected.
An attempted concurrent observation briefly exhausted the generic short request window, but it
did not cancel or mutate Daemon work; the contract-required serial CLI previews all returned
normally, so no new timeout Work Item is admitted. No reclaim, commit, push or reset occurred.

## 2026-08-16 — Authorized reclaim pauses on a real storage-read observation gap

一骏 explicitly authorized six exact per-Task reclaim calls. Main preserved the frozen ids and ran
a new serial preview before mutation. The first command again returned
`ForkLight daemon request timed out: storage_preview`; no deletion started. This occurrence had no
parallel request. The Daemon completed the read-only scan after client disconnect, and a follow-up
no-timeout read returned the expected Task classification, unknown 0, process 0 and integrity
`ok/0`; a warm scan still measured 12,631 ms against the fixed 15-second transport limit.

The earlier conclusion that serial CLI preview was reliably within the short window is therefore
superseded. The concrete M2-C blocker is now accepted as
`specs/m2-c-task-storage-lifecycle/work-items/storage-read-transport-observation/spec.md`. One Grok
4.6 Xhigh native Goal Writer may change only `src/daemon/client.ts` and `tests/daemon.test.ts`,
reusing the established long observation mapping for `storage_audit` and `storage_preview` while
keeping `storage_retain`, health and storage semantics unchanged. Independent verification, two
Judges, Main safe Integration, activation and fresh real CLI previews precede the already-authorized
reclaim. No real Home mutation, fallback mechanism, commit, push, reset or M3 work has started.

## 2026-08-16 — Storage observation delivered; six exact reclaims complete M2

ForkLight implementation Task `14992906-f3ac-4ff6-8555-e3859792ae88` completed one real Grok 4.6
Xhigh native Goal and produced exact two-path Revision
`20662dd6-0206-434c-9e90-4797996482e1`, digest
`6354907cf0a3003bcb11cc5bb4b2a5a06158ca1519eb929661fb76db432d0134`. Verification sequence
`1882` passed build, focused daemon/CLI/MCP tests and diff validation. Review Graph
`a9e592b3-77cd-43eb-b38d-145b1869aa2b` returned usable accepts from Codex Luna Max and DeepSeek
Flash; Main accepted and Integration `8e97d3f9-0c83-43f5-b18d-5ae083ed23e0` passed apply,
verification, build and activation. Final `npm run check` passed 2,971/2,971 and restarted client/
Daemon identities matched.

The activated CLI then returned a global audit plus six fresh serial exact previews normally. All
six remained `reclaimable/main-resolved-terminal`, with the frozen 31 targets, 685,377,515
regenerable bytes, 9,102,847 durable bytes, zero unknown bytes/processes and integrity `ok/0`.
Main consumed 一骏's exact authorization through six serial `storage reclaim --task <id>` calls;
every call applied fully, removed only its named known targets, recorded disposition and preserved
the six durable categories. No other eligible Task was included.

Post-reclaim preview classifies all six as `reclaimed/known-regenerable-removed`, with zero targets,
zero regenerable/unknown bytes, no processes and all 9,102,847 durable bytes still present. Final
global audit reports 430 known entries: 97 protected, 67 unrelated reclaimable and 266 reclaimed;
there are zero unknown-orphan entries, zero processes and integrity `ok/0`. The 5,196,678 bytes of
unknown content inside known historical roots remain protected. M2 is graduated; Main stops at the
required M3 confirmation boundary. No commit, push or reset occurred.

## 2026-08-19 — M5-B switches from failed Grok delivery to Main-direct UI execution

Exact-Candidate delivery Task `78c6fec4-b030-4b30-988b-41d01973cbdd` is terminal `failed` after
one Attempt. The process exited zero after 11 turns, but Task-local native Goal state was absent;
ForkLight recorded no Candidate, verification, review or Integration. The Task changed zero files
and its Workspace remains `protect-review`. The earlier structural Workspace still passes Main's
build and 424 focused tests and remains the sole reusable UI output.

一骏 explicitly directs Main to implement the UI itself. Main will not create another M5-B UI
Worker. A ForkLight `main-direct` record tracks this source-drift-proven seven-path application,
machine verification, full 320/360/390/667x375/1440 browser acceptance and any same-boundary
correction. Read-only final review remains independent of implementation. M5-C remains held.

Future Grok work is uncapped for Task duration, Token, no-progress and model-work waiting. A
mandatory client observation timeout is not a Worker deadline and must never cancel, fail,
recreate or reroute the still-running Task; observation resumes from durable events.

ForkLight Main-direct decision `ceba063f-8ce3-4773-b8cd-fa039bd9ea17` is now open for this exact
M5-B slice. It records one considered Grok 4.6 Xhigh profile, zero Worker Attempt and no routing
or Token-saving claim.

Live Main browser review then found a product-level presentation gap in the applied structural
Candidate. Work is form-first and flat: a large outcome form plus “Task、Plan、Goal 怎么选” lesson
pushes the user's real Goals below the phone viewport, while desktop reads like an administration
document rather than the user's work. 一骏 explicitly rejects this mechanical framing.

M5-B acceptance is updated in its existing Work Item spec. Main is replacing the lesson and flat
cards with a real Goal → Plan → Task tree. The root adds a Goal, each Goal adds a Plan and each Plan
adds a Task through the existing intake/confirmation flow. The tree is first on mobile; independent
work remains reachable; no API/schema or backend hierarchy changes. Machine and five-viewport
browser acceptance restart against this user-view result. M5-C remains held.

## 2026-08-19 — M5-B user-view Hub redesign graduates; M5-C starts

Main-direct decision `ceba063f-8ce3-4773-b8cd-fa039bd9ea17` is terminal `completed` with
verification `passed`. The final Work surface leads with the user's real nested Goal → Plan → Task
tree; New Goal, Goal-level Add Plan and Plan-level Add Task open one contextual instance of the
existing intake/confirmation flow. The rejected form-first lesson is absent, independent work stays
reachable, parent context is exact and raw ids remain out of primary copy. No API, schema, Core,
Store or daemon hierarchy changed, and no successful Task, Candidate, Integration receipt or Token
saving was fabricated for the Main-direct path.

The final acceptance correction keeps contextual composers visible inside folded Goal/Plan
branches, includes submit/error/confirm state in visible-work retention, prevents a closed then
same-id-reopened Task from accepting an older response, and gives the mobile Task sheet one visible
Back/focus lifecycle across loading, success and failure. `npm run build`, the six accepted Hub test
files (424/424), `git diff --check`, Hub script parsing and the final full `npm run check`
(3162/3162) pass. Real browser checks pass at 320, 360, 390, 667×375 and 1440×900, including
overflow, hierarchy, search ancestry, contextual add/cancel, empty-submit recovery, System
dismissal, Task detail Back/focus and narrow/wide transitions. The original UX and contract
reviewers both return `accept` after the corrections, with no remaining Blocker/P1.

Evidence is `evidence/m5-b-main-direct-user-view-delivery.md`. The two failed Grok UI Tasks and
their historical evidence remain truthful and protected; no commit, push or reset occurred. M5-B
is graduated. M5-C is now the sole active serial Work Item. Its accepted spec has been reread: Main
can begin package/visual/restart preflight, but the 30-minute journey and uncoached comprehension
answers require a genuinely unfamiliar operator and clean disposable environment. No synthetic or
coached substitute is admitted.

M5-C package preflight then produced a temporary normal `npm pack` artifact at
`/tmp/forklight-m5c-preflight.qIQMjM/forklight-0.2.0.tgz`. Prepack independently reran and passed
3162/3162 tests; the tarball contains the four built Hub assets and build identity, with SHA-256
`d88ede8138429763397d648f723ca33f8b4f37ef1e37d7a98b4e2f86232a9b36`. This artifact is
regenerable preflight evidence, not the final frozen unfamiliar-user bundle.

The same preflight found one real packaged-journey blocker: `docs/m1-clean-user-runbook.md` still
directs a new user to the retired Overview first-Task card, while graduated M5-B authenticates into
the Work hierarchy. The accepted M5-C spec now contains one bounded pre-observation prerequisite:
exactly the runbook and `tests/clean-run-bundle.test.ts`, no product-code change. ForkLight Task
`83c3142a-8868-4ae6-94ca-22b2fcc51a5f` is queued on Grok 4.6 Xhigh native Goal with one base
Attempt, one validation repair, zero other recovery/fallback, one ordinary Judge, and unlimited
duration/Token/no-progress/model waiting. Its accepted task file is
`execution/m5-wave-5/01-m5-c-current-work-runbook.yaml`.

## 2026-08-19 — Reopen M5-B human acceptance after user review

一骏 rejects the current Hub as still too mechanical. The accepted Goal → Plan → Task tree and its
safe creation/detail continuity remain reusable, but the selected Goal still reads as a stack of
system sections and status fragments instead of the user's current-work story. This is a real M5
human-acceptance failure, so the earlier M5-B graduation evidence remains truthful history but no
longer authorizes unfamiliar-user observation.

Main updated the existing M5-B spec instead of creating another roadmap or UI Worker. The new
boundary makes the Goal desired result, current truth and next action the opening hierarchy; treats
the tree as a document outline; demotes redundant Work instructions, daemon/count chrome and empty
no-decision/no-blocker panels; keeps decisions prominent only when real; and limits motion to
state-explaining branch, composer, selection and pane transitions. No backend/API/Store/Core change
is admitted.

M5-C Task `83c3142a-8868-4ae6-94ca-22b2fcc51a5f` remains running because its exact runbook/test
paths are orthogonal to the Hub assets and its instruction to use New Goal → Add Plan → Add Task
remains correct. Main does not cancel, recreate or time-limit it. The Task may complete its quality
chain, but the final unfamiliar-human clock stays held until the revised Hub passes machine,
responsive, interaction, copy and final user review.

## 2026-08-19 — Reopened M5-B closes on a user-readable Goal document

Main completed the authorized UI revision without another UI Worker. Work now uses the tree as a
real outline instead of a labeled object inventory. The selected Goal opens with its desired
result, “Now” and “Next”; only a real decision is promoted; current Plan and Task read as the Goal's
active chapter; empty blocker, delivery and active-task panels disappear; technical truth stays
folded after the document. The rejected vocabulary lesson, duplicate Work heading and worker/count
chrome no longer compete with the user's work. Contextual New Goal/Add Plan/Add Task still uses the
existing proposal and confirmation path, and Task detail keeps the accepted Back, focus,
stale-response and live-refresh behavior.

`npm run build` passes. The accepted focused Hub pair passes 269/269, `git diff --check` and Hub
script parsing pass, and the required single Impeccable detector run returns no findings. Batched
browser QA confirms fitted desktop and landscape layouts plus exact 320/360/390 measurements with
no horizontal overflow; the retained desktop, mobile Goal-file and mobile contextual-add captures
are indexed in the existing M5-B evidence. One fresh independent read-only reviewer returns
`accept` with no Blocker/P1 after independently rerunning the scoped checks.

This is Main-direct truth: no Worker Attempt, Candidate Revision, Integration receipt or Token
saving is claimed for the revision. No backend, API, schema, Store, Core or daemon path changed.
M5-B is graduated again on the new human-acceptance result. M5-C is the sole active Work Item; its
orthogonal runbook Task keeps its durable state and is not canceled, recreated or time-limited.

## 2026-08-19 — M5-C runbook delivery and clean bundle complete technical preflight

Grok 4.6 Xhigh native-Goal Task `83c3142a-8868-4ae6-94ca-22b2fcc51a5f` completed one Attempt and
captured exact two-path Revision `f3c764c1-f8bf-4364-9122-4628057ec445`, digest
`9f3c0f084f97c455af0f1eb05f13dd4bee0d50ac66ddaa843de9c54d1ebfa374`. The runbook now tells an
unfamiliar operator to follow Work → New Goal → Add Plan → Add Task, review ForkLight's proposal
and explicitly confirm before records or work begin, then continue through ordinary Task Detail.
One focused real-file test prevents the retired Overview instruction from returning. Exactly the
accepted runbook and test paths changed; no product behavior changed.

ForkLight independent verification passed 32/32 focused tests, build and diff check. Review Graph
`9d6b6e7f-52b8-4492-92d4-cf915b11070c` returned one usable Codex Luna Max `accept`. Main inspected
the exact diff and accepted it. Preflight receipt `97f2a345-b09b-4c66-af28-e8591d919084` found no
source/path rejection; Integration `c78b0c3a-fbc5-442f-9df3-a161694004df` passed source apply,
three source checks, build, Daemon activation and matched-health verification. No repair, retry,
correction, fallback or Competition was used. Worker Token volume remains unavailable, so this
Task makes no Token-saving claim.

The activated source generated `/Users/Shared/ForkLight-Clean-Run.43560ab8`. Its tarball SHA-256 is
`e737f895a80cd8f180035190fdb4f880fecf28ebd1bc131c2f0f59b5cb98d30b`; build id is
`43560ab8c35e70307fa742e25399eeb59a69da405313dcc37119ec2cef50b2ca`. Prepack passed 3163/3163;
isolated prefix install, installed CLI/MCP/Hub entry, identity match, sensitive-filename scan and
clean shutdown all passed. The packaged runbook contains the current Work path. This is the fresh
frozen unfamiliar-user packet, not human evidence. M5-C now waits only for a genuinely unfamiliar
operator in a clean disposable environment; Main does not substitute an Agent or favorable replay.

## 2026-08-19 — Completion audit restores the required M2 delivery artifact

The contract completion audit found that `evidence/README.md` indexed the required
`m2-delivery-graduation.md`, but the file itself was absent. Current Store/Goal truth still proves
the Coding final audits, handoff destination-truth recovery and exact non-Coding recovery are each
durably `completed` at 4/4, so this was an evidence-packaging gap, not a product or Milestone
regression.

Main created the exact contract artifact from existing accepted Task, Revision, verification,
Judge, Main-decision, Integration and zero-change audit records. It preserves original failures and
links storage disposition to the existing M2 storage evidence; it does not rerun M2, invent a new
roadmap, change product code or fabricate a cleaner history. The completion audit still does not
graduate the overall Goal because M5-C's genuinely unfamiliar-human journey remains missing.

## 2026-08-19 — M5-C unfamiliar-operator handoff is frozen

Main added the final no-coaching observation protocol to the existing M5-C preflight evidence. It
defines subject eligibility, clean-environment input, the only permitted opening sentence, exact
timing checkpoints, intervention/privacy recording, five uncoached comprehension questions,
visual/restart checks and a fail-closed verdict. This removes the risk that a future observer
explains the product, reruns an unfavorable result or treats field presence as comprehension.

No product code, package byte, Test, Task, Worker or runtime state changed. The verified packet
remains `/Users/Shared/ForkLight-Clean-Run.43560ab8`. No eligible unfamiliar operator or separately
authorized clean system environment is currently available, so no further in-scope automated work
can produce the missing human evidence.

## 2026-08-22 — Bare local Hub access becomes invisible and Main-direct

一骏 opened the printed bare `http://127.0.0.1:62283/` address and received the retired
“未认证会话。请运行：forklight hub” state. GoalBoard confirmed the intended family behavior: retain
one private local control token but inject it invisibly, so the user never manages authentication.
The existing M5-B Spec now freezes that corrected boundary.

Main directly applied the correction after 一骏 stopped further ForkLight implementation. CLI
start/reuse/restart paths now return bare loopback URLs. The Hub rejects non-loopback Host values
before page or API work, injects its per-process token only into the in-memory index response and
keeps the checked-in asset token-free. The browser reads that meta value into memory, sends the
existing private header, never reads a fragment or persists the token in session storage, and
reloads at most once after a stale-token 401. Repeated failure stops in one plain local-connection
state. Existing mutation confirmation and Daemon boundaries are unchanged.

Build passes; the accepted instance/CLI/operations/UI suite passes 316/316; the corrected settings
fixture passes 75/75; JavaScript parsing and `git diff --check` pass; final `npm run check` passes
3186/3186. The single final Impeccable detector run returns no findings. Hub PID 62329 is active on
port 62283 after confirmed detached replacement. A real bare-address reload reaches the Work
surface, keeps the browser URL exactly `http://127.0.0.1:62283/`, and contains neither the retired
unauthenticated copy nor the local-connection failure state.

The first failed isolated attempt and the user-canceled recovery remain historical product data;
they are not delivery evidence. Per 一骏's current rule, subsequent repository implementation and
review are Main-direct unless he explicitly reverses it. No commit or push occurred. M5-C's
unfamiliar-human evidence remains deferred and missing.
