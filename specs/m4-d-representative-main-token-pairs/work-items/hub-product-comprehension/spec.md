# M4-D3 Work Item — historical Hub comprehension paired calibration

## User result

Turn accepted historical Hub-labelled task/routing evidence into a small future-M5 requirements
artifact, explicitly without starting or changing Hub/UI, then form the final same-scope
direct/delegated Main Token pair for `hub-product-comprehension`.

## Background and current evidence

Accepted M3 evidence read the historical family without creating Hub work and returned
`cannot-determine / score-gap-too-small`. The historical task contracts cover six future product
needs: truthful failure detail, human information hierarchy, Now/History separation, durable
history pagination, readable dependency lineage and submit-preview information hierarchy. These
are requirements inputs for M5, not evidence that M5 is implemented.

## `depends_on`

- M4-D2 has a durable pair assessment/report or terminal decision packet.
- The accepted M3 entry/wave/graduation evidence and six historical Hub-labelled task contracts
  remain readable.
- No Hub process is started and no other M4-D pair is active.

## Inputs and outputs

- Input: `goals/forklight-main-led-execution/execution/m4-calibration/inputs/hub-product-comprehension.json`.
- Shared validator: `goals/forklight-main-led-execution/execution/m4-calibration/validate-artifact.mjs`.
- Output: `goals/forklight-main-led-execution/evidence/m4-calibration/hub-product-comprehension.json`.
- Pair truth: one delegated Task/comparison identity, two count-only Main samples, one explicit
  M4-B assessment and one canonical report.

## Execution contract

Use the same serial pair method and fixed Codex Main profile as M4-D1/M4-D2. The direct Main reads
the bounded historical input and writes the requirements artifact. The delegated Main admits one
Grok 4.6 Xhigh native Goal, supervises independent verification, exactly two different-view Judges,
Main decision and one safe Integration into the delegated comparison project. It does not run the
Hub or solve the Worker's artifact task itself.

At most one evidence-producing validation repair or Main correction is available. No extra
Attempt, replay, replacement, fallback, model switch, Competition or third Judge exists. Do not
rerun a valid negative Main Token result.

## Allowed and forbidden paths

- Writers may touch only their isolated `hub-product-comprehension.json`.
- Main may update only the M4 calibration execution directory, this family evidence JSON and
  existing Goal SSOT after acceptance.
- Strictly forbidden: `src/hub/**`, Hub assets/routes/API/UI, `forklight hub`, product source/tests,
  model ranking/routing changes, credentials, commits, pushes and resets.

## Acceptance and verification

- The artifact names exactly the six bounded future-M5 needs and their historical task-contract
  sources; it preserves `cannot-determine / score-gap-too-small` as historical routing truth.
- `evidenceMode` is `historical-read-only`, `hubMutationOccurred` is false and `m5Implemented` is
  false. It does not claim design, implementation, launch or current routing superiority.
- Preflight truth is an already-existing unrelated Hub process at PID `52551`, port `62182`, state
  `different-build`. Both measured runs must leave that same observation unchanged; they do not
  start, stop, restart, replace, open or otherwise interact with the Hub, and create no Hub/UI or
  product diff.
- Direct and delegated validation use identical input/output/acceptance. Delegated verification,
  two usable Judge results, Main exact-Revision accept and safe Integration pass.
- Both complete terminal usage events bind to the same delegated Task/comparison identity and one
  explicit M4-B assessment is recorded.

Commands:

```text
node validate-artifact.mjs calibration-input.json goals/forklight-main-led-execution/evidence/m4-calibration/hub-product-comprehension.json
git diff --check
forklight hub status --json
forklight main-token status --task-id <task> --comparison-id <comparison> --json
forklight main-token pair-report --task-id <task> --comparison-id <comparison> --json
```

## Handoff and workspace disposition

Handoff includes the two Main usage events, delegated quality chain, exact artifact, pair
assessment and before/after no-Hub-mutation observation. Preserve both comparison roots and the
Task Workspace until the three-family canonical value report and `main-token-pairs.json` are
durable. Cleanup is a later audited storage-lifecycle action; M5 remains gated on 一骏's milestone
confirmation.

## Execution outcome — valid pair, delegated Main higher

The one comparison preserved the pre-existing Hub PID/port/state and produced byte-identical
artifacts. Exact Revision `bacb156c-38d1-430c-a70e-bb191309ff55` passed independent verification,
two usable different-view accepts, Main accept and Integration
`e4208850-e99b-4467-b673-82a3c35a8ab6`. Assessment
`mpa-5bda4279-7e59-4676-a01e-08f889f7363d` accepted equivalence and quality.

Direct Main used 119,202 gross Tokens; delegated Main used 2,128,876. The valid pair reports
`saving.status: higher` and family `cannot-determine / not-strictly-positive`. No replay is allowed.
Full evidence is
`goals/forklight-main-led-execution/evidence/m4-d-hub-comprehension-pair-delivery.md`.
