# M4-E Work Item — current Hub product-comprehension pair

## User result

Produce exactly one distinct current `hub-product-comprehension` Main Token pair. Convert bounded
M2–M4 graduation truth into six future-M5 acceptance requirements while explicitly leaving Hub/UI
and M5 untouched. If the valid equal-quality pair proves delegated Main lower, M4 may proceed to
its boundary verification; every other terminal result stops M4 without replay.

This is not a replay of historical comparison `cmp-m4d-hub-product-comprehension-20260818`. That
pair's six historical UI-oriented requirements, 119,202 direct / 2,128,876 delegated counters and
valid negative assessment remain immutable. The new subject describes current delivery truth and
uses a new input, output, task class, Task, comparison, samples and assessment.

## Background and current evidence

The canonical six-comparison report now proves lower Main Token for both current prerequisite
families:

- `worker-runtime`: 171,984 direct versus 155,804 delegated, saving 16,180;
- `forklight-storage-lifecycle`: 174,536 direct versus 63,097 delegated, saving 111,439.

Historical incomplete/negative evidence stays visible. Only Hub product comprehension remains
`cannot-determine / not-strictly-positive`. Current accepted product truth covers truthful Runtime
and Goal continuation, dependency-ready work, independent verification/Judges/Main decision,
partial-result and Workspace disposition, safe Integration/recovery, and transparent Main Token
value reporting. A bounded requirements projection is therefore the sole ready M4 unit.

Read-only preflight reports a pre-existing unrelated Hub process at PID `52551`, port `62182`, state
`different-build`. It predates this Work Item and is outside its authority.

## `depends_on`

- M2 and M3 are graduated; M4-A/B/C and M4-E delivery are activated.
- The current worker-runtime and host-staged storage pairs are valid and `proven-lower`.
- The historical Hub negative pair and all prior comparison evidence remain readable and immutable.
- Client/Daemon identities match; Grok 4.6 Xhigh resolves `native-goal`; two different-view Judge
  profiles are launchable.
- No product Writer or other M4 comparison is active. This Work Item is serial.

## Inputs

- Bounded accepted input:
  `goals/forklight-main-led-execution/execution/m4-calibration/inputs/hub-current-graduation-comprehension.json`.
- Existing closed validator:
  `goals/forklight-main-led-execution/execution/m4-calibration/validate-artifact.mjs`.
- Existing generic Task renderer:
  `goals/forklight-main-led-execution/execution/m4-calibration/render-task.mjs`.
- Read-only Hub observation: PID `52551`, port `62182`, state `different-build`.
- Complete native `turn.completed` counters from each measured Codex Main session.

## Outputs

- Byte-identical isolated direct/delegated roots containing only the bounded input, validator and
  empty output base.
- One artifact per side at
  `goals/forklight-main-led-execution/evidence/m4-calibration/hub-current-graduation-comprehension.json`.
- One direct and one delegated Main sample under task class
  `m4-current-hub-product-comprehension` and comparison
  `cmp-m4e-hub-current-graduation-20260818`.
- One Grok 4.6 Xhigh native Goal Candidate, ForkLight independent verification, exactly two
  different-view Judge opinions, one measured Main decision and safe isolated Integration.
- One M4-B assessment, scoped pair/value report, canonical seven-comparison report and compact
  delivery or stop packet.

## Execution and Main authority

### Direct Main

One isolated Codex CLI session using `gpt-5.6-sol / xhigh` reads only the bounded input, writes
exactly the output, runs the accepted validator and `git diff --check`, inspects its one-path diff
and stops. It cannot call ForkLight, inspect the source project or interact with Hub.

### Deterministic host staging

Outside measured model sessions, Main's deterministic Host submits the frozen delegated Task and
observes Worker verification plus the fixed two-Judge Review Graph. It makes no semantic decision
and cannot integrate. Observation timeouts end only the observation request; they never cancel,
fail, recreate or widen the durable Task.

### Delegated Main

Exactly one isolated Codex CLI session uses the same profile. It writes no artifact. It receives
the exact ready Candidate, untruncated diff, verification and both Judge results through
`delivery prepare --task <task-id>`. It independently decides the contract. An accept must use the
published positional form exactly:

```text
forklight delivery decide <task-id> --decision accept --revision <revision> --digest <digest> --reason exact-current-hub-comprehension-accepted --timeout-ms 3600000 --confirm --json
```

Only ForkLight may run fresh preflight and Integration after exact accept. The measured sandbox
allows writes only to the delegated root and ForkLight Home, with local control-plane network
access. Source project write, full-access/bypass, web, apps, subagents and granular fallback are
forbidden.

## Allowed paths and orthogonality proof

- Grok/direct Writers: only their isolated copy of the single output path.
- Main planning/operation: this Spec; the new bounded input, prepare script and focused preparation
  test; existing Goal `plan.md`, `progress.md`, `decisions.md`, evidence index/delivery packet and
  `main-token-pairs.json` after acceptance.
- No other Writer is active. The sole Candidate path is disjoint from every previous artifact and
  all product paths. Hidden ordering through one Main profile, Task, Review Graph, assessment and
  canonical report makes the unit serial; there is no useful parallel implementation wave.

## Forbidden paths and non-goals

- No `src/hub/**`, Hub assets/routes/API/UI, `forklight hub start|stop|restart|open`, product
  source/tests, Store implementation, model routing/ranking, Runtime/Profile, credential,
  historical comparison, commit, push, reset or reclaim change.
- No replay of the historical Hub pair, synthetic extra task, Competition, model switch, third
  Judge, automatic replacement or favorable rerun.
- No lock, lease, checksum/content-addressed manifest, version handshake or distributed mechanism.
- No saving claim from host work, receipts, missing counters, incomplete Integration or reduced
  acceptance.

## Acceptance

1. The input/output/task/comparison/sample/assessment identities are new. Old Hub evidence remains
   visible and unchanged.
2. Both roots start byte-identical and use the same task, output and commands.
3. The artifact contains exactly six supplied current-graduation requirements and source refs;
   preserves the historical routing result as historical-only truth; records
   `evidenceMode: current-graduation-read-only`, `hubMutationOccurred: false` and
   `m5Implemented: false`; and makes no implementation or current-routing-superiority claim.
4. Direct/delegated measured sessions both use `gpt-5.6-sol / xhigh` and produce one complete exact
   terminal usage event each. Host staging is disclosed separately and never counted as model work.
5. Delegated Candidate changes exactly one path and passes the same validator/diff check, two usable
   Judges, exact Main decision and safe Integration. One purposeful validation repair or same-
   Worker correction may close one concrete artifact gap while reusing accepted paths.
6. Before and after both runs, Hub remains exactly PID `52551`, port `62182`, state
   `different-build`; no Hub/UI or product diff occurs.
7. Main assesses once only after byte equality, same scope/acceptance and non-lower delegated
   quality. Worker Token, cost, elapsed time and corrections remain separate.
8. Only a valid positive result admits M4 boundary verification. Valid non-positive, invalid,
   incomplete, repeated failure, missing counter, scope/quality drift or source drift stops M4 with
   no replay/replacement. Crossing M4 into M5 still requires 一骏 confirmation.

## Verification commands

In each isolated root:

```text
node validate-artifact.mjs calibration-input.json goals/forklight-main-led-execution/evidence/m4-calibration/hub-current-graduation-comprehension.json
git diff --check
```

Read-only boundary and pair checks:

```text
forklight hub status --json
forklight main-token status --task-id <task> --comparison-id cmp-m4e-hub-current-graduation-20260818 --json
forklight main-token pair-report --task-id <task> --comparison-id cmp-m4e-hub-current-graduation-20260818 --json
forklight value-report --families '["hub-product-comprehension"]' --comparisons '["cmp-m4e-hub-current-graduation-20260818"]' --json
```

The Worker has no absolute duration, Token ceiling or no-progress deadline. Acceptance commands
retain the local 30-minute per-command safety breaker. Observation windows are not Worker limits.

## Handoff and workspace disposition

Handoff names both Codex run refs/counters, deterministic Host staging, Task/Attempt/native Goal,
Revision/digest, verification, Review Graph/Judges, Main decision, receipt/Integration, exact
artifact, assessment/report and before/after Hub observation. Preserve both roots and the Task
Workspace until M4 boundary evidence is durable. This Work Item performs no reclaim.

## Outcome — delivered, proven lower, M4 exit satisfied

Direct run `codex-run:01a01346-7bba-78e3-8a6c-432e91e1cc31` and delegated run
`codex-run:01a01351-2a51-7c62-b1f8-8d2379c3633e` completed under the same profile. Task
`074084d4-4f13-4333-9f9d-52d7971a96ff` produced exact one-path Revision
`60c9fbc8-a29a-4602-adbf-a26dc1f65299`, digest
`80be3fd23deabf61fc31d55c883b44a81d0def883a1358a9b7fc93353d9ba309`. ForkLight independent
verification, two usable different-view accepts, exact Main accept, fresh preflight and Integration
`556105c5-95da-4b8a-b117-51f981c9d019` passed without repair or correction.

All artifact copies have SHA-256
`899d84d895374541e88aa3886eb37707dea85fc9af610a2a27555623ba815a8f`. Assessment
`mpa-cd6c4af0-3fac-4f61-9122-6ec66c60cf26` accepts equal scope/acceptance and non-lower quality.
Direct Main is 120,989 gross Tokens; delegated Main is 75,277, proving a 45,712-Token /
37.78194711915959% saving. The canonical seven-comparison report is `proven`, with all three
families `proven-lower`.

Milestone verification passes build plus 3,096/3,096 tests, matched client/Daemon build
`550f82dd...`, exact live/canonical report parity and unchanged Hub PID 52551 / port 62182 /
`different-build`. M4 is graduated. M5 remains closed pending 一骏's explicit milestone
confirmation.
