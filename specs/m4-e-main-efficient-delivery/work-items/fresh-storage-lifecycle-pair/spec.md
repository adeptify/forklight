# M4-E post-activation Work Item — fresh storage-lifecycle checkpoint pair

## User result

Produce exactly one fresh, meaningful, non-replay `forklight-storage-lifecycle` Main Token pair
using the activated `delivery prepare` and `delivery decide` checkpoints. The bounded result tells
the local developer which current M4-E lineage Task must remain protected and which two delivered
Tasks contain only regenerable space eligible for an explicit later reclaim. This Work Item never
deletes anything.

The pair answers whether the same `gpt-5.6-sol / xhigh` Main completes that equal-quality decision
with strictly fewer gross Main Tokens through ForkLight. A valid non-positive or incomplete result
stops M4; no favorable rerun is admitted.

## Background and current evidence

The old M4-D storage comparison projected three M3-C2 Tasks and stopped without a complete Judge /
Integration chain. It remains immutable and is not replayed. The new subject is current post-M4-E
truth:

- rejected but reusable Task `2d774265-344f-43ea-8f69-79e2624765d3` is
  `protected / unresolved-partial / protect-and-wait`;
- activated M4-E delivery Task `3e2740eb-4c4e-4a55-9a80-86c51c35a5b5` and positive-checkpoint
  Task `da6a7615-2b2a-483b-bb1c-3b8a1269e65b` are both
  `reclaimable / integration-delivered / confirm-reclaim`.

Fresh exact previews report zero unknown bytes, zero owned processes and Store integrity `ok/0`.
The bounded input preserves exact regenerable/durable byte counts. No historical cleanup or actual
reclaim belongs to this comparison.

## `depends_on`

- M4-E delivery is activated and the fresh worker-runtime pair is accepted positive at
  171,984 direct versus 155,804 delegated gross Main Tokens.
- The three exact read-only storage previews above completed against matched client/Daemon truth.
- Grok 4.6 Xhigh is launchable as native Goal; both Main sides use
  `codex-main-gpt-5.6-sol-xhigh-v1`.
- No other M4 comparison or product Writer is active. Hub remains dependency-held and M5 closed.

## Inputs

- Accepted bounded input:
  `goals/forklight-main-led-execution/execution/m4-calibration/inputs/forklight-storage-lifecycle-checkpoint.json`.
- Existing closed validator:
  `goals/forklight-main-led-execution/execution/m4-calibration/validate-artifact.mjs`.
- Current Task preview and accepted/rejected Main-disposition evidence named in the bounded input.
- The same-source, same-profile, terminal-counter method accepted for the fresh worker-runtime pair.

## Outputs

- Byte-identical direct and delegated isolated roots containing only the input, validator and empty
  output base.
- One complete direct Main and one complete delegated Main native terminal-usage event bound to a
  new delegated Task and comparison id.
- One independently verified, dual-Judged, Main-decided and safely integrated delegated artifact:
  `goals/forklight-main-led-execution/evidence/m4-calibration/forklight-storage-lifecycle-checkpoint.json`.
- One explicit M4-B assessment, scoped canonical pair/value report and compact delivery or stop
  packet.

## Execution and checkpoint boundary

### Direct side

One isolated Codex CLI Main using `gpt-5.6-sol / xhigh` reads the bounded input, writes exactly the
single artifact, runs the shared validator and `git diff --check`, then stops. It never calls
ForkLight or changes the source project.

### Delegated side

A separate isolated Codex CLI Main with the same model, effort and terminal-counter method does not
write the artifact. It uses only:

1. one `delivery prepare --task-file ...` with Codex Luna Max and DeepSeek as different-view
   reviewers, a one-hour observation window and bounded included Diff;
2. after inspecting the exact Candidate and both Judge results, one
   `delivery decide ... --decision accept` bound to the returned Revision/digest and a one-hour
   observation window.

Observation timeout permits exact checkpoint re-entry only; it never cancels, recreates or widens
the Task. Granular submit/wait/inspect/review/main-review/preflight/apply fallback is forbidden
inside the measured delegated Main run.

## Allowed paths

- Direct and delegated Writers: only their isolated
  `goals/forklight-main-led-execution/evidence/m4-calibration/forklight-storage-lifecycle-checkpoint.json`.
- Main operational paths: this accepted Spec and existing
  `goals/forklight-main-led-execution/execution/m4-calibration/` preparation files.
- After acceptance, Main may copy the exact delegated artifact to the existing Goal evidence path
  and update only the existing Goal SSOT/evidence index.

## Forbidden paths and non-goals

- No product source/tests, old M4-D artifacts, Store mutation, actual reclaim, Hub/UI, routing,
  Worker Profile, credential, Git remote/history, commit, push or reset change.
- No second output, model switch, Competition, third Judge, whole-pair replay, automatic
  replacement or later-family calibration before this result.
- No lock, lease, checksum/content-addressed manifest, version handshake or distributed
  coordination. Byte equality protects only against comparing different source inputs.
- No saving claim from exchange receipts, missing counters, reduced acceptance or incomplete
  evidence.

## Acceptance

1. New identity is proven by the current M4-E subject, new Task class, output path, Task,
   comparison, samples and assessment; no old M4-D sample or assessment is reused.
2. Direct/delegated roots start byte-identical and receive the same task, output and commands.
3. Both Main sides use `gpt-5.6-sol / xhigh` with complete native terminal counters.
4. The exact artifact protects only `2d774...`, marks only `3e274...` and `da6a...` reclaimable,
   preserves the supplied reasons, byte counts, zero unknown/process truth and Store `ok/0`, and
   states `reclaimExecuted: false`.
5. Direct validation passes. The delegated Candidate changes exactly one path, passes the same
   validator and diff check, receives two usable different-view Judge opinions, exact Main accept
   and isolated safe Integration.
6. Measured delegated Main uses only `delivery prepare` and `delivery decide`, with no granular
   fallback, Task recreation or source-project access.
7. Main records one assessment only after equal scope/acceptance and non-lower delegated quality
   are evidenced. Worker Token/cost/time remain separate from Main Token.
8. Only direct gross minus delegated gross greater than zero admits Hub as the next family. Zero,
   higher, invalid or incomplete stops M4 and leaves M5 closed.

## Verification commands

In each isolated root:

```text
node validate-artifact.mjs calibration-input.json goals/forklight-main-led-execution/evidence/m4-calibration/forklight-storage-lifecycle-checkpoint.json
git diff --check
```

For the delegated Task and pair:

```text
forklight main-token status --task-id <task> --comparison-id <comparison> --json
forklight main-token pair-report --task-id <task> --comparison-id <comparison> --json
forklight value-report --families '["forklight-storage-lifecycle"]' --comparisons '["<comparison>"]' --json
```

The Worker has no absolute duration or Token ceiling. Acceptance commands keep the local 30-minute
per-command safety breaker; one-hour Main observation windows remain observation-only.

## Handoff and workspace disposition

Handoff names both Codex run refs/counters, direct validator evidence, delegated
Task/Attempt/native Goal/Revision/verification/Review Graph/receipt/Integration, exact artifact,
assessment and canonical saving result. Preserve both roots and the Task Workspace until these
records are durable. Then only normal audited storage preview/reclaim may remove regenerable
terminal space; this Work Item performs no reclaim.

One purposeful validation repair or same-Worker correction may close one concrete artifact gap.
Repeated failure, missing terminal counters, quality/scope drift, granular fallback need or a valid
non-positive result stops this Work Item. There is no replacement pair.

## Outcome

Completed on 2026-08-18 with one valid non-positive pair. The direct and delegated artifacts are
byte-identical and the full delegated quality chain passed, but direct Main used 154,171 versus
delegated Main 553,038 gross Tokens. Assessment
`mpa-c5b26b85-6853-4882-998b-12db3b04b9a8` accepts all quality gates; canonical saving status is
`higher` by 398,867 Tokens. Per this accepted contract, no replay or Hub pair is admitted. M4 stops
and M5 remains closed. Exact evidence:
`goals/forklight-main-led-execution/evidence/m4-e-fresh-storage-lifecycle-pair-stop-decision-packet.md`.
