# M4-E Work Item — host-staged storage-lifecycle checkpoint pair

## User result

Produce exactly one distinct `forklight-storage-lifecycle` Main Token pair after 一骏 explicitly
revoked the stopped Main-offline Work Item's no-replacement boundary. The artifact preserves one
current reusable partial and identifies two current integration-delivered Tasks whose regenerable
Workspace state is eligible for a separately confirmed later reclaim. This Work Item deletes
nothing.

The old valid 154,171-direct / 553,038-delegated negative, the stopped 128,302-direct root and both
46,366 / 46,796 pre-admission delegated events remain immutable. No old sample, artifact, Task,
comparison or assessment is reused. A valid non-positive, invalid or incomplete result stops M4
without another pair.

## Background and current evidence

Fresh serial previews against matched build `2399b5af...` report:

- rejected reusable Task `2d774265-344f-43ea-8f69-79e2624765d3` remains
  `protected / unresolved-partial / protect-and-wait`, with 142,781,211 regenerable bytes,
  5,515,895 durable bytes, zero unknown bytes and no owned process;
- activated Main-offline delivery Task `6676be3a-24b4-4bbc-a8c0-7f2a3079e848` remains
  `reclaimable / integration-delivered / confirm-reclaim`, with 159,988,810 regenerable bytes,
  13,865,918 durable bytes, zero unknown bytes and no owned process;
- newly delivered Daemon safety Task `b664e69e-ee30-4268-8de3-1f7c07fb808d` is
  `reclaimable / integration-delivered / confirm-reclaim`, with 147,589,524 regenerable bytes,
  6,735,929 durable bytes, zero unknown bytes and no owned process.

Every preview reports SQLite `quickCheck: ok` and zero foreign-key violations. This subject differs
from both earlier storage subjects by using the newly delivered `b664...` Task and a new output,
Task class and comparison identity.

## `depends_on`

- Main-offline usage episodes and permission-aware Daemon probing are delivered and activated.
- 一骏 explicitly revoked only the stopped storage Work Item's no-replacement boundary and
  authorized one distinct subject; favorable replay and old-sample reuse remain forbidden.
- Grok 4.6 Xhigh is launchable as native Goal; Codex Luna Max and DeepSeek Pro are launchable as
  different-view Judges.
- Client and Daemon identities match. No product Writer or other M4 comparison is active.
- Hub/UI remains dependency-held and M5 closed.

## Inputs

- Bounded accepted input:
  `goals/forklight-main-led-execution/execution/m4-calibration/inputs/forklight-storage-lifecycle-host-staged-checkpoint.json`.
- Existing closed validator:
  `goals/forklight-main-led-execution/execution/m4-calibration/validate-artifact.mjs`.
- Existing generic delegated Task renderer:
  `goals/forklight-main-led-execution/execution/m4-calibration/render-task.mjs`.
- The three exact current previews above and accepted Main dispositions.
- Complete native `turn.completed` counters from each measured Codex Main session.

## Outputs

- Byte-identical direct and delegated isolated roots containing only the input, validator and empty
  output base.
- One new direct Main sample and one new delegated Main sample under task class
  `m4-host-staged-storage-lifecycle-checkpoint` and comparison
  `cmp-m4e-storage-host-staged-20260818`.
- One artifact per side at
  `goals/forklight-main-led-execution/evidence/m4-calibration/forklight-storage-lifecycle-host-staged-checkpoint.json`.
- One Grok 4.6 Xhigh native Goal Candidate, ForkLight independent acceptance, two different-view
  Judge opinions, one measured Main semantic decision and safe isolated Integration.
- One M4-B assessment, scoped pair/value report and compact delivery or stop packet.

## Execution and Main authority

### Direct Main

One isolated Codex CLI session using `gpt-5.6-sol / xhigh` reads the bounded input, writes exactly
the artifact, runs the accepted validator and `git diff --check`, inspects its one-path diff and
stops. It never calls ForkLight or accesses the source project.

### Deterministic host staging

Outside every measured model session, Main's deterministic host invokes the accepted public
`delivery prepare` checkpoint with the frozen Task, Reviewer order and reason. It may observe and
re-enter the exact Task after an observation timeout until Worker verification and both frozen
Judges are terminal. This host path performs no semantic review, records no Main decision and
cannot start Integration. Observation timeout never cancels, fails, recreates or widens work.

### Delegated Main

Exactly one isolated Codex CLI session uses the same model and effort. It writes no artifact. It
re-enters `delivery prepare --task` once to receive the exact Candidate, bounded diff, verification
and both Judge results. It independently decides whether the closed contract is satisfied. Only if
the answer is yes may it call `delivery decide accept` bound to the exact Task, Revision and digest;
otherwise it records `revise` or `reject` and stops. ForkLight performs preflight and isolated safe
Integration only after exact accept.

The measured runner uses `workspace-write`, the isolated delegated root and exact ForkLight Home as
its only writable roots, plus `sandbox_workspace_write.network_access=true` for the local control-
plane connection. The source project is not writable; full-access/bypass, web, apps, subagents and
granular ForkLight fallback are forbidden.

## Allowed paths

- Grok and direct Main Writers: only their isolated copy of the one output path above.
- Main operational paths: this Spec; the new input, preparation script and focused preflight test;
  the existing Goal `plan.md`, `progress.md`, `decisions.md` and evidence index/packet.
- After acceptance, Main may copy the exact delegated artifact to the existing Goal evidence path
  and update the canonical `main-token-pairs.json`.

## Forbidden paths and non-goals

- No product source/test, old pair root/artifact/sample/assessment, Store implementation, actual
  reclaim, Hub/UI, routing, Runtime, Worker Profile, Provider credential, Git remote/history,
  commit, push or reset change.
- No model-active waiting while Worker or Judges run; no granular submit/wait/inspect/review/main-
  review/preflight/apply fallback inside the measured delegated session.
- No Task recreation, model switch, Competition, third Judge, automatic replacement or later-
  family work before this result.
- No lock, lease, checksum/content-addressed manifest, version handshake or distributed mechanism.
- No saving claim from receipts, host execution, missing terminal counters or reduced acceptance.

## Acceptance

1. Input, output, Task class, Task, comparison, samples and assessment are new and distinct; every
   old storage result and stopped event remains visible and unchanged.
2. Direct/delegated roots start byte-identical and use the same task, output and commands.
3. Both measured Main sessions use `gpt-5.6-sol / xhigh` with one complete exact terminal event;
   deterministic host calls are disclosed separately and never mislabeled as Main Token.
4. The artifact protects only `2d774...`; marks only `6676...` and `b664...` reclaimable; preserves
   exact status/disposition/classification/reason/action/byte/process/integrity truth; and records
   `reclaimExecuted: false`.
5. Direct validation passes. Delegated Candidate changes exactly one path, passes the same
   validator/diff check, receives two usable different-view Judge opinions, exact Main decision and
   safe isolated Integration only after accept.
6. Receipts prove the measured delegated session uses only exact `delivery prepare` and `delivery
   decide`; Host staging is deterministic, has no model session and records no Main verdict.
7. Main assesses once only after byte equality, same scope/acceptance and delegated quality not
   lower. Worker Token, cost, time and repair truth remain separate from Main Token.
8. Only direct gross minus delegated gross greater than zero admits Hub. Valid non-positive,
   invalid, incomplete, repeated failure or missing counters stops M4 with no favorable rerun.

## Verification commands

In each isolated root:

```text
node validate-artifact.mjs calibration-input.json goals/forklight-main-led-execution/evidence/m4-calibration/forklight-storage-lifecycle-host-staged-checkpoint.json
git diff --check
```

For the delegated Task and pair:

```text
forklight main-token status --task-id <task> --comparison-id cmp-m4e-storage-host-staged-20260818 --json
forklight main-token pair-report --task-id <task> --comparison-id cmp-m4e-storage-host-staged-20260818 --json
forklight value-report --families '["forklight-storage-lifecycle"]' --comparisons '["cmp-m4e-storage-host-staged-20260818"]' --json
```

The Worker has no absolute duration, Token ceiling or no-progress deadline. ForkLight acceptance
commands retain the local 30-minute per-command safety breaker. Host/Main observation windows are
not Worker deadlines.

## Handoff and workspace disposition

Handoff names both Codex run refs/counters, every deterministic Host receipt, delegated
Task/Attempt/native Goal/Revision/verification/Review Graph/Judges/Main decision/receipt/Integration,
exact artifact, assessment and pair result. Preserve both roots and the Task Workspace until all
records are durable. This Work Item performs no storage reclaim.

One purposeful validation repair or same-Worker correction may close one concrete artifact gap.
Stop on repeated failure, missing/ambiguous terminal evidence, scope/quality drift, source drift,
granular fallback need or any boundary expansion. There is no replacement pair.

## Evidence amendment — one same-Judge summary repair

Host staging produced a verified exact Candidate and one usable Codex Luna accept. The frozen
DeepSeek Pro Judge also returned `accept` with one unchanged `info` finding, but its otherwise-valid
summary is 508 characters against the published 500-character maximum. ForkLight therefore stored
the assignment as `schema-violation`; this is not a Candidate or semantic-quality failure.

The existing one-shot `review-graph repair-result` contract is exactly applicable. Main authorizes
one same-Judge, summary-only repair for assignment `75297ca6-131f-4c3d-ba64-668f5192d4b9`: same
frozen DeepSeek identity, same Candidate Revision, disposition and findings; only the summary may
be shortened. The original failed assignment remains durable. No Candidate rerun, replacement or
third Judge, Main model session, automatic retry or further result repair is allowed. Semantic
drift or another unusable result stops the Work Item before measured Main or Integration.

## Terminal outcome — incomplete evidence, no replay

The one authorized same-Judge repair succeeded without semantic drift and left two usable accepts.
Measured delegated Main run `codex-run:01a012fa-57d8-76d3-b31f-eb26d3fe18a5` then inspected the
exact ready checkpoint and correctly judged the Candidate acceptable. Its only `delivery decide`
call nevertheless used `--task <id>` where the published command requires positional `<task-id>`.
The CLI rejected the invocation before recording Main review or starting Integration. No receipt or
Integration result exists and the delegated root output remains zero bytes.

Both complete count-only Main events are retained: 174,536 direct and 63,097 delegated gross.
ForkLight reports `cannot-determine / incomplete-evidence` and saving `unavailable`; the apparent
111,439 count difference is not an accepted saving. The no-favorable-replay rule now stops this Work
Item. No resume, corrected decide, Host-side fallback, replacement pair, assessment or Hub work is
allowed. Preserve the pair roots, Candidate, Judge lineage and samples. Exact evidence is
`goals/forklight-main-led-execution/evidence/m4-e-host-staged-storage-pair-stop-decision-packet.md`.

## Authorized decision-recording-only recovery

一骏 explicitly supersedes only the preceding one-session/no-corrected-decide boundary. The failed
measured Main session already inspected the exact untruncated Candidate, passed verification and
both usable Judges, selected `--decision accept`, and stopped only because it supplied the Task id
through unsupported `--task` syntax. That terminal transcript is the frozen semantic decision.

Main may now invoke exactly once:

```text
forklight delivery decide 214a49f1-87bf-4f41-b165-5a3ca4c68c12 --decision accept --revision 74f1ba9c-eb3a-47fc-a144-50bb997e19ab --digest 087352f491ec62d7626b96e3bd2f164fce911e2581cd425c78f3d1eae7eeed0b --reason exact-host-staged-storage-lifecycle-accepted --timeout-ms 3600000 --confirm --json
```

This is mechanical recording and execution of the already measured semantic accept, not a new
comparison-model session or a new review. It therefore does not create another Main Token segment
or modify either captured sample. Deterministic root orchestration around both comparison sides
remains outside the frozen isolated Main measurements; no unmeasured semantic judgment is added.

The command may record the fresh Main review, run its one fresh exact-bound preflight and safe
isolated Integration. There is no prepare re-entry, Candidate/verification/Judge rerun, repair,
correction, retry, fallback, Host substitution, manual apply or replacement pair. Any command
failure is terminal. On success, Main performs only deterministic post-state validation, byte
equality and the one normal M4-B equivalence/quality assessment, then reads pair/value reports.

## Recovery outcome — delivered and proven lower

The one authorized positional decision call completed. ForkLight recorded the frozen Main accept,
passed fresh preflight receipt `b1815fd9-b890-434d-b2bc-e4412439d0eb`, and applied Integration
`91dfa097-3ef3-41f1-918c-77166e128430`. Source verification reran both accepted commands. Direct,
Candidate Workspace and delegated integrated artifacts are byte-identical at SHA-256
`755016be82f5cbf8c960b0fb246238e13d245db799f608a3a5b841c881176be4`.

Assessment `mpa-0eb1cbb7-9c5c-42c7-a5cd-664041d3f7e2` accepts same scope, same acceptance and
delegated quality not lower. The immutable samples remain 174,536 direct and 63,097 delegated
gross Tokens. The accepted pair therefore proves a 111,439-Token saving, or
63.848718888939814%. No new comparison-model session, Candidate, Judge or retry ran. This valid
strictly positive storage result admits exactly one current Hub comprehension pair; it does not
start or modify Hub/UI and does not open M5.
